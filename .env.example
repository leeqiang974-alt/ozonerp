# Store real Ozon credentials only in a local .env or secrets manager. Never commit them.
OZON_API_BASE_URL=https://api-seller.ozon.ru
APP_ENV=development
# 正式环境使用 PostgreSQL；自动化测试仍使用独立 SQLite。
DATABASE_URL=postgresql+psycopg://ozon_erp:replace-me@127.0.0.1:5432/ozon_erp
DEFAULT_LOCALE=zh-CN
DEFAULT_CURRENCY=CNY
# Generate once with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ERP_CREDENTIAL_ENCRYPTION_KEY=replace-with-a-fernet-key

# LLM translation service (for Chinese -> Russian product content generation)
# Optional: when configured, product titles/descriptions are AI-translated to Russian.
# When not set, a dictionary-based fallback is used and content is marked as unverified.
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
