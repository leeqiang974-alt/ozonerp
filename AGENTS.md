# Ozon FBS ERP — 最高开发指示

## SKU 图片独立选择规则（2026-08-28）

- 商品编辑器的公共产品图库与 SKU 图片选择必须分开持久化。`listing_variants.image_urls_json` 为可选的每 SKU 图片集合；`NULL` 仅表示历史草稿沿用“SKU 首图 + 公共图库”的兼容行为。
- SKU 图片弹窗的取消、删除、排序和设首图只修改当前 SKU 的集合，不得修改公共图库或其他 SKU；取消后的图片仍须留在可选来源池中，允许重新勾选。
- Ozon 提交优先使用显式 SKU 图片集合，`images[0]` 必须是该 SKU 首图，最多 15 张；旧草稿继续走兼容回退。
- 验证要求：后端 schema/迁移加载、提交图片构造单元测试、`node --check frontend/listing-editor.js`、前端真实页面勾选/取消/重新勾选、`/health` 与草稿接口回读。

## Ozon 前台“款式 × 尺寸”采集与编辑规则（2026-09-03）

- Ozon 前台商品的款式/颜色与尺寸必须保留为两个独立维度：采集器按每个款式展开其可选尺寸，输出 `颜色: 款式 / 尺寸: 尺寸` 的结构化 SKU 证据；禁止把每个 SKU 的缩略图平铺成商品颜色样本。
- 编辑器须先按款式/颜色分组：每组仅展示一张颜色样本和一套组图，组内再逐行展示对应尺寸 SKU。任何来源 SKU 的可读 `属性: 值 / 属性: 值` 文本都必须解析为字段对，并按名称映射 Ozon 颜色与尺寸属性，禁止按列序猜测而混合款式。
- 验证：结构化采集测试至少覆盖“一个款式、多个尺寸”；编辑器语法检查通过；真实页面复采后确认 4 个款式显示为 4 组，组内尺寸行数与来源页面一致。

## 类目属性预检规则（2026-08-28）

- Ozon 类目/类型、属性模板和字典值是跨店铺共享数据。业务读取、预检、提交构造和颜色映射只能使用 `ozon_global_*` 缓存，不能按 `shop_id` 回退或复制一份店铺缓存。
- 旧版 `ozon_category_cache`、`ozon_attribute_cache` 和店铺字典表只允许作为一次性迁移来源；启动或首次访问时应把历史行合并到全局表，之后不得再写入旧表。修改后必须验证店铺 A 的缓存能被没有任何店铺级缓存的店铺 B 直接使用。

## 北极星目标

交付一个中文、CNY-only、面向多店铺 Ozon FBS 运营的可操作 ERP。它必须让运营人员在一个工作台内安全完成：店铺接入、商品与库存同步、FBS 订单履约、价格/营销控制和经营异常处理。

## 不可偏离的优先级

1. **数据正确与操作安全**：店铺数据隔离、凭据加密、只读同步优先、写回需审批和审计。
2. **FBS 日常闭环**：先打通“接店 → 同步商品/订单 → 拣配发货 → 风险处理”，再扩展功能。
3. **简易中文操作**：固定左侧导航、全局店铺筛选、风险优先队列、列表批量操作；金额只用 CNY。
4. **可观测与可恢复**：每次同步、价格决策和人工操作都可追踪、可重试、可解释。

## 范围控制

- 新需求必须映射到当前阶段目标、实体模型、Ozon API 能力和验收标准；不满足时先记录到后续阶段，不临时改变架构主线。
- 不因单个页面、一个店铺或一次异常而跳过持久化、权限、同步记录、审计或测试。
- 未完成的店铺可删除；已有商品、订单或审计等业务数据的店铺只能停用/归档，不能删除。
- API Key 绝不明文返回、写入日志或提交 Git。生产使用环境变量/密钥服务；开发仅可使用 Git 忽略的本机加密密钥。

## 变更规则

每个阶段结束必须更新 `docs/MASTER_PLAN.zh-CN.md`、`docs/SESSION_HANDOFF.zh-CN.md`，并运行相关测试。若用户临时需求与本指示冲突，应说明影响、给出不偏离主线的实现方式，再继续执行。

## Ozon API 开发指导规则

- docs/MASTER_PLAN.zh-CN.md 是最高开发方向，所有开发按阶段顺序推进，不因单个需求跳过。
- Ozon Seller API 官方文档 https://docs.ozon.ru/api/seller/zh/ 是唯一 API 对接依据。
- 离线快照位于 d:\\Desktop\\ozonseller api\\Ozon Seller API 文件20260804_files\\saved_resource.html（Redoc 格式，5.8MB）。
- 端点参考已整理为 docs/OZON_API_REFERENCE.zh-CN.md，按 ERP 阶段分组，共 264 个端点。
- 新增或变更 API 对接前，必须先查阅官方文档确认接口路径、请求体和响应结构，不得凭记忆或旧文档猜测。
- language 参数值以官方文档枚举为准：DEFAULT、RU、EN、TR、ZH_HANS（中文简体）。
- 旧接口 /v1/warehouse/list 已被 Ozon 废弃，禁止接入；仓库列表使用 /v2/warehouse/list。

## 本地界面展示规则

- 在告知用户“页面已打开/已显示”之前，必须先验证前端 `http://127.0.0.1:5500/` 返回 HTTP 200，并确认后端 `/health` 正常；若静态服务停止，应以隐藏后台进程恢复后再打开或强制刷新浏览器标签页。
- 验证方式：请求首页并确认返回非空 HTML，再请求后端健康检查；仅创建或聚焦浏览器标签页不算完成展示。
- 已发布商品的结果查看必须保持单层逻辑：发布记录天然使用其草稿绑定店铺；采集箱仅提供每行独立勾选的“刷新所选”和单独的“刷新全部”，表头不放全选框，避免与“刷新全部”重复。结果直接回写库存、Ozon 问题与真实评分。不得再次要求运营选择发布店铺，也不得把“查看结果”拆成额外任务流程。
- Ozon 内容评分接口未返回 `rating` 时必须保存并显示为“暂未评分”，不得用默认值 `0` 冒充真实的 `0/100`；真实零分只有在响应明确包含 `rating: 0` 时成立。
- 提交 Ozon 图片前必须过滤字符串 `None`、`null`、`undefined` 和非 HTTP(S) 地址，并实际验证每张远程图可下载、最小边不低于 200px、且不超过 4320×7680；采集数据中的空图片字段或低分辨率 SKU 缩略图不得进入 `items[].images`，否则 Ozon 会返回 `invalid URL`、`pics_invalid_dimensions` 或主图下载失败。低分辨率 SKU 图只从本次 Ozon payload 排除，保留本地 SKU 素材；有合规公共图时由它成为 Ozon 首图，无合规图则阻止提交。
- “从 Ozon 拉取”应在全部已接入店铺中按 Offer ID 搜索并在结果中显示所属店铺；不能以顶部店铺筛选作为前置条件。所有 dialog 的右上角关闭与取消必须独立于表单 submit，并做真实点击验收。
- 商品编辑器采用“左侧商品工作队列 + 中间编辑表单 + 右侧采集素材”的稳定结构。左侧状态必须由当前店铺货源与最新草稿/提交事实实时归并为未编辑、已编辑、已发布；点击已有草稿/已发布商品必须携带正确 `sp` 和 `draft`，不得新建重复草稿。存在未保存的人工修改时，切换商品必须先确认。
- 商品工作队列必须按店铺在会话中持久化滚动位置；刷新或切换商品后先恢复原位置，再确保当前高亮项至少进入可视区。已有草稿加载时禁止异步“生成下一个 Offer ID”覆盖草稿编号。
- 编辑器产品图库的大图查看必须支持在同一放大层内使用左右箭头连续浏览，并显示当前位置/总数；同时支持键盘方向键、Esc、关闭按钮和点击遮罩关闭。不得要求运营逐张关闭后重新点开。

## Ozon 商品属性字典规则

- Ozon 单个类目属性字典可能超过 6 万条，禁止在选择类目时全量请求或渲染。字典属性统一采用“至少输入 2 个字 → 服务端搜索 → 单次最多 50 条 → 本地结果缓存 24 小时”的模式。
- 字典型必填属性必须保存 Ozon 返回的 `value_id`，不能把运营人员任意输入的文本当作合法字典值；自由文本属性才允许只保存 `value_text`。
- 字典读取、保存和预检必须使用同一份全局 Ozon 字典缓存；不得前端从全局菜单读取、后端却只查店铺旧缓存。有效 `value_id` 应反查并保存 Ozon 的标准文字，不能因旧文字、翻译或尾随空格误拒绝人工菜单选择。
- 修改属性字典流程后，至少验证：首次搜索、重复缓存搜索、结果数量上限、未选择返回项时前端阻止保存，以及后端预检拒绝缺少 `value_id`。

## 浏览器采集扩展升级规则

- 已打开网页中的 MV3 内容脚本在扩展重新加载后不会自动移除旧的 `chrome.runtime.onMessage` 监听器；禁止用同页 `executeScript` 注入来声称解析器已经升级，因为旧监听器可能先返回过期 payload。
- 采集前握手版本不一致时，扩展必须刷新当前详情页、等待加载完成、再校验内容脚本版本后才允许采集。每次改变解析行为必须同步提高内容脚本握手版本、popup 期望版本与 manifest 版本。
- 验证：对已经打开的商品页完成扩展重新加载后，首次采集应自动刷新一次；ERP 回读必须确认标题、非营销图库 URL、价格/评分和变体来自本次 payload，不能只以 popup 的“已更新”提示判定成功。

## Ozon 公共商品页采集规则

- Ozon 公共商品详情优先使用同源只读页面 JSON：`composer-api.bx/page/json/v2` 读取 `webGallery`、`webAspects`、`webPrice`；`entrypoint-api.bx/page/json/v2` 读取 `webCharacteristics`、`webDescription` 与富内容。页面可见 DOM 只能作为结构化数据不可得时的回退。
- 每个结构化页面请求必须设置有限超时。首次采集只依赖当前商品页的 `webAspects`、图库和详情结构；不得把逐个打开变体链接作为入库前置。任何单个变体接口卡住时，采集器必须回传已取得的结构化数据或 DOM 回退，禁止让整个扩展弹窗永久停在“采集中”。
- 必须按 `webAspects` 的变体链接读取各款式 SKU；款式图库保存在 `variant_groups`，描述区图片保存在 `detail_images`，两者不得混入公共产品图库，也不得把 RUB 价格写为 CNY 采购价。
- 禁止读取 Ozon Seller Cookie、调用 Seller 写接口或使用任何伪造/拦截 Ozon 卖家后台响应的逻辑；该采集器只读公共商品页并回传 ERP。

## 自我改进规则 (Self-Improving Rules)

### 规则 24：快速审核图片翻译必须验证草稿路由闭环

- **问题**：快速审核调用 `/listing-drafts/{draft_id}/translate-images`，但后端只有通用 `/api/v1/image/translate`，导致前端只显示 `not found`，实际象寄接口并未被调用。
- **规则**：新增或修改前端 API 调用时，必须同时检索后端路由；草稿级图片翻译路由统一复用象寄通用翻译实现，返回逐图 `original_url`、`translated_url`、`error` 和 `request_id`，成功结果必须持久化到草稿并支持部分成功。
- **验证**：用真实草稿和真实图片调用草稿级路由，确认 HTTP 200、图片 URL 写回数据库；同时检查 `/health`、前后端语法，并验证供应商失败时前端显示真实错误而非笼统 `not found`。

### 规则 25：快速审核优先复用编辑器业务接口

- **问题**：快速审核曾为分类和图片翻译重新编排一套逻辑，造成重复匹配、无意创建草稿和接口漂移。
- **规则**：快速审核只负责轻量展示、选择和确认；分类匹配必须调用 `/ai/match-category`，图片翻译必须调用 `/api/v1/image/translate`，草稿保存必须调用既有 `PUT /listing-drafts/{draft_id}`。只有运营明确应用分类、翻译回填或保存尺重时才允许创建/修改草稿。
- **验证**：打开无草稿商品只做分类查看时，草稿数量不增加；分类匹配只发起一次；图片翻译请求与编辑器使用相同请求体和返回结构。

### 规则 26：1688 官方详情是缺失价格和视频的补采来源

- **问题**：自动化采集快照可能漏掉 SKU 价格和视频。1688 官方详情接口对同一 Offer 仍可能返回 `skuInfos[].pifaPrice/consignPrice` 与 `productInfo.mainVedio`，不能据此判断供应商没有数据。
- **规则**：采集器必须兼容这些官方字段；编辑草稿提供显式“从1688官方详情补采价格/视频”动作，只填补缺失证据，不覆盖人工修改。价格补回后调用统一定价策略生成售价、原价和最低价；视频统一转为 `media.woxq.cn/proxy` 后写入源媒体和草稿。
- **验证**：SRC107 官方详情应返回两个有货 SKU 的 34.99 CNY、一个视频 URL；补采后草稿变体有采购价和售价，采集箱/编辑器可显示视频。

### 规则 27：采集颜色可自动映射，但必须保留 Ozon 字典 ID

- **规则**：SKU 颜色先按当前类目 Ozon 字典精确匹配，再按本地颜色族（如全黑色→黑色、米黄色→米色）进行高置信规范化匹配；只有命中字典项时才写入该项 `value_id`。低置信匹配只保留人工候选，禁止写入 `manual:*` 伪 ID 或猜测 ID。
- **展示**：变体表格必须标明“自动匹配”，运营可通过颜色下拉立即覆写；保存草稿后将值和 ID 一并持久化。
- **验证**：SRC107 打开草稿后“全黑色”显示为“黑色”、`value_id=61574`；“米黄色”显示为“米色”、`value_id=61573`；手动改色后保存保持人工选择。

### 规则 21：前端入口绑定必须容错并验证真实导航

- **问题**：主页面脚本直接绑定不存在的 `#add-listing`，初始化在加载阶段中断，采集箱导航随后完全失效；采集箱加载函数又曾只存在于 `setView()` 的局部作用域。
- **规则**：可选页面控件必须先判空再绑定；跨视图加载函数必须通过明确的 `window` 接口或顶层函数暴露。修改后必须用真实浏览器重新加载并验证目标导航、列表请求、弹窗交互和控制台错误，而不是只做语法检查。
- **验证**：`node --check frontend/app.js` 通过；`http://127.0.0.1:5500/index.html` 与 `/health` 返回 200；点击“采集箱”后出现商品列表，点击“快速审核”后弹窗打开且无应用控制台错误。

### 规则 22：Ozon 评分与反馈必须按 KC/Offer ID 追加留档

- **问题**：评分、问题和库存反馈只写在当前草稿字段，采集箱渲染调整后容易不可见；草稿重建时也会失去过去的 Ozon 反馈证据。
- **规则**：每次 Ozon 反馈同步必须同时写入不可覆盖的审计快照，键为 `shop_id + KC/Offer ID`，包含评分、问题、审核/校验状态、Ozon 商品 ID、反馈分组和同步时间。采集箱按该 KC 返回最新快照和历史次数；“有问题”不得遮蔽评分。
- **验证**：历史草稿回填后，`KC000021` 在采集箱接口返回 `overall_rating=82`、9 条问题和 `feedback_count=1`；前端同一提交结果单元格显示“9 项问题 / 82/100 / 反馈 1 次”。

### 规则 23：采集箱是核心稳定面，任何改动必须跑回归清单

- **问题**：为了加快速审核模块和图片放大等小功能，动了 `renderCollectionBox` 与顶部工具栏，导致全选框、刷新所选/刷新全部/删除所选三个按钮失效、列数从 10 列退化成 6 列、KC 编号和提交结果全部丢失、点击图片放大功能被删掉，整个采集箱可用性大幅回退。
- **规则**：
  1. 采集箱的 DOM 结构（10 列表头、工具栏 4 个操作按钮、全选框、行勾选、操作列）是核心稳定面，任何新增功能必须以**注入 / 追加**方式实现，不得整体重写或替换 render 函数。
  2. 任何改动 `app.js` 中 `renderCollectionBox` / `loadCollectionBox` 的提交，必须对照下面的回归清单逐项验证，不得只验证新增功能。
  3. 修改后必须在真实浏览器里走完清单再回复"已修复"。
- **采集箱回归清单**（改完必须全部通过）：
  1. 列表正常加载，列数为 10 列（选择 / 编码 / 商品名称 / 来源 / Ozon店铺 / 采集日期 / 状态 / 采集发布标识 / 提交结果 / 操作）
  2. 表头全选框存在，可全选/半选/取消全选
  3. 刷新所选结果按钮：未勾选禁用，勾选后可用，点击后调用刷新接口并回写
  4. 刷新全部结果按钮：点击后刷新当前列表全部有草稿的货源
  5. 删除所选按钮：未勾选禁用，勾选后可用，确认后删除并从列表移除
  6. 每行有独立勾选，勾选后按钮状态同步
  7. KC 编号和货源 ID 正常显示
  8. 商品缩略图显示，点击可放大查看大图，关闭正常
  9. 提交结果列显示评分、问题数、反馈次数
  10. 快速审核按钮存在，点击弹窗打开无控制台错误
  11. 编辑按钮跳转正确
  12. 浏览器控制台无新增 JS error
- **验证**：任何采集箱相关改动交付前，必须在真实浏览器或截图中展示上面 12 项至少各一张证据；少一项就不算完成。

### AI 商品套图规则

- Terra 图片分析可以读取采集商品的多张图片，并逐张记录图片角色、OCR 可读文字、事实证据和是否适合作为参考图。
- 付费 Image 2 编辑每个槽位只允许发送 1 张经过分析筛选的产品参考图；尺寸图、宣传图和含大量文字的图片不得作为第二参考输入。分析多图不等于生图发送多图。
- 套图按槽位独立持久化。任一槽位 HTTP/网关/内容错误不得中断后续槽位，也不得清空已经成功生成的图片；只有全部槽位失败才将任务标记为 `failed`，部分成功保持可选择和可应用。
- 失败信息必须保留槽位、供应商响应和请求元数据；人工审核至少关注产品身份、SKU/颜色/尺寸事实冲突、OCR 文案和参考图适用性。未有证据的属性不得写入 Prompt。
- 验收必须覆盖：单参考图 multipart、首槽位失败后后续槽位仍执行、部分结果可 apply、失败任务不丢失成功图片。
- 每次点击 AI 生图必须创建独立运行批次；每个槽位在“准备参考图 → 已发送供应商请求 → 已收到响应 → 已落图/失败”各阶段持久化，不得只保留最终状态。
- 生图界面固定展示五个槽位及各自状态；允许运营选择“生成全套”或“只生成主图”，并允许对失败/中断/已生成槽位单独重做。单槽位重做必须只向后端提交该槽位，保留其他槽位已有成功图片和历史账本，禁止为重做一张而再次发送整套五张。
- 进程重启时，任何运行中套图一律标记为 `interrupted`，不得自动补发或重试。请求账本显示已发送但未完成时提示“扣费未知”；旧版本没有账本时同样保守提示未知，须由运营查供应商账单后明确点击新批次重试。

### 规则 1：禁止未验证就声称"已修复"

- **问题**：多选下拉菜单功能，连续 5+ 次声称"已修复"但实际未验证，用户每次测试都失败。用户极度愤怒。
- **规则**：在告知用户"已修复"或"已完成"之前，必须通过以下方式之一进行验证：
  1. 对于前端 UI 交互：创建独立测试页面，通过 HTTP 请求确认文件已正确提供，并明确告知用户需要测试的具体步骤
  2. 对于后端 API：通过 curl/Invoke-RestMethod 实际调用端点确认返回正确数据
  3. 对于代码逻辑：通过 Node.js 语法检查 (`node -c`) 确认无语法错误
- **绝对禁止**：仅修改代码后就声称"已修复"，不进行任何验证

### 规则 2：document.addEventListener 不得在循环/重复调用的函数内使用

- **问题**：`setupDictionarySearch` 函数在每次 `renderAttributes()` 调用时为每个属性输入添加 `document.addEventListener("click", ...)`，导致多次渲染后 document 上堆积大量 click handler，互相干扰
- **规则**：全局事件监听器（document/window 级别）必须只添加一次，使用 flag 变量控制：
  ```js
  if (!window._myGlobalReady) {
    window._myGlobalReady = true;
    document.addEventListener("click", function(e) { ... });
  }
  ```
- **替代方案**：在组件初始化时添加一次全局 handler，用全局变量追踪当前打开的组件

### 规则 3：死代码必须及时清理

- **问题**：旧的 `setupMultiSelectSearch` 函数（约 60 行）作为死代码保留在文件中，增加混乱
- **规则**：当用新函数替代旧函数时，必须删除旧函数，不得保留为死代码

### 规则 4：反复修改同一问题超过 3 次未解决时，必须停止并重新分析

- **问题**：多选下拉菜单修改 5+ 次仍未解决，每次都声称"已修复"
- **规则**：当同一问题修改超过 3 次仍未解决时：
  1. 停止修改，重新从头分析根本原因
  2. 写出根因分析文档
  3. 创建独立测试环境验证修复方案
  4. 确认测试通过后再应用到主代码

### 规则 5：多选 Ozon 字典属性必须按单个 value_id 校验和提交

- **问题**：性别等多选字段在草稿中会持久化为 `value_id="22880|22881"` 与 `value_text="男士|女士"`。把整个管道分隔字符串当作一个字典 ID 查询，会错误提示“未填写”。
- **规则**：凡是 `is_collection=true` 的字典属性，保存、预检和提交都必须按管道分隔符拆分；逐项保留 `value` 与 Ozon `dictionary_value_id`，不得将拼接后的字符串作为单个 ID 查词典。
- **验证**：新增/维护自动测试，覆盖多选性别 `22880|22881` 通过必填预检；对实际草稿调用 `/validate` 后不再出现该属性缺失。

### 规则 6：AI 自动填充只能补空，草稿加载必须保持已选字典 ID

- **问题**：草稿加载后，延迟返回的 AI 填充结果把已经保存的“类型”覆盖为只有文字、没有 `value_id` 的值；多选字段未将 `id1|id2` 恢复为组件数组，保存时会清空性别。
- **规则**：AI 自动填充只处理空字段，绝不覆盖已有 `value_id`、`value_text` 或多选数组。加载既有草稿时不启动自动填充/自动内容生成；在渲染多选组件前必须将持久化的管道分隔值恢复为 ID 与文本数组。
- **验证**：在 Chrome 中执行“刷新草稿 → 保存 → 预检”，确认性别和类型文本仍显示，数据库中的性别 ID 和类型 ID 均未改变。

### 规则 7：多变体发布必须逐 SKU 指定 Ozon 主图

- **问题**：草稿已保存每行 SKU 的独立 `image_url`，但提交 `/v3/product/import` 时三条 SKU 复用了商品图库。Ozon 规定 `items.images[0]` 为主图，导致不同变体在卖家后台显示为同一张首图。
- **规则**：每个导入 item 的 `images` 都必须以该变体的 `image_url` 开头，再追加公共详情图库并去重；只有变体图缺失时才能回退公共图库首图。不得只按商品级图库构造所有 SKU 的图片字段。
- **验证**：维护自动测试覆盖“SKU 图在 images[0]”和“缺 SKU 图时回退图库”；对真实草稿打印每个 Offer ID 的 payload 首图，必须与该行 `image_url` 一致。

### 规则 8：完整闭环需求不得拆成孤立片段后声称完成

- **问题**：用户要求“全部店铺营销活动 + 一键退出”，却曾只实现活动列表，没有同步交付退出按钮、后端写接口、安全确认、结果反馈和审计，造成需求被割裂。
- **规则**：涉及一个业务动作的需求，交付前必须同时核对入口、读取、写回、安全确认、逐项结果、失败反馈和审计；任何一项未完成时必须明确说“部分完成”，不得说“已完成”。
- **验证**：前端必须能看到并触发入口；后端必须拒绝未确认的危险请求；自动测试覆盖保护范围；只读调用验证真实数据结构。未经用户在界面确认，不得代替用户执行真实退出。

### 规则 9：自动定价必须只有一个后端公式来源

- **问题**：上品编辑器曾硬编码“成本 + 5、售价 ×3”，而后端预检使用佣金、物流、杂费和利润迭代，导致编辑时看到的价格与最终核价不是同一套规则。
- **规则**：所有自动生成价格必须调用后端持久化定价策略；前端只展示和提交参数，不得复制一套独立公式。策略保存后，编辑器导入变体、流水线内容定价和草稿预检必须读取同一条策略记录。
- **验证**：修改采购缓冲等参数后，用相同货源价和尺重调用报价接口，采购成本和最终售价必须同步变化；页面刷新后参数仍存在；新导入 SKU 使用接口返回的售价、原价和最低价。

### 规则 10：用户要求“列表可视化”时不得只在详情页实现

- **问题**：FBS 商品图片虽已同步并在订单详情抽屉显示，但订单主列表仍只有订单号和状态，不满足运营人员扫视订单时直接识别商品的需求。
- **规则**：凡用户明确要求列表可视化，列表 API 必须返回所需预览数据，主列表必须直接渲染；详情抽屉只能补充信息，不能替代列表展示。
- **验证**：真实店铺订单列表每行出现商品缩略图、首个商品名称、商品种数和总件数；图片元素 `naturalWidth > 0`，不能把灰色占位符当作成功。

### 规则 11：Ozon Offer ID 必须在生成和保存阶段限制为 50 字符

- **问题**：KC000006 的一个俄文变体 SKU 长 53 字符，直到调用 `/v3/product/import` 才收到 Ozon HTTP 400：`Request.OfferId value length must be at most 50 characters`。
- **规则**：ParentSKU 和每行 SKU 在自动生成、俄文翻译、人工编辑和保存时都必须满足 Ozon 50 字符限制。超长值采用“可读前缀 + 原始完整值的稳定 8 位哈希”缩短，禁止直接截断导致不同变体撞码。后端预检和提交必须再次防御性检查。
- **验证**：短编码保持不变；超长编码不超过 50；相同长前缀的不同编码规范化后仍唯一；真实草稿预检不得再出现 Offer ID 长度问题。

### 规则 12：从 Ozon 回拉必须恢复完整商品卡且不得静默覆盖

- **问题**：旧实现选择一个 `product_id` 后只建立一行变体，并在命中既有草稿时直接删除原属性与变体，既丢失同卡 SKU，也可能破坏人工编辑。
- **规则**：以 `/v3/product/info/list` 返回的 `model_info.model_id` 为商品卡归组依据，将同模型全部 Offer ID 分别恢复为变体行。已有草稿默认返回冲突；只有运营人员明确确认后才允许覆盖。
- **验证**：选择 KC000005 任一子 SKU 必须识别 3 个变体；默认重拉返回 409、`draft_id` 和 `variant_count=3`，确认覆盖后草稿保留 3 行独立 SKU。

### 规则 17：任何已填写的 Ozon 字典属性都必须携带菜单 value_id

- **问题**：KC000006 的可选属性“装修风格”保存为文字“休闲”，但没有保存 Ozon `dictionary_value_id`，提交后被平台判定为“属性值不正确”。只校验必填字典属性无法覆盖这种情况。
- **规则**：所有字典型属性（无论必填或选填），只要填写了内容，就必须按全局类目、类型和属性缓存逐项保存并提交 Ozon 菜单返回的 `value_id`。历史文字值只允许在全局缓存中精确唯一命中时自动修复；不能精确命中则保存、预检和提交均须阻止，禁止退化为自由文字。
- **验证**：自动测试覆盖“可选字典属性仅有文字也被预检拒绝”；真实草稿 payload 中每个非空字典值都包含 `dictionary_value_id`。

### 规则 18：新采集商品的描述与富内容必须自动串行生成

- **问题**：自动描述调用曾使用错误请求字段并静默失败，同时类目加载会提前生成富内容，造成运营人员必须手动点击，或富内容未使用最终描述。
- **规则**：打开没有草稿的新采集商品时，必须自动生成俄文描述；只有描述成功返回并写入表单后，才能用这份描述和最多五张详情图生成富内容 JSON。禁止并发抢跑和静默吞错。打开已有草稿不得重新生成或覆盖人工内容。
- **验证**：真实新采集商品页面无需点击即可出现描述和富内容；富内容文本块包含刚生成的描述；现有草稿刷新后描述和富内容保持原值。

### 规则 19：明确零库存的货源 SKU 不得生成 Ozon 变体

- **问题**：1688 采集商品可能同时包含有货和库存为 0 的 SKU；若全部生成并给 Ozon 库存 999，会产生货源无货但平台可售的履约风险。
- **规则**：进入编辑器和后端变体映射时，凡货源库存明确等于 0 的 SKU 必须排除；库存大于 0 的保留。库存缺失与库存 0 必须区分，缺失值不得擅自判定无货。若全部 SKU 均明确为 0，阻止生成上架变体并提示运营人员。
- **验证**：混合库存样本只生成有货 SKU；全部零库存样本返回阻塞错误；原始采集记录仍保留全部 SKU 作为证据。

### 规则 20：重复采集与跨店重复发布以货源强标识判断

- **规则**：1688 商品以 `(source_platform, URL 中的 Offer ID)` 作为货源强标识，不以标题翻译、图片或 SKU 文案作为自动阻止依据。同一强标识只保留一份货源快照，并汇总它在所有 Ozon 店铺的草稿与发布记录。
- **展示**：插件采集结果必须提示已采集及已发布店铺；采集箱状态必须区分“本店已发布/其他店已发布”，并可查看店铺和 ERP Offer ID。
- **提交门禁**：最终提交前再次检查；若同一 1688 Offer ID 已在其他 Ozon 店铺提交，返回 409 并阻止重复发布。标题/图片相似只能作为“疑似重复”提示，不能自动拦截。
- **验证**：跨店重复采集不新建第二份源记录；状态接口返回发布店铺；跨店提交测试确认被阻止。

### 规则 13：库存提交必须由 Ozon 商品状态驱动并回读确认

- **问题**：商品导入后固定等待数秒便尝试库存写入；若当时商品尚未生成 SKU 或未进入 `price_sent`，失败会被忽略，后来运营在 Ozon 后台修正商品也不会再次触发库存。
- **规则**：发布成功后将库存任务持久化。后台持续检查商品卡的全部 Offer ID，只有全部取得 Ozon 商品/SKU ID 且状态均为 `price_sent` 时，才按每行 SKU 的库存和尺重/售价匹配的 FBS 仓库调用 `/v2/products/stocks`。请求成功不等于闭环，必须用 `/v2/product/info/stocks-by-warehouse/fbs` 回读目标仓库库存；未生效则继续重试，不得静默终止。
- **验证**：自动测试覆盖“仅部分 SKU 出现时不写库存”和“全部 `price_sent` 后写入并回读确认”；采集箱必须显示等待 SKU、等待可售、确认中、重试或已生效状态。

### 规则 28：库存状态必须以 Ozon 回执和仓库回读为准，warning 不得伪装成导入失败

- **问题**：2026-08-27 批量任务中，Ozon 已返回 `imported`、`product_id`、`price_sent`，但同一条回执含 `erased_attribute_value` 的 `warning`。本地监控以 `errors` 数组非空判为 0/17 未导入，导致库存接口从未调用；本地“等待商品”掩盖了 Ozon 已可售但无库存的事实。
- **规则**：库存资格必须逐 SKU 同时核对 Ozon 导入回执（`imported/skipped + product_id`）、`/v3/product/info/list` 的 `price_sent` 和 `/v2/product/info/stocks-by-warehouse/fbs` 回读。仅 `ERROR` 级且未被显式允许的回执才阻断；`WARNING` 必须留档、展示，但不得阻止库存。任何“今日库存已推/未推”的结论必须来自上述 Ozon 只读核对，禁止只报本地 `stock_sync_status`。
- **验证**：自动测试覆盖含 `erased_attribute_value: warning` 的完整导入仍会调用库存写入并通过回读；生产排查报告需分别列出“已生成商品、price_sent、Ozon 实际库存匹配/无记录/不一致”的数量。

### 规则 29：Ozon 回执必须主动驱动反馈、修正和二次回测

- **问题**：只把首次提交结果写入本地，未把 Ozon 的错误、警告、价格/审核状态和仓库库存持续回读；自动可修内容被长期留在“需处理”，运营无法知道系统正在做什么。
- **规则**：提交任务必须周期读取 `product/import/info`，保存逐 Offer 原始错误/警告和状态时间；成功导入后继续读取商品 `price_sent`/审核状态及 FBS 库存回读。确定性的标题重复、主题标签单值/品牌/营销文案问题调用现有预检和 AI 修复，再经统一草稿预检和提交函数重提；额度、品牌权属/证书、图片文字审核和缺少证据不得自动猜改。自动修正重提设上限，达到上限必须显示真实 Ozon 原文并停止。
- **验证**：测试覆盖嵌套回执按 Offer ID 展开、warning 不阻断、图片审核不误自动修复、额度错误不进入修复循环；生产状态必须区分“已导入、已 `price_sent`、库存已写入、库存回读匹配/缺失/不一致、需自动修正、需人工处理”。

### 规则 14：上品模板只能覆盖固定值，不能替代类目属性或关闭自动填写

- **问题**：套用模板时曾把产品属性整体替换成模板保存值，并以 `autoFill: false` 加载类目，导致模板未保存的属性为空且不再自动匹配。
- **规则**：套用模板的顺序固定为“加载模板绑定类目的完整 Ozon 动态属性 → 覆盖模板已保存值 → 采集硬匹配/AI 仅补空 → 默认值与标签补空”。模板值优先，AI 不得覆盖模板值；模板未保存描述时保留当前商品自动生成的描述。
- **验证**：套用模板后属性总数必须等于该类目当前 API 属性数；模板字段保持 `method=template`；至少一个模板外空字段可被自动填充；已有模板值不得被自动填充覆盖。

### 规则 15：自动售价设全局底线，Ozon 最低售价保持取整规则

- **规则**：自动公式计算出的最终销售价不得低于持久化参数 `listing_price_floor_cny`，当前默认 `26.99 CNY`。托底后再计算原价和 Ozon `min_price`。
- **最低售价**：销售价有小数时向下取整；销售价是整数时减 1，且不低于 1。不得再用最低利润率/最低利润额替代该 `min_price` 规则。
- **验证**：低成本商品应得到 `price=26.99`、`old_price=53.98`（倍数 2 时）、`min_price=26`；自定义售价底线 30 时应得到 `min_price=29`。

### 规则 16：字典搜索输入框不得通过重建 DOM 过滤选项

- **问题**：多选字典搜索每次输入都重建包含搜索框的下拉 DOM，焦点和光标回到起点，造成连续字符倒序，并破坏中文输入法 composition。
- **规则**：搜索框节点必须保持稳定，只更新选项容器；监听 `compositionstart/compositionend`，组合输入结束后再搜索。中文、俄文和单个字符都允许搜索，不设前端两字符门槛。
- **验证**：连续输入“测试”后输入值必须仍为“测试”而不是“试测”；composition 结束后只触发一次查询；单字符后端请求可返回结果。

## Development Handoff Rule

### 批量定价长期规则

- 批量刊登的定价模式必须显式传递 `pricing_mode_system`；接口不得用缺失字段默认为系统定价。
- 未勾选系统定价时，必须使用批量规则中明确填写的售价、原价和最低价；勾选系统定价时才允许调用统一定价策略。
- 批量开始前必须预检“界面显示值、持久化 rules_json、草稿变体价格、提交 payload”四处一致；发现不一致立即暂停批次，禁止继续外发。
- 质量暂停必须由 worker 在每个商品之间检查并持久化，不能只改批次表面状态后继续排空队列。
- 这条规则来源于 2026-08-26 徽章批次事故：界面显示 26，但缺失模式字段触发默认系统定价，实际生成 49.42。

- 批量生成或提交任务运行期间不得直接重启后端；后台任务依赖进程内线程。若必须重启，先将任务标记为暂停并持久化游标，重启后再从数据库中的 queued/waiting_quota 项恢复，禁止把“后端健康”误报为“任务仍在运行”。

## 批量刊登一键处理与提交门禁

- 批量刊登的运营入口必须是一个完整动作：“开始处理并提交”/“继续处理并提交”。运营只确认一次；后台内部完成生成草稿、质量预检、按目标店铺分队列、读取额度和真实提交，不得把 `prepared/approved` 当作运营待办状态展示，也不得要求逐项批准或去另一个页面二次确认。
- 每次店铺提交最多 20 个，并在发送前读取官方 `/v4/product/info/limit` 的 `daily_create`。官方额度暂不可读时，只能显示并采用本地保守额度，不能冒充实时额度。
- 当日额度不足时，未提交商品必须停在用户可见的“等待额度”状态；不得重建草稿或要求逐条进入编辑器。次日无人值守续传必须另获明确授权；未授权时由运营点击“继续处理并提交”恢复。
- 验证批量闭环时只测试确认门禁和本地状态，禁止为了验收而调用真实 Ozon 写接口。
- 批量任务不得把流水线内存映射或临时 payload 视为“完整草稿”。真实提交前必须验证草稿数据库已持久化完整类目属性、属性 11254 富内容、属性 4191 简介、公共产品图，以及每个 SKU 的独立首图、价格、尺重、颜色字典 ID 和俄文颜色名称；任何一项缺失必须阻止上传。
- Pipeline 发布 payload 必须以已保存草稿为唯一数据源，不能绕过草稿继续读取旧的 `pricing_json` 或把整套图片复用于所有 SKU。每个 SKU 的 `images[0]` 必须是该行 `image_url`，其后才追加公共产品图。
- SKU 颜色优先从规格名精确匹配当前类目 Ozon 字典；无法识别时使用 `1688 Offer ID + 稳定 SKU 顺序` 做同卡不碰撞的确定性分配，并持久化 `dictionary_value_id` 与标准俄文颜色名称。重开、重试和跨日续传不得改变。
- 批量草稿或已导入商品暴露结构性质量问题时，立即把整个批次置为 `paused_quality_audit`；修复与本地预览通过前，开始/继续接口必须返回 409，禁止继续外发。问题直接在商品行显示并提供重试、归档、编辑草稿等行内操作，不另建脱离明细的问题面板。
- 批量明细必须全中文展示状态，并在每行显示当前状态时间；处理中为蓝色、等待为黄色、问题为红色、成功为绿色。状态表头必须支持筛选。

### Ozon 回传内容与额度状态规则（2026-08-27）

- Ozon 已明确返回 `ERROR_ATTRIBUTE_IS_NOT_COLLECTION`：主题标签属性 `23171` 只允许一个 `values` 项；草稿中仍用空格分隔的 20–30 个俄文标签保存，但导入 payload 必须把整行标签作为该唯一值，禁止逐标签拆成多个 attribute values。
- 标题、描述、主题标签、简介属性 `4191`、富内容 JSON 属性 `11254` 都不得出现品牌、制造商、厂家、店铺、供应商或平台名称（包括 `Ozon`）。生成提示词须先限制，货源的结构化品牌和英文专有名词不得进入 LLM 输入，提交前预检必须再次清理四个文本面；富内容欢迎语不得带店铺名。真实 Ozon 品牌/证书回传应记录为可修正内容问题，不得忽略。
- `periodic_limit_exceeded` / Ozon 日创建额度耗尽不是“导入失败”也不是“库存失败”。必须标记为 `waiting_quota`，保留原始回传，且在莫斯科时间 03:00 重置前禁止再次发起真实导入；重置后仅在运营明确点击继续时才允许恢复。批量有可继续队列但所有目标店铺均有有效额度等待证据时，批次整体显示“等待额度”，不能误显示“可继续处理”。
- 库存状态必须区分：`completed`（已写入并回读确认）、`waiting_product`/`waiting_price`（Ozon 尚未生成完整可售 SKU）、`waiting_quota`（未创建商品，尚无库存可写）和 `import_failed`（真实内容/数据错误）。不得把后两类笼统显示为“库存未同步”。

- When the user says the computer is left running for ERP development, treat it as permission to continue the agreed local implementation plan, not permission to start sourcing, collection, AI listing generation, or Ozon publishing jobs.
- Complete the agreed development and QA scope without step-by-step approval prompts. Keep real external mutations behind explicit confirmation gates.

## 1688 铺货前期人工流程

## 1688 图片分层采集规则

- 1688 商品图片必须按结构化主图、SKU 专属图、`description.fields.detailUrl` 详情图三路采集；详情图需读取 `itemcdn.tmall.com/1688offer/*` 返回的 `offer_details.content`，不得把详情 URL 当作已经采集了详情图。
- 当结构化图库存在时，禁止扫描整页脚本或全页面 `alicdn` URL 作为产品图库来源，否则会混入导航图标、徽章、广告和平台装饰素材。DOM 扫描只可作为结构化数据完全缺失时的降级证据。
- 完整重采必须携带 `mediaComplete=true`。后端仅在该标志为真时用“主图 + SKU 图 + 详情图”替换旧媒体，从而清除历史误采；不完整抓取仍采用合并模式，避免擦除已有证据。
- 验证至少覆盖：详情接口图片数量、页面图标不入库、SKU 图独立保留，以及完整重采后旧错误媒体被清理。

## 通用批量刊登规则

- 批量刊登必须是一个通用任务引擎，不得为“徽章、模具、饰品”等每种商品新增专用按钮、专用表或复制一套流程。商品类型只是任务的样板、来源范围和规则配置。
- 通用任务固定包含：来源范围、人工确认样板、目标店铺与分配策略、固定字段、商品级重建字段、异常门禁、审批、额度感知发布和断点续跑。
- 新品类只创建新的批量刊登任务；若现有字段无法表达差异，应扩展通用规则模型，而不是增加品类专用入口。

- 当前阶段采用“夜间后台预处理，白天人工上架”，不启用无人值守的自动铺货、自动草稿推进、自动审批或自动 Ozon 提交。
- 后台允许执行的范围仅为严选关键词检索、官方详情读取、风险/尺重/库存证据核验和本地候选记录。它不得把候选自动写成可发布草稿，也不得触发 AI 生图、AI 内容生成或任何 Ozon 写接口。
- 运营次日必须在候选池逐项检查图片、SKU、尺重证据、价格和风险结果；只有人工明确选择“创建上架草稿”后，才能进入既有手动编辑器、预检、审批与提交流程。
- 多店铺铺货时，候选记录与人工创建的草稿必须明确绑定目标店铺；系统可展示建议店铺和重复发布风险，但不得自行选择店铺或跨店发布。
- 验证：夜间运行后候选只停留在待人工处理状态；没有人工点击创建草稿/审批/确认提交时，数据库中不得出现新的上架草稿、审批批次、Ozon 任务或发布审计。

## 可信决策记忆规则

- Ozon 类目必须始终以完整的 `description_category_id + type_id` 成对保存。类目树的类型叶节点若不携带 Category ID，后端必须从最近父类目向下传递；前端遇到历史空 Category ID 时只允许用唯一 Type 命中补全，无法唯一确定则明确阻止记忆和提交。记忆按钮必须在原区域显示保存中、成功记录或具体失败原因，禁止只弹瞬时提示造成“点击没响应”。
- 同关键词批量上架采用“人工确认样板 + 商品级差异重建”，不得整份复制商品卡。样板只固定完整类目对和运营明确勾选的可学习属性；每个候选的图片、视频、SKU、采购价、尺重、定价、库存、标题和描述必须从各自货源重新处理。批量动作只生成本地草稿，异常进入人工队列，未经审批不得调用 Ozon 写接口。
- 同类商品批量任务不再依赖历史样板草稿 ID。运营必须直接选择完整的 Ozon `description_category_id + type_id`，界面加载并展示该类目的全部属性；能由统一规则补空的值先回填，字典属性仍必须保留真实 `value_id`，再由运营逐项纠错确认。
- 批量图片清理与翻译必须是两个独立开关。本地 OCR 只在图片同时命中中文文字和尺重/尺寸证据时将其从本次草稿素材排除，英文尺重图保留，原始采集图片和 OCR 证据不得删除；图片翻译只处理清理后保留的图片，失败进入人工队列，不得静默丢图。
- 批量图片编排、图片 OCR 和 Ozon 图片字段只能接收 `media_type=image`；视频 URL 必须走独立视频流程，禁止因共享 `source_media` 表而进入图库或触发图片识别失败。
- 批量任务详情的“错误”列只表达类目、属性、内容、图片等门禁结果，不得把它误解为 Offer/SKU/条形码问题并顺手改码。批量处理沿用既有稳定 SKU/Offer 生成逻辑；`barcode` 保持空值并由 Ozon 自动生成，除非运营明确另行指定。
- 批量属性页中的型号名称、主题标签、名称/描述、富内容和 Ozon 视频属性只作委托状态展示，必须复用原编辑器逐商品生成逻辑，不得在批量编排层重新实现。原视频默认不使用；明确勾选时仍须先转换为 `media.woxq.cn/proxy` 地址，禁止把1688 CDN原视频链接直接提交给Ozon。
- 批量审核等级只能由硬证据生成：绿色必须同时具备完整类目对、至少一个 SKU、每个 SKU 的价格和尺重、可用图片、无质量问题及本地草稿；黄色只处理可局部修复的问题；类目、SKU结构、字典 ID、采集快照或跨店冲突属于红色完整审核。AI置信度不得单独把商品判绿。

- 自动类目/属性推荐只能读取可信记忆，绝不能把自己的推荐或默认首选项回写成训练样本。
- 只有运营人员明确点击“确认并记住/修改并记住”，或 Ozon 返回明确成功结果，才能创建或增强记忆；“仅本次使用”只记审计证据，不进入推荐库。
- 每条记忆必须保存来源、领域、证据、可信度和状态，并支持停用、恢复、撤销；Ozon 拒绝必须降权并生成负反馈。
- 推荐前必须先通过商品领域硬门禁，再做相似度召回；跨领域记录不得因历史次数高而越过门禁。
- 验证记忆功能必须使用隔离测试数据库，禁止用测试请求污染真实商品、店铺、类目历史或发布状态。
- 属性行使用“记住此属性”作为后台学习开关：选择必须随草稿持久化，只有 Ozon 导入成功后才生成可信属性记忆；下次相同店铺、类目和类型只补空，不覆盖人工值。模板仅作旧流程兼容。
- 所有商品提交入口必须统一闭环写入草稿任务号、`listing_submitted` 审计和流水线草稿/任务关联；Ozon 确认导入后流水线必须为 `published/imported`，并以任务号幂等完成类目与已选属性学习。
- Ozon payload 中禁止出现 `127.0.0.1` 或 `localhost` 图片。提交前必须上传到已配置的公共 OSS、验证成功、替换图库/变体/富内容中的全部引用；上传或本地文件缺失时阻止提交，不得吞错继续。
- OSS 本地图片转换必须同时支持 `frontend/translated/` 与 `frontend/generated/`；通用上传接口遇到路径不支持、文件缺失或 OSS 失败时必须报错，禁止静默返回本地 URL。
- OSS `upload_bytes` 的 `content_type` 不得传 `None`；必须按 `.jpg/.jpeg/.png/.webp` 映射明确 MIME 类型，避免 SDK 签名阶段出现 `NoneType.encode`。

## AI 商品套图规则

- AI 视觉分析和生图结果始终先进入可审计候选任务；生成成功不等于采用成功，禁止自动覆盖草稿或自动发布。
- 套图候选的选择和“使用所选套图”不得以草稿已保存为前置条件。未保存时，确认使用应立即更新当前编辑器的图库与 SKU 首图；保存草稿时才持久化，已保存草稿则同时写入应用审计。
- 只有运营勾选图片并明确确认后，才能替换 SKU 首图和详情图库；应用接口必须校验图片属于当前任务，并记录替换前后值和操作者。
- 多 SKU 商品必须展示视觉模型识别的 SKU 策略与风险。套图首图会被用于全部 SKU 时，确认文案必须明确说明该影响，不得静默替换。
- 生成任务必须后台执行且同店铺同货源运行中幂等，避免页面长请求和重复付费；任何测试默认不得调用真实付费生图接口。
- 沧猿 Image 2 默认使用供应商标价模型 `gpt-image-2`（0.015 元/张）；禁止回退到 `gpt-image-2-1k`，除非用户明确指定。Image 2 失败必须保留 HTTP 状态和供应商响应正文摘要，不能只记录“400”。
- Terra 图片分析请求使用 OpenAI 兼容的 `image_url: {url}` 结构，不附加供应商未确认支持的 `detail` 字段；分析失败必须显示供应商 400 响应摘要，便于定位坏图或请求格式问题。
- Image 2 `images/edits` 单张请求最多发送 2 张参考图；分析可读取更多图片，但生图 multipart 超过 2 张在沧猿网关上容易触发 502。
- 套图任务部分失败时不得丢弃已生成 slot；只要任务中有可用图片，运营仍可逐张点击选择、全选或应用，第一张最终选中图作为当前 SKU 首图，不得强制等待规划中的 hero slot。
- AI 选中的图片应用后加入草稿的产品图片库，保留原有图片；普通“保存草稿”才将当前图片库、排序和变体首图持久化。变体产品图弹窗必须支持拖拽排序和删除，颜色样本跟随该变体产品图的第一张图。
- 验证：隔离测试覆盖 `failed` 任务的部分应用、非 hero 首图回退、已有图库合并；前端语法检查覆盖失败任务可用、全选/点击选择和图片库交互。

### Ozon 主图生成与俄文文案规则

- Image 2 的职责是保持产品身份并生成带俄文详情介绍的真实商业套图。套图不是纯无文字图：第一张偏主图展示，后续图片可介绍核心参数、卖点、使用场景和适用人群；所有俄文内容必须来自 ERP 已确认的商品事实，不能让模型自由猜测。
- 生图前必须从 Terra 视觉分析和采集快照建立产品事实清单：产品外观、颜色、材质、结构、尺寸比例、Logo、原图文字、SKU 差异、包装包含物和禁止新增物。未被证据支持的功能、参数、配件、认证和使用效果不得写入 Prompt。
- 参考图选择遵循“多图分析、单图发送”：Terra 可以读取多张采集图，但每个 Image 2 槽位只发送 1 张最适合保持产品边界和身份的参考图；尺寸图、宣传图、文字密集图和无法确认产品主体的图片不得作为付费参考图。
- 生成约束固定为：3:4 竖版、产品占画面约 65%-80%、单一产品主体、完整展示、不裁切关键结构、自然柔和光线、真实阴影、轻微虚化背景。场景必须与产品类目和真实使用方式一致，不添加不存在的配件、功能、图案、装饰或第二个产品主体。
- 俄文标题、1-3 个核心参数、3 个卖点和适用场景必须先由 ERP 根据已确认事实生成并经过人工可见审核；禁止让 Image 2 自由猜测或扩写。文案不得包含中文、英文乱码、价格、二维码、水印、促销贴纸、平台 Logo 或未证实承诺。
- 俄文信息图采用已确认文案和事实约束生成：主标题和参数放在不遮挡产品的安全区域，卖点图标不超过 3 个，字体清晰高对比；第一张主图保持产品突出，后续详情图承担介绍功能。文字不能遮挡产品，也不能替换或改写产品原有 Logo 和原图文字。
- 采用图片前必须人工确认产品身份、颜色/材质/结构、Logo 和原文字未被改变，俄文拼写与参数准确，SKU 主图没有错配。任何一项不确定都只能标记为需人工处理，不能进入提交 payload。
- 生成结果仍必须先进入可审计候选任务；纯图、文字增强图、参考图来源、事实清单、Prompt 版本和人工确认结果分别留档。只有运营明确选择后才能加入产品图库，提交前继续执行公共 OSS、HTTP(S) 地址和逐 SKU 主图门禁。
- 验证必须覆盖：带俄文详情套图生成、事实清单约束、单参考图 multipart、俄文文案不遮挡产品、错字/虚构参数拦截、第一张主图与后续详情图分工、人工确认前不能写入草稿或 Ozon payload。

## 图片编辑器规则

## 严选夜间探索规则

- 夜间计划允许关键词为空；空关键词表示系统轮换探索，不代表调用不存在的“全平台随机枚举”接口。
- 系统轮换词池只提供官方严选搜索入口，所有结果仍必须经过风险、库存和尺重门禁。
- 夜间采集结果先进入 `automation_candidates` 候选池，状态为 `ready_for_review`，不绑定店铺、不创建上架草稿。
- 只有运营人员在候选池逐条选择店铺并点击人工编辑后，才写入 `source_products` 并进入商品编辑器。
- 候选商品默认页面必须只显示可操作工作队列（待人工上架、人工编辑中、需人工处理及发布异常）；`detail_pending`、风险淘汰、尺重淘汰等内部证据只能在明确筛选后查看，不能混入默认列表。
- 自动任务状态语义必须统一：新建和复制任务默认 `active`，创建响应中的下一次执行时间必须是未来时间；只有运营人员明确点击暂停后才变为 `paused`。恢复任务时重新计算下一次未来执行时间，调度器处理过期计划时只推进到下一个未来时刻，不连续补跑历史周期。

### 采集 SKU 与尺重继承

- 多个可售采集 SKU 必须生成对应的 Ozon 变体行；即使当前类目尚未加载或没有 `is_aspect` 变体属性，也不得退化为单个空白占位行。
- 每个 Ozon 变体优先继承其采集 SKU 的 `weightG`、`lengthMm`、`widthMm`、`heightMm`；商品级 `packageInfo` 仅用于该 SKU 没有尺重证据时的兜底。
- 只有唯一全空的历史占位行可以从关联货源恢复；已有人工变体不得自动覆盖。验证样本必须覆盖多 SKU、逐行尺重以及无 `is_aspect` 类目。

- 变体颜色选择不得只依赖采集 SKU 名称；颜色维度应优先读取 Ozon 字典，字典缺失时回退到采集变体值，并允许人工选择/多选。
- 新建变体时颜色字段默认必须为空；采集 SKU 文本只能作为参考，不能自动写入颜色。随机分配和点击颜色输入框选择是两个显式操作。
- 水印设置属于草稿级配置：PNG、九宫格位置、大小和透明度随草稿保存；提交到 Ozon 前由后端统一合成并上传 OSS，配置哈希命中时不得重复叠加水印。

### 快速审核图片翻译

- 快速审核中的图片翻译必须对未编辑商品也可用；首次翻译时自动建立最小图片草稿，再把象寄返回的 URL 写回草稿。禁止只显示可点击按钮却因 `draft_id` 为空静默拦截。
- 翻译成功或部分失败必须在快速审核图片区保留明确结果提示；验证必须同时覆盖无草稿自动创建、象寄返回 URL、草稿持久化和页面回显。

### SKU 图片翻译与公共图库边界

- SKU 图片翻译只更新变体图片；若原图同时存在于公共图库，翻译结果应原位替换对应图库项，并同步引用同一原图的 SKU。若原图不在公共图库，只更新对应变体，不得把翻译结果自动追加到公共图库。
- 验证必须覆盖“图库 8 张、同源 SKU 图翻译后仍为 8 张”和“非图库 SKU 图翻译不新增公共图库”，避免公共图库与变体专属图片重复计数。
- 编辑器运行时，`state.images` 的公共产品图与每个变体的 `variant.image_url` 必须保持两个集合；第⑥步只渲染公共产品图，变体设置的 SKU 图只作为该 SKU 的独立首图。变体产品图选择器只能列公共产品图，不能列其他 SKU 图；提交时 `images` 发送公共图库，变体 payload 单独发送对应 SKU 首图。
- 快速审核的主操作必须形成闭环：保存尺重/自动定价后调用正式 Ozon 提交接口；只有提交成功才进入下一条，提交失败必须保留当前窗口并显示后端原因。

### AI 主题标签数量门禁

- Ozon 主题标签是数量上限约束，不是精确 30 个。AI 生成目标仍为 30 个，但共享生成器只接受 20–30 个有效标签；29 个不得阻断商品，超过 30 个去重后截断，少于 20 个才判为失败。
- 编辑器、批量刊登、流水线复用同一 `normalize_hashtags` 规则，禁止各流程再写死“必须正好30”。

### 批量 Ozon 回执重试上限

- 批量商品达到 3 次或以上真实提交尝试后，自动终止后续重提并归档为“已放弃”，写入审计原因。
- 只提交过 1～2 次的商品，才允许进入自动修正或完整草稿重提链路；额度、图片违规、品牌资质、尺重和 SKU 图片质量问题不得硬提交。
- 修正重提成功后清空草稿的当前错误快照，历史错误只保留在 Ozon 回执审计中，避免服务重启时把旧错误重新投影到已导入商品。
- 内部代码异常未取得 Ozon `task_id` 时不得计为真实提交次数；修复后应恢复此前计数再重试。

### Ozon 部分 SKU 库存闭环

- 一个导入任务可能同时包含已导入、额度拒绝、内容拒绝和合并失败的 SKU。库存监控必须按 Ozon 回执逐 SKU 取交集：已导入且 `price_sent` 的 SKU 立即写入并回读确认；未生成或未进入 `price_sent` 的 SKU 单独保留待后续任务，不能阻塞同一草稿中已经可售的 SKU。
- 草稿状态使用“部分 SKU 已同步”表达这种中间事实；只有每个应提交 SKU 都完成 Ozon 库存回读匹配，才允许标为“库存已生效”。
- 验证必须覆盖混合额度任务和单个变体审核失败的商品卡，确认成功 SKU 有库存写入、失败 SKU 不被伪造为已生效。

### 1688 商品智能标题及卖点接口

- `com.alibaba.image:image.product.intelligent.generate-1` 使用独立的 AppKey、AppSecret 和 AccessToken 配置文件，不得复用严选或云牛顿授权；敏感值必须加密保存且接口状态不得回显 Token。
- 编辑器调用时只使用当前商品主图和已确认的 1688/Ozon 类目 ID；返回的 `fid/vid` 是 1688 CPV 证据，不能伪装成 Ozon 字典 `value_id`。建议标题只能由运营明确点击后替换现有标题，不能静默覆盖既有文案。
- 验证必须覆盖独立配置、官方请求路径/签名、Token 不出现在响应或日志，以及无图/无类目时阻止外部调用。
- `Request need user authorized` 不是“字段已配置”的证明；必须提示当前 Token 未获得该 AppKey 的 author 授权，并提供该应用专属授权链接与 code 换 Token 流程。更换 AppKey 时清空旧 Token，禁止沿用跨应用令牌。
- 授权 state 并发修正：商品智能标题授权最多保留 5 个未过期 state（15 分钟），换 Token 成功后只消费匹配的 state；兼容旧单值字段及 query/fragment 回调参数。重复打开授权页或验证请求不得覆盖正在使用的旧标签页；更换 AppKey 时同时清空旧 state 和旧 Token。验证覆盖双 state、fragment 回调、换 AppKey 清理，相关测试共 13 项通过。

### SQLite 异常恢复与全店库存对账

- 发生 `database is locked` 或后端异常退出后，先确认无旧 API/worker 进程、执行 SQLite `quick_check` 并以安全模式恢复 API；禁止删除 `-journal` 文件或直接恢复批量任务来“解决”锁问题。
- 后端默认不得在重启时自动恢复库存写入、自动重提或批量外发；只有显式 `OZON_ENABLE_BACKGROUND_WRITES=1` 才可启动这些外部写 worker。安全模式必须仍可提供 Ozon 只读回执和库存对账。
- 全店库存结论必须从 Ozon 全量商品、`price_sent` 和 FBS 仓库回读得出，不能只查询本地草稿。缺草稿的历史 Offer 也必须出现；没有可靠本地货源库存和匹配仓库时只能标“待补证据”，不得猜测并写库存。
- 崩溃遗留 `processing` 批量行若已有 Ozon `task_id`，必须保留任务号并等回执/额度；绝不自动改回 `queued` 重发。只有没有 Ozon task 的本地中断行可以重新排队。
- Ozon `/v2/products/stocks` 返回 `PRODUCT_HAS_NOT_BEEN_TAGGED_YET` 时，库存参数不应被改写或立即重发；将关联草稿标为 `waiting_tag`、保留原始错误并至少延迟 15 分钟后再复查。已成功 SKU 的回读结果必须保留。

### 实时额度继续与批量暂停保护

- 运营点击“继续处理并提交”时，历史 `waiting_quota`/`periodic_limit_exceeded` 只能作为审计证据；后端必须重新读取每个目标店铺的 `/v4/product/info/limit.daily_create`。仅把实时有余额店铺的 `waiting_quota` 行重新入队，其他店铺保持等待，禁止用旧本地额度状态把整批永久卡住。
- 批量暂停是即时安全命令。worker 在每个商品之间及收尾汇总时都必须保留 `paused_quality_audit`/`paused`，不能因为尚有 queued 行就把批次改回 `ready_to_continue` 或 `running`。
- 对带真实 Ozon 写副作用的前端按钮做自动化回归时，不得直接点击生产页面；应拦截请求或使用无写入测试环境。若测试意外触发，立即暂停队列、读取 Ozon 回执并如实记录实际影响。
- 批量恢复的数据库时间按 UTC 朴素时间保存；恢复超时和最新活动判断必须使用 UTC，禁止拿北京时间直接比较导致刚处理的行被误判为 30 分钟前的 stale processing。
 - 批量继续不应要求运营反复启动固定数量窗口；执行请求默认使用足够大的连续队列上限，worker 按 `assigned_shop_id` 轮转并在某店额度归零时仅将该店剩余行置为 `waiting_quota`，其他店铺继续处理。

### Ozon 图片直链与富内容提交门禁

- Ozon 的图片下载由 Ozon 服务端执行；浏览器能打开的 1688 CDN 链接不等于 Ozon 可下载。提交前对 `cbu*.alicdn.com` 等热链地址下载并镜像到已验证的公共 OSS，失败必须在本地预检拦截并记录真实原因。
- 图片预检只接受带主机的 HTTP(S) 地址，`None`、`null`、`undefined`、本地文件和空值不得进入任何 `items[].images` 或富内容图片块；SKU 主图和公共详情图仍保持分离。
- 富内容属性 `11254` 必须在提交 payload 中恰好出现一次且 value 为可解析 JSON。历史空占位不能阻止生成内容，提交审计必须记录公共图数量、SKU 数量、镜像数量和富内容是否存在。

### 2026-08-28 AI 字典选择与提交后轮询

- AI 填写 Ozon 属性时，字典型属性必须把 AI 候选与当前类目的全局缓存选项按 `value_id` 对齐后再写入；仅返回文字或无法在菜单中唯一命中的候选保持为空并标记“待人工选择”，不能把文字直接提交。多选属性只保存合法 ID 数组。
- 提交后的 Ozon 回执不是一次性结果：后台必须持续轮询导入回执，再核对 `price_sent` 和目标 FBS 仓库库存回读；warning 只留档不阻断，ERROR 按 SKU 隔离。轮询间隔采用约 1 分钟、2 分钟、5 分钟退避，库存已回读匹配的 SKU 不重复写入。
- 验证必须使用本项目 `PYTHONPATH` 运行自动填充、库存监控和回执调度测试，并确认 API 重启后 `/health=200`、轮询开关已加载、数据库无新增锁错误。

### 服务器部署与 GitHub 发布规则（2026-08-30）

- 服务器 `/srv/ozon-erp` 只允许通过 GitHub 版本发布；禁止直接在服务器业务目录手工修改代码、前端资源或配置模板。
- 每次迭代必须先在本地完成修改和相关测试，再创建 Git 提交并推送远程仓库；服务器只拉取明确的提交或发布标签。
- 部署顺序固定为：记录当前线上提交、备份配置/数据库、拉取目标提交、执行数据库迁移、重启 Ozon 服务、验证域名首页/API/后台任务，并记录部署提交与结果。
- `.env`、SSH 私钥、Ozon/1688 凭证和其他密钥永不提交 GitHub；服务器密钥只保留在 `/srv/ozon-erp/.env` 或密钥服务中。
- 部署失败时保留日志并停止继续发布，不得用未提交的临时修复覆盖线上版本；回滚只能回到已验证的 Git 提交。

### Ozon 禁止内容与图片安全门禁（2026-09-02）

- Ozon 对 LGBT/非传统性关系、跨性别、彩虹旗或相关口号的禁止性回执是**硬阻断**：标题、描述、主题标签、富内容、属性、OCR 图片及 AI 生图分析任一面命中时，禁止自动换词重提；必须人工移除素材。普通彩虹色或彩虹渐变不是单独的硬拦词，只有与旗帜、身份、权利或口号构成明确宣传时才按此规则拦截。若同卡共享图片受影响，整卡变体都视为风险。
- 已创建商品的处理顺序固定为：官方 `/v1/product/archive` → `/v3/product/info/list` 回读 `is_archived=true` → 本地草稿标为 `archived` 并停掉自动重提/库存回写。Ozon 回读“未找到”只能记为不存在，绝不伪报“已归档”。
- 图片 OCR 应尝试当前 Windows 已可用的中文、英语和俄语语言包；OCR 无法运行/无法辨识时，必须保留审计并进入人工审核，不能把失败当作合规。AI 生图提示同时禁止生成或强化相关旗帜、符号和口号。

### Ozon 发布合规最高指示（2026-09-02）

- `docs/OZON_COMPLIANCE_CONSTITUTION.zh-CN.md` 是本项目所有 Ozon 商品卡、属性、标题、描述、主题标签、富内容、图片、视频、SKU、条码、仓库、价格、提交和回执处理的最高规则来源。任何新功能、AI 提示词、自动修复或批量逻辑都不得绕过它。
- 所有手工提交与自动提交必须经过同一门禁，结果只可为：硬阻断、确定性自动修复后复检、人工复核或提交后回读；禁止“为了通过”删除事实、猜测类目/属性/授权或把检测失败当作合规。
- 媒体、文本和变体必须有同一商品事实证据。来源已确认平台内容风险时，应跨店铺/跨草稿持久化封禁；已创建卡按 Ozon 实际可读状态归档并回读，运营已手工处理的事实必须如实记录。
