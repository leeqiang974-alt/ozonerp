# ERP 变更记录（本轮）

更新时间：2026-05-27

## 1. 上架文案与属性逻辑

### 1.1 标题属性不再写入分类属性
- 已移除“标题类属性”自动回填与提交。
- 标题仅走商品主标题 `name` 字段，不再写入动态分类属性。

### 1.2 标题改为通用规则并支持更长
- 标题生成改为通用描述，不带容量/尺寸/颜色/机型等 SKU 规格。
- 标题长度上限由默认 200 扩到 220（提交前仍会清洗）。

### 1.3 俄文兜底（提交前强制）
- 新增提交前俄文兜底：
  - `ensureRussianTitle`
  - `ensureRussianTags`
  - `ensureRussianDescription`
  - `ensureRussianRichContent`
- 若表单中存在英文或非俄文内容，会自动回退到俄文模板生成后再提交。

---

## 2. JSON 富内容（rich content）

### 2.1 结构规范化
- 富内容清洗升级为标准结构输出：`{ content: [...] }`。
- 仅保留合法 widget：
  - `raTextBlock`
  - `raGallery`
- 非法结构会自动归一，避免 Ozon 因格式报错。

### 2.2 暂停“自动写入富内容属性”
- 为规避“JSON 内容与模板不匹配”问题，默认不再把 rich-content 自动塞进动态属性字段。
- 富内容改为手动可控（页面字段保留，可人工确认）。

---

## 3. 图片处理链路

### 3.1 全量图片走 OSS
- 上架前统一通过 `/api/images/prepare-ozon` 处理并映射 OSS 链接。
- 避免直接提交阿里源图链接（Ozon 下载不稳定/不接受风险）。

### 3.2 OCR/过滤/翻译策略
- 启用 OCR 检测，过滤工厂介绍/证书类无关图（`factory_intro`）。
- 中文图翻译改为：
  - 若象寄配置缺失：中断并报明确错误。
  - 若翻译失败但配置存在：跳过该图，不中断整单。

### 3.3 SKU 图默认组图规则
- SKU 默认图片组改为：`SKU 主图 + 全局图库中除主图外的详情图`。
- 防止主图重复追加。

### 3.4 SKU 图片编辑器增强
- 从“手工粘贴 URL”升级为“按图片库序号选择”。
- 第一张自动同步为颜色样本图。

---

## 4. 1688 常驻采集插件（content.js）

### 4.1 面板交互
- 新增最小化按钮：可缩成圆形 icon。
- 新增拖拽：面板位置不再写死右下角，可自由拖动。

### 4.2 SKU 勾选采集
- 新增 SKU 列表，默认全选。
- 新增“全选/不全选”按钮。
- 未勾选 SKU 不进入采集 payload。

---

## 5. 编码与乱码保护

### 5.1 上架接口乱码拦截
- `POST /api/ozon/product-import` 增加标题乱码保护（疑似 `????`/`��` 拦截）。
- 防止脚本或终端编码问题把乱码标题送到 Ozon。

### 5.2 拦截规则修正
- 规则调整为“只拦明显乱码且无西里尔字符”的情况，降低误拦截概率。

> 说明：命令行（特别是 PowerShell）在某些输入方式下会导致俄文变 `?`，网页内提交（UTF-8）是推荐链路。

---

## 6. 品牌、类型、容量属性

### 6.1 品牌默认值
- 品牌属性优先填“无品牌”字典项。
- 若未命中无品牌，回退到字典第一项并写入 `dictionary_value_id`，避免纯文本报错。

### 6.2 类型默认值
- 类型属性优先按当前 `type_id` 匹配对应字典值并写 `dictionary_value_id`。

### 6.3 容量数值自动填充
- 新增容量识别逻辑：从标题/属性/SKU 规格提取 `ml/L/升`。
- 对“体积/容量/毫升/升”类属性，自动填纯数字值（如 `1500`）。

---

## 7. 象寄图片翻译配置

已确认并测试可用：
- `userKey`: `1435694106`
- `imgTransKey`: `4510841405`（默认阿里标识）
- `sourceLanguage`: `CHS`
- `targetLanguage`: `RUS`
- 返回 `Code=200`，可拿到 `translatedUrl`。

配置文件：
- `data/xiangji-config.json`

---

## 8. 实测结果（关键）

- 多次自动上架回测均返回：`status=imported`。
- 俄文标题修正后可成功覆盖同一 `offer_id`。
- 图片翻译接口可用，OSS 链路可用。

---

## 9. 主要变更文件

- `public/app.js`
- `src/server.js`
- `browser-extension/1688-collector/content.js`
- `data/xiangji-config.json`

