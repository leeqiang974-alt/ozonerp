# 阶段交接：MVP 领域骨架

## 2026-09-01：Alembic 完整 schema 修复与暂停写入保护

- 背景：历史生产迭代曾依赖 `Base.metadata.create_all()`，而 Alembic 初始链只覆盖 MVP 表；新 PostgreSQL 或部分历史库升级时，后续迁移会引用未在迁移史中创建的表/列，无法可靠部署。
- 修正：新增 `e5f6a7b8c9d0_repair_complete_operational_schema.py`。该迁移按 ORM metadata 创建缺失表、补缺失列和索引，并只对旧 `skus.min_price_cny` 做保守的数值→文本兼容转换；已存在的结构和业务行均保留。此前 `9a72…/a41…/c8d…/cf2…/d4…` 迁移已加幂等保护。
- 启动规则：`main.py` 仅在 SQLite 使用 `create_all()`；PostgreSQL 只能由 Alembic 迁移。`deploy/start_backend.sh` 在 Uvicorn 前执行 `alembic upgrade head`，Docker compose 与远程 bootstrap 使用同一入口。进程环境变量优先于 `.env`，避免服务器注入的 `DATABASE_URL` 被本机模板覆盖。
- 安全：自动库存写入必须同时设置 `OZON_ENABLE_BACKGROUND_STOCK_MONITOR=1` 和 `OZON_ENABLE_BACKGROUND_WRITES=1`；自动回执修正/重提同样受写入总开关控制。默认安全启动不外发。
- 暂停保护：OCR 处理只可修正本地图片证据，不能把已暂停批次恢复为 `running` 或隐式启动 worker；单条/批量重试和普通 start 在暂停时返回 409。worker 在领任务及 Ozon 提交前都检查持久化暂停状态，暂停接口与最终提交共用批次锁，避免暂停竞态多发一条。
- 验证：全新 SQLite、旧 `d4a8…` 数据库、历史 `create_all` 完整数据库均升到 `e5f6…`；重复 `upgrade head` 和 `alembic check` 通过。`pytest` 全套为 `217 passed, 3 subtests passed`；暂停/回执相关定向测试为 `6 passed`；`compileall`、两个前端 `node --check` 通过。尚未连接真实 PostgreSQL、未启动批量发布或库存补录。
- 笔记本密钥目录：新增 `backend/app/secret_paths.py`。若没有显式 `ERP_API_DIR`、`DEEPSEEK_API_KEY_FILE`、`CANGYUAN_API_KEY_FILE` 或 `ALIYUN_OSS_CREDENTIAL_FILE`，Windows 会优先沿用 `D:\Desktop\api`，不存在时自动读取当前用户 `Desktop\api`（本机为 `C:\Users\Administrator\Desktop\api`）。只读取文件路径，绝不复制、打印或提交密钥内容。

## 2026-09-01：批量刊登增加可持久化暂停/继续

- 批量刊登列表的运行任务现在显示“暂停任务”；暂停通过 `POST /api/v1/automation/bulk-listing-batches/{batch_id}/pause` 持久化为 `paused`，worker 在商品之间和收尾阶段都保留该状态，不会继续生成或提交新商品。
- “已暂停”批次复用现有“继续处理并提交”入口，从数据库中的 `queued`/`waiting_quota` 等断点恢复；已提交商品的 Ozon 回执与库存监控不受影响。当前运行中的单条外部请求允许自然收尾后停止领取下一条。
- 前端缓存版本更新为 `app.js?v=127`、`styles.css?v=41`；后台安全重启后 `/health=200`、首页 HTTP 200，`node --check frontend/app.js` 通过，暂停接口回归测试通过。
- 2026-09-01 实际批次 #1 已通过暂停接口停止，当前无 `processing` 行；因旧服务在重启前将手动暂停写成 `paused_quality_audit`，页面仍显示“需处理”，这属于更严格的质量暂停，不会外发，待后续明确修正或继续。
- 暂停态进度提示已区分：普通暂停显示“等待点击继续”，质量暂停显示“先处理质量问题后再继续”，避免进度条下方沿用旧的“等待后台领取下一条”造成误判。

## 2026-08-28：xymallD / XY000002 类目属性缓存误报修复

- 根因：草稿 `460`（店铺 `xymallD`，shop_id `5`，Offer ID `XY000002`）绑定类目 `17028650 / 122770107`，全局 Ozon 属性快照存在 37 条，但店铺级属性缓存为空；旧预检只查店铺缓存，错误返回“当前类目的 Ozon 属性模板尚未缓存”。
- 修复：`validate_listing_draft` 优先使用全局属性快照，只有全局快照不存在时才回退旧店铺缓存；提交和字典规范化继续使用同一全局来源。新增“新店无店铺缓存但有全局属性快照”回归测试。
- 立即补救：在不重启正在运行的批量后端、不调用 Ozon 写接口的前提下，将该类目 37 条全局属性定向补入 shop_id `5` 的店铺缓存，当前预检接口返回 `ready_for_approval`、`issues=[]`。

## 2026-08-28：类目与属性缓存统一全局化

- 上述给 `xymallD`（shop_id `5`）补店铺缓存只是运行中批次的临时救急，不是最终架构。已将类目匹配、内容生成、决策记忆、批量属性预检、草稿校验、提交颜色字典和编辑器属性读取统一改为 `ozon_global_category_cache`、`ozon_global_attribute_cache`、`ozon_global_dict_values`。
- 类目同步、属性模板拉取和字典值拉取不再复制到店铺级缓存。新增全局字典查询缓存，空结果也会缓存，避免重复访问 Ozon。
- 启动时会把旧店铺缓存一次性合并到全局表；未经过启动钩子的旧数据库/测试库在首次访问时也会惰性合并。全局行优先，旧表不再作为业务回退来源。
- 验证：相关编辑器/批量/流水线/决策记忆测试通过；`python -m compileall -q app` 通过。旧测试中仍有与本次无关的反馈字段断言、颜色字典缺失和“同步后仍期待店铺旧表”的历史断言。

## 2026-08-28：编辑器按类目预加载全局属性与字典选项

- 新增只读接口 `GET /api/v1/shops/{shop_id}/metadata/categories/{category_id}/types/{type_id}/cached-values`，按类目/类型返回本地全局字典选项，每个属性最多 200 条；不调用 Ozon。
- 编辑器选定类目/类型后，与属性模板请求并行预加载该接口；多选/单选下拉先从 `state.attributeOptionsCache` 本地过滤和展示，只有本地没有匹配时才调用原有 Ozon 搜索接口。
- 字典选项仍按需限制数量，避免把超大字典一次性塞进浏览器；首次搜索返回后继续写入全局字典值表与全局查询缓存，后续店铺复用。
- `node --check frontend/listing-editor.js`、`python -m compileall -q backend/app` 和 4 项缓存/属性回归测试通过。当前运行中的旧后端未重启，新接口需在批量任务安全暂停并持久化游标后重启才会对浏览器生效。

## 2026-08-28：SKU 图片库双栏与可拖动排序

- “SKU 图片设置”弹窗拆为左右两栏：左侧是当前 SKU 已选图片，右侧是公共总图库，可将图片补充到当前 SKU；移除、加入和排序均只修改当前 SKU 的 `image_urls`，不删除公共图库，也不影响其他 SKU。
- 左栏支持 HTML5 拖放、指针和鼠标兜底拖动；第一张始终作为该 SKU 首图，另有“设为首图”按钮。修复了重排后渲染又按公共图库顺序覆盖的问题，历史草稿缺少 `image_urls` 时也能正常初始化并排序。
- 前端缓存版本同步更新为 `listing-editor.js?v=98`、`listing-editor.css?v=29`，真实浏览器验证双栏布局、拖动换序、设首图、移除后从右栏重新加入，公共图库数量保持不变，控制台无 error/warn。

## 2026-08-28：SKU 图片弹窗滚动与总图库展示

- 删除/加入/设首图触发重渲染前后，保存商品弹窗和两侧图库的 `scrollTop`，不再因操作跳回页面顶部。
- 桌面端弹窗扩大到约 1120px，图片卡片缩小为紧凑网格；左右图库各自滚动，便于一屏查看更多图片。
- 右栏始终展示完整公共总图库；已加入当前 SKU 的图片显示“已加入当前 SKU”，未加入的显示“加入此 SKU”。SKU 专属图仍只保留在左栏当前 SKU 集合中。

## 2026-08-28：商品编辑器 SKU 独立图片选择

- 变体记录新增可选 `image_urls_json`，用于保存每个 SKU 自己选择的图片集合；公共详情图库仍保存在草稿 `images_json`。
- 编辑器“产品图”入口改为“SKU 图片设置”：可针对当前 SKU 勾选/取消图片、设置首图，变更不影响公共图库和其他 SKU。取消后的公共图仍可重新勾选。
- 提交路径和批量发布路径均优先使用显式 SKU 图片集合，并保持首图和 15 张上限；历史草稿无该字段时保留原有兼容行为。
- SQLite 启动增量迁移和 Alembic 迁移均已加入。

## 验证

- `node --check frontend/listing-editor.js`、`python -m compileall -q backend/app` 通过。
- `backend` 定向测试：6 passed；既有测试中 2 个与本次无关的基线失败仍存在（全局类目测试数据前置、反馈字段断言）。
- 浏览器编辑器实测：SKU 面板显示独立设置文案；取消一个图片后选中数变化、公共图库数量不变；控制台无新增 error/warn。
- `http://127.0.0.1:5500/listing-editor.html` 与 `http://127.0.0.1:8000/health` 均返回 200；草稿接口返回 `image_urls: null` 证明旧草稿兼容加载。

## 2026-08-26：商品编辑器货源素材与多 SKU 图片边界

- 编辑已有草稿时，按草稿保存的 `source_product_id` 自动加载右侧 1688 货源素材；即使货源原始关联属于另一店铺，只要草稿明确绑定该货源，也允许读取，不要求运营在全量采集商品中重新选择。
- 右侧素材明确分为“详情/公共产品图”和“SKU 专属图”。Ozon 最多 15 张的限制只作用于公共图库；SKU 图继续保留在对应变体的 `image_url`，不得因公共图库截断而删除或混入公共图。
- 提交预览与实际提交统一以已保存草稿的变体、价格、尺重和 SKU 首图为准，避免旧 `pipeline.variant_mapping_json` 覆盖已编辑的多 SKU 草稿。每个 item 的 `images[0]` 必须是该 SKU 独立图片；公共图最多追加 15 张。
- 本地样本 `draft_id=174` 已验证：草稿 42 个变体，提交预览 42 个 item、42 个不同首图；货源素材显示 13 张公共图和 44 张 SKU 图。验证未调用 Ozon 写接口。
- 后续修正：不得要求运营点击“回填最新货源图片与 SKU 图”。草稿创建时从当前货源快照自动持久化公共详情图；打开历史草稿时自动进行本地安全迁移。徽章批次 #1 已检查 273 份草稿并修复其中 231 份；样本 `draft_id=172` 现有 13 张公共图、18 张 SKU 图，提交预览每个 SKU 为“独立首图 + 13 张公共图”。全程未发送 Ozon 写请求。
- 批量行操作已明确为“重新处理并提交”或“重新校验后继续”，不是重复发送旧 payload：重新处理会从货源重建本地草稿，并执行图片隔离、属性/内容预检和自动修复；仍有阻塞则保持“需处理”。仅在预检通过且店铺有额度时才提交；等待额度复核后仍不足则继续等待，禁止反复外发。
- Ozon 新反馈 `BR_attribute_advertising` 与主题标签单项长度限制已提升为提交前硬规则：标签逐项 20–30 个、每项最多 30 字符、俄文格式、去重，并删除批发/促销/礼物/时尚等营销词；标题同样删除 `оптом`、`модный` 等营销/批发修饰。该清洗不靠 AI 提示词，重试和新建草稿都会执行。根因已修正：编辑器存储可用空格分隔显示，但 `/v3/product/import` 必须把每个标签拆为属性 `23171` 的独立 `values[]` 项，不能把整串当作一个值。
- 两个真实错误码已固化到 `ozon_error_patterns`：`BR_hashtags_symbols_limit / 23171 → clean_marketing_tags`，`BR_attribute_advertising / title → clean_advertising_title`；本地预检确认会按该错误码命中，短于20个标签则保留“需按事实重新生成”的阻塞说明，禁止用营销词填充。
- Ozon 回传状态闭环补齐：批量行不再把“导入任务已接收”误展示成成功。历史草稿本地 Ozon 回传已回灌：徽章批次 #1 有 69 条实际卡片错误改为“需修正提交”，连同既有 14 条图片质量问题，共 83 条需处理、190 条已成功、2803 条等待。列表刷新不会再把这些行覆盖回“已成功”；`SPU_ALREADY_EXISTS_IN_ANOTHER_ACCOUNT` 按运营已确认的“跨店重复可接受”规则不升级为待修正。质量暂停绝不因此自动恢复或外发。
- 修复质量预检模块中的陈旧模型导入：`quality_preflight.py` 不再引用已不存在的 `OzonErrorPatternRecord` / `OzonSubmissionErrorLogRecord`；否则重试时预检模块本身会导入失败，自动修正规则不会执行。

## 2026-08-19：Ozon 生图规则优化

- Image 2 恢复为生成带俄文详情介绍的产品套图候选：第一张偏主图展示，后续图片介绍核心参数、卖点和使用场景；俄文内容必须由 ERP 根据已确认事实提供，不能由模型自由猜测。
- 生图前建立产品事实清单并锁定外观、颜色、材质、结构、比例、Logo、原文字、SKU 差异和包装包含物；未有证据的属性不得进入 Prompt。
- 保留“多图分析、单图发送”、3:4 竖版、产品占 65%-80%、单一主体、完整展示和人工采用门禁；第一张主图与后续详情介绍图分工明确。
- 验证范围增加纯图/文字图分离、俄文后处理准确性、产品结构与 SKU 主图一致性、人工确认前禁止写入草稿或 Ozon payload。

## 2026-08-17：1688 严选挂机采集风险与尺重门禁调整

- 产品主线调整为“夜间预处理，白天人工上架”：后台只形成待人工候选，禁止自动创建上架草稿、自动 AI 处理、自动审批和自动 Ozon 提交。次日运营确认图片、SKU、尺重、价格和风险后才可创建草稿，且必须明确选择目标店铺。
- 严选搜索列表缺尺重不再作为淘汰证据；系统继续调用官方详情，并从商品属性、SKU 规格、包装和物流字段提取重量与长宽高。
- 官方详情仍无法证实完整尺重时，候选保留为“待补尺重”，不自动入采集箱、不进入 AI 或发布流程，供运营补充证据后处理。
- 挂机采集增加不可绕过的风险规则：危险/易燃易爆、液体喷雾、服装鞋靴、农资农药与驱虫液、工业品和原材料；重量超过 2kg、单边超过 400mm、体积超过 20L 的商品也自动拦截并记录原因。
- 验证应覆盖：详情属性提取尺重、缺尺重进入待补状态、风险关键词和超大件均保留明确淘汰原因；不得启动真实采集任务作为自动测试。

## 2026-08-13：1688 严选独立导航

- 目标会话 `019fe197-4c51-7b90-8c9b-5fbbf5990b60` 对应项目 `E:\new ozon erp`；严禁再修改旧 `ozon-project-copy` 作为该 ERP。
- 左侧导航新增“1688 严选”，使用官方 `com.alibaba.fenxiao/jxhy.product.getPageList-1` 做只读关键词搜索。
- AppKey、AppSecret、AccessToken 仅从后端环境变量 `OPEN1688_*` 读取；前端只显示是否配置和 AppKey，不回显密钥。
- 当前功能只展示候选商品，不下单、不自动上架。验证顺序：配置 OAuth accessToken、重启后端、搜索得到真实响应后，再固定详情字段并接入采集箱。

- 夜间严选计划支持“无关键词挂机”：留空时由服务端使用受控的系统轮换探索词池。官方接口目前没有全平台随机枚举端点，因此不能声称无条件随机抓取全部商品。
- 夜间结果进入 `automation_candidates` 候选商品池（`ready_for_review`），详情证据保存在 `capture_json`；未选店铺前不写 `source_products`，人工选择店铺并人工编辑后才进入商品编辑器。
- 自动任务状态逻辑已统一：新建/复制默认启用；暂停仅由人工操作产生；恢复时重算未来执行时间；调度器不连续补跑已经错过的历史周期。已建立的 4 个店铺分类任务已切换为启用，目标每店每晚 250 个。
- 候选池默认已收敛为可操作工作队列，不再把 `detail_pending`、风险淘汰和尺重淘汰混在首屏；内部淘汰证据仍可通过状态筛选查看。

## 已完成（2026-08-03）

- FastAPI 店铺管理：健康检查、店铺 CRUD、仓库及 API 凭据的安全数据模型。
- FBS 领域服务：商品/SKU、可售库存和预占、订单状态机、取消释放库存、打包时限风险。
- 定价与营销控制：人民币物流档位、迭代定价、`old_price`/`min_price`、活动价格与利润保护、审批及审计事件。
- 静态运营概览前端壳，可读取 `/api/v1/shops`。
- 12 个本地领域测试通过。
- 产品语言固定为中文（`zh-CN`），店铺和商品价格币种固定为 CNY；创建或更新店铺时拒绝其他币种。
- 店铺接入已改为 Ozon Seller 标准的“店铺名称 + Client ID + API Key”；密钥写入时加密、读取时绝不回显。
- 商品与 FBS 订单的只读同步 API 已接入本地持久化和同步运行日志；前端同步中心可选择店铺后触发。
- 删除规则已校正：仅有失败同步记录的未完成店铺可删除；已拥有商品、订单或审计等业务数据的店铺只能停用/归档。
- 项目总目标、范围控制和阶段验收以根目录 `AGENTS.md` 与 `docs/MASTER_PLAN.zh-CN.md` 为最高指示。
- 已完成一个真实已授权店铺的只读同步验证：FBS 订单同步成功写入 5 条；商品同步接口成功但该次响应为 0 条。已新增商品与 FBS 订单本地列表 API，前端可展示同步结果。
- FBS 订单现按“订单头 + 商品行”落库；同步会替换同一订单的商品行，避免旧明细残留。订单列表新增“查看明细”抽屉，展示订单状态、截单时间与商品/Offer ID/数量。
- 已用真实授权店铺再次完成只读验证：同步得到 5 条 FBS 订单，抽样订单的商品行可由详情接口读取。验证过程未输出 Client ID 或 API Key。
- 后端测试增至 26 项通过，前端脚本语法检查与差异格式检查通过。
- FBS 订单图片已接入：订单图片同步通过只读 `/v3/product/info/list` 按 SKU 或 product_id 获取图片，并保存主图链接；订单明细抽屉展示缩略图，缺图时显示“无图”占位。已对真实授权店铺验证抽样订单的商品主图可显示。
- FBS 订单风险队列已完成：订单页支持全部、待打包、配送中、风险订单筛选；对未关闭订单自动识别已超截单和两小时内截单风险，状态中文显示，导航红点显示风险数量。已在本地页面完成筛选交互和浏览器控制台验证。
- 已完成商品上架模块边界与开发方案：以草稿、类目/属性映射、图片与 CNY 核价预检、审批、`task_id` 跟踪为核心；真实 `/v3/product/import` 写回归入阶段 7，避免绕过审批和审计。
- 商品上架第一期已完成：本地上架草稿、单变体、CNY 核价和预检 API/中文页面可用；缺类目、主图、采购成本、重量或包装尺寸会阻止草稿进入审批。未调用任何 Ozon 写 API。
- 商品上架类目基础已接入：4 个测试店铺均完成 Ozon 末级类目与商品类型的只读缓存，共 29,688 条；草稿页面改为中文类目选择器，不再要求运营手填类目 ID。
- 已接入当前只读 `/v1/description-category/attribute` 属性接口。真实抽样类型返回 44 个属性，其中 3 个必填；页面选择类目后会展示必填属性摘要。当前仅完成“发现与提示”，属性字典值、属性录入和逐项校验仍是下一步，不得把当前状态描述为可直接发布。
- 草稿模型新增 `type_id`，本机 SQLite 采用向前兼容的增量迁移；预检缺少商品类型时会阻止进入审批。整个流程仍未调用 Ozon 商品写 API。
- 已建立后台数据库同步架构：正式环境配置 PostgreSQL 16、psycopg 与 Alembic 完整初始迁移，SQLite 仅保留给单机开发和自动化测试。
- 页面改为本地数据库优先：选择店铺或切换经营概览、FBS 订单、商品与库存、商品上架页面时，先显示本地数据，再请求统一 `auto-sync` 入口进行后台校正；普通操作不再要求点击同步。
- `sync_states` 按店铺和资源保存最近成功时间、商品滚动游标、订单窗口及可续期任务租约。同一资源成功后 5 分钟内不重复访问 Ozon；PostgreSQL advisory lock 保护首次建状态的并发竞争，多标签页同时触发只允许一个任务启动。
- FBS 订单采用上次成功窗口减 10 分钟的重叠校正并完整遍历全部分页；任一页失败都不推进检查点。商品每轮最多滚动 5 页并持久化下一游标。类目和图片资源缓存 24 小时；订单图片增加行级校正时间并在订单行 upsert 时保留，避免重复下载已有图片。
- 人工按钮保留但改为“强制校正/强制完整校正”；所有自动流程仍为 Ozon → ERP 只读，不含库存、价格、营销、发货或商品发布写回。
- 商品上架必填属性闭环已完成：属性模板和字典值均落本地数据库缓存；页面按类目动态生成必填字段；字典字段输入至少 2 个字后调用 `/v1/description-category/attribute/values/search`，单次最多 50 条，必须选择带 `value_id` 的 Ozon 返回项才能保存。草稿属性独立持久化，预检会逐项拒绝缺失的必填字典值。
- 真实授权店铺已完成只读验证：缓存类目 1000 条、抽样商品类型属性 44 条、字典搜索 20 条，重复请求结果一致；未创建虚假商品，未调用任何 Ozon 写 API。
- 完成独立代码审查后的安全收口：字典 `value_id` 必须存在于当前店铺/类目/类型/属性缓存且文本一致；前端用请求序号抵御店铺 A→B→A 的旧响应覆盖；后端拒绝跨店类目；同名字典值以 Ozon ID 区分；属性与查询缓存不会阻止删除未完成店铺；空搜索和窄/宽搜索按查询维度独立缓存。

## 安全与 API 结论

- 不可把店铺 API Key 写入源码、测试、日志、Git 或本文件；仅在本地 `.env`/密钥管理服务提供。
- Seller API 的旧 `POST /v1/warehouse/list` 已在只读探测中被明确标为废弃，禁止接入。
- 后续只读连通性优先使用当前产品或 FBS posting 查询接口；写类 API 必须先有人工确认、审计和沙箱/小范围验证。

## 下一阶段验收

1. 为 FBS 订单列表实现状态筛选与可执行的本地拣配清单（不调用 Ozon 写 API）。
2. 使用当前 Seller API 的替代仓库接口完成只读适配，并以 mock contract test 固化。
3. 将当前应用内后台任务迁移到独立 worker，并补限流退避与订单/商品多页完整周期契约测试。
4. 商品上架下一步完成图片可访问性/数量/格式预检和多变体属性模型；随后建设审批界面与 payload 预览，不提前接入发布写回。


## 1688 -> Ozon 自动上架流水线（P0-P7）

### 主线调整（2026-08-05）

- 取消“同一主体下店铺 A -> 店铺 B 直接复制商品”方案；该方案不作为 ERP 商品来源主线。
- 新主线改为 Ozon 公开详情页采集 -> 本地二开草稿 -> 内容/图片/价格重做 -> 预检 -> 审批 -> 发布。
- 扩展已支持 Ozon 详情页快照发送到 `/api/ozon/capture`；后端以 `source_platform=ozon_public` 入库，公开页面价格只保留在 `raw_json`，不得当作 CNY 采购成本。
- 公开采集仍需人工处理品牌、版权、水印和重复内容风险，当前不自动发布。
- 已将可用扩展复制到 `browser_extension/erp-collector-extension`，保留 1688/Ozon 详情采集、SKU 筛选、店铺选择和 crawler worker；新增 Ozon 搜索列表卡片浮层。浮层通过 MutationObserver 处理无限滚动卡片，展示评分、评价、页面可见尺重和销量提示，并避免重复挂载。
- Ozon 详情快照新增 `packageInfo`（`weightG`、`lengthMm`、`widthMm`、`heightMm`）与 `salesHint`；页面推测数据仅标记 `capture_hint`，缺失时不填默认值。扩展语法检查通过，后端测试 78 项通过。
- 新增浏览器端 `composer-api.bx/page/json/v2` 尝试：列表商品链接会用当前 Ozon 页面会话读取同源页面 JSON，解析可用尺重/销量字段并回填浮层；失败时静默回退到 DOM，未向浏览器暴露店铺 API Key。
- 已增加主世界 Fetch/XHR 观察器：仅转发当前 Ozon 页面已加载的 JSON 响应摘要到内容脚本，按商品 ID 匹配浮层并回填字段；不读取或转发 Cookie/API Key，响应过大时丢弃。

- 新增 `backend/app/pipeline/` 包，覆盖 P0-P7 全链路：
  - P0 `contract.py`：1688 商品快照 JSON Schema、幂等键、校验与规范化。
  - P1 `ingestion_service.py`：幂等 upsert 导入，重复导入更新而非新建。
  - P2 `fact_extraction.py` + `category_matching.py`：产品事实提取、Ozon 类目召回 Top 20 + 规则重排 Top 5 + 人工锁定。**已加入产品类型-类目域映射和负向关键词排除**，防止跨域误匹配（如耳机匹配到消防设备）。
  - P3 `attribute_mapping.py`：确定性映射规则（材质 -> Material 等）+ 中俄同义词表 + 字典搜索与 value_id 验证。
  - P4 `variant_mapping.py`：1688 规格轴解析、稳定 SKU 编码（SHA-256）、图片编排（8-15 张）与合规检查。
  - P5 `content_generation.py` + `llm_translate.py`：**LLM 驱动的中俄翻译**（OpenAI 兼容 API，通过 `LLM_API_KEY` 环境变量配置）；无 API key 时使用词典回退并标记 `content_verified=False`。俄语标题/描述/规格块生成 + 每 SKU CNY 定价（复用 PricingService，规则版本 1.0.0）。
  - P6 `quality_check.py`：类目置信度、属性覆盖率、内容完整度（**含西里尔字母验证和中文字符惩罚**）、图片/SKU 合规、定价健康度评分 + Ozon payload 预览。未通过 AI 验证的内容会被标记为需人工审核。
  - P7 `publish_service.py`：草稿创建 -> 审批（审计记录）-> **真实提交 Ozon `/v3/product/import`** -> 状态轮询。用户已批准使用真实写回以便发现问题。
- 新增 5 个数据模型：`SourceProductRecord`、`SourceVariantRecord`、`SourceMediaRecord`、`PipelineProductRecord`（含 `content_verified` 字段）、`PipelineProgressRecord`。
- 新增 14 个 API 端点 + Chrome 扩展桥接端点，全部注册到 `main.py`。
- `frontend/progress.html` 从后端 API 实时拉取进度，10 秒自动刷新。
- 后端测试增至 74 项通过（含域兼容性测试、LLM 翻译回退测试）。

### 已发现并修复的问题（2026-08-04）

1. **内容语言错误**：P5 曾直接输出中文标题作为 `title_ru`，已修复为 LLM 翻译 + 词典回退。
2. **类目跨域误匹配**：P2 曾将蓝牙耳机匹配到"防护和消防设备"类目，已加入域兼容性检查。
3. **质量分虚高**：P6 曾只检查字段是否存在不检查语言，已加入西里尔字母验证和中文字符惩罚。
4. **测试破损**：4 项测试失败已修复（FakeOzonClient 签名、缺失属性缓存、P7 API mock）。
5. **代码卫生**：清理了 5 个根目录临时脚本。

### 待改进项

- 定价模型仍使用硬编码尺寸（500g/200x150x100mm），需从 1688 采集数据中提取真实重量和尺寸。
- LLM 翻译需要配置 `LLM_API_KEY` 才能产出高质量俄语内容；无 key 时词典回退质量有限。
- P7 真实写回已用于测试，已成功发布 1 个商品到 Ozon（task_id 5306797528），但该商品的标题曾为中文（已修复代码，旧数据需重新生成内容后重新发布）。


## 2026-08-06 商品发布编辑器开发

### 完成内容

**后端新增**
- `ai_service.py` — DeepSeek AI 服务模块（翻译、属性推荐、描述生成、富内容生成）
- `POST /api/v1/ai/translate` — 文本翻译（中文→俄文）
- `POST /api/v1/ai/suggest-attribute` — 属性值 AI 推荐
- `POST /api/v1/ai/generate-description` — 商品描述 AI 生成
- `POST /api/v1/ai/generate-rich-content` — 富内容 JSON 生成
- `GET /api/v1/shops/{shop_id}/pipeline/source-products/{sp_id}` — 采集商品详情（含变体和图片）
- `GET /api/v1/shops/{shop_id}/listing-drafts/{draft_id}` — 获取单个草稿
- `PUT /api/v1/shops/{shop_id}/listing-drafts/{draft_id}` — 更新草稿
- `OzonAttributeCacheRecord` 新增 `complex_id`、`is_aspect`、`description`、`is_collection` 字段
- 正确从 Ozon API 捕获 `attribute_complex_id` 和 `is_aspect`（变体属性标识）

**前端新增**
- `listing-editor.html` — 完整商品发布编辑器（9 个分区，对齐无忧易售）
  1. 店铺与类目（搜索+选择+Offer ID+标题+智能标题）
  2. 产品属性（动态渲染，必填/选填分组，字典搜索，AI填充）
  3. 本地信息（来源URL+备注）
  4. 文字描述（AI生成+AI翻译）
  5. JSON富内容（AI生成：欢迎语+描述+5张图）
  6. 产品图片（URL添加+采集导入+主图标记）
  7. 视频（链接+封面）
  8. 变体设置（is_aspect驱动变体识别+笛卡尔积生成SKU行）
  9. 产品信息（增值税+积分评价）
- `listing-editor.js` — 编辑器逻辑（类目联动、AI填充、变体生成、草稿保存）
- `listing-editor.css` — 编辑器样式
- `index.html` 商品上架 tab 新增"进入发布编辑器"链接

### 关键设计决策
- **表单驱动**：不是流水线/Agent，而是每个字段独立填写+AI辅助
- **is_aspect**：Ozon API 通过 `is_aspect=true` 标记变体属性，非 `complex_id`
- **属性值收集**：保存时直接从 DOM 读取，不依赖事件回调
- **DeepSeek**：翻译走 `deepseek-chat` 模型，API Key 在 `.env` 中配置

### 文件路径
- 编辑器页面：`E:\new ozon erp\frontend\listing-editor.html`
- 编辑器JS：`E:\new ozon erp\frontend\listing-editor.js`
- 编辑器CSS：`E:\new ozon erp\frontend\listing-editor.css`
- AI服务：`E:\new ozon erp\backend\app\ai_service.py`
- 访问地址：`http://127.0.0.1:5500/listing-editor.html`

### 待后续完善
- 富内容可视化编辑器（当前为文本编辑）
- 图片处理（裁剪、去水印、白底）
- 定价模型集成到变体表格
- 草稿列表管理（在主页面展示已保存草稿）

## 2026-08-10 编辑器字典属性修正

- 修复多选字典属性的预检：性别等字段的 `value_id` / `value_text` 按 `|` 拆分，逐项核验 Ozon 缓存字典，避免已选择值被误判为空。
- 新增 `test_listing_validation_accepts_multiple_dictionary_values` 回归测试；实际草稿 `KC000005` 的 `/validate` 已返回 `ready_for_approval`。
- 变体商品颜色保留真实 Ozon `dictionary_value_id`，UI 中文显示来自 `ZH_HANS` 字典缓存，提交识别以 ID 为准。
- 编辑器产品信息的增值税显示并锁定为 `0%`；写入 Ozon 的 payload 同样固定 `vat: "0"`。

## 2026-08-10 Chrome 真实提交验证

- 已在 Chrome 当前编辑器草稿 `KC000005` 完成“刷新 → 保存 → 预检 → 提交”全链路验证：性别多选和类型的 Ozon `value_id` 刷新保存后仍保留，输入框固定左对齐。
- 修复草稿加载与 AI 填充竞态：加载既有草稿不再启动 AI 覆盖；AI 仅补空字段；多选管道值在渲染前恢复为数组。
- 真实 Ozon 导入任务 `5364955457`：`KC000005-XZ6030`、`KC000005-XZ6031`、`KC000005-XZ6029` 均为 `imported`，3 个 SKU 库存均成功上传 999，导入警告为 0。
- 平台初次警告的系列（9725）、首饰风格（10016）、保证（10400）已补全 Ozon 字典 ID 后重新提交；同步反馈为 `validation=success`、`issueCount=0`。

## 2026-08-10 多变体 SKU 主图修复

- 根因：提交 `/v3/product/import` 时每个变体都使用商品级 `images[:15]`，忽略了 `ListingVariantRecord.image_url`。由于 Ozon 将 `items.images[0]` 作为主图，KC000005 三个 SKU 被错误地展示为同一张图库首图。
- 修复：新增 `build_variant_image_list()`，每行发布数据以该 SKU 图作为第一张，随后追加公共详情图并去重；变体图缺失时才回退公共图库。
- 验证：新增两项回归测试；用真实草稿 4 生成的三个 payload 首图分别与 XZ6030、XZ6031、XZ6029 的 `image_url` 精确一致。未自动重新提交 Ozon，避免未经确认改写已发布商品。

## 2026-08-10 草稿上品模板

- 新增 `listing_templates` 持久化表与店铺级 API：列出模板、保存当前模板。
- 模板保存类目/类型、固定产品属性和值，并可由运营明确选择是否一并保存固定俄文描述；不保存标题、Offer ID、图片、SKU、价格、库存或尺重。
- 编辑器顶部新增“选择上品模板 / 套用模板 / 存为模板”。套用时切换到模板类目并加载该类目属性，再回填模板值；AI 不参与覆盖。
- 验证：`tests/test_listing_templates.py` 与 `tests/test_listing_service.py` 共 9 项通过；服务重启后 `/health` 为正常，模板表查询可用，编辑器页面返回 HTTP 200 且包含模板控件。

## 2026-08-10 全店铺营销活动与一键退出

- “定价与营销”已拆成“自动定价 / 营销活动”两个页签；营销活动统一读取 ERP 名下全部店铺，不受顶部店铺筛选限制，并逐行显示所属店铺。
- 对接 Ozon `/v1/actions`、`/v1/actions/products`、`/v1/actions/products/deactivate`。真实 K 店铺验证表明，活动商品分页请求的 `last_id` 必须发送字符串（首次为空字符串），不能发送数字 `0`。
- “退出全部可退出活动”会先读取全店活动，由前端展示影响数量并要求确认；后端还要求 `confirmed=true`，否则返回 422。仅精确类型 `CPC`、`CPO` 受保护；`ELASTIC_BOOSTING`、`STOCK_DISCOUNT` 等活动可退出。
- 后端按店铺、活动分页读取参与商品，按最多 1000 个商品分批退出；返回完成活动、移除商品数和逐项失败，并写入 `promotion_exit_all` 审计事件。
- 已通过营销保护/商品 ID 提取、Ozon 请求体测试、Python/Node 语法检查；前端与后端健康检查通过。Codex 未替用户执行真实退出，只有运营人员在界面确认后才会写回 Ozon。

## 2026-08-10 自动定价策略参数化

- 新增 ERP 全店铺共享的 `pricing_policy` 持久化策略；可调整采购缓冲、佣金率、比例/固定杂费、目标/最低利润、最低利润额、原价倍数、物流占比提醒和最大迭代次数。
- 自动定价页以四段公式展示“采购成本 → 物流等级 → 成本合计 → 迭代售价”，并显示人民币物流等级。参数可编辑、即时试算、保存，策略修改会为每个店铺写入 `pricing_policy_updated` 审计事件。
- 新增 `/api/v1/pricing/policy` GET/PUT 和 `/api/v1/pricing/quotes` 批量试算接口。试算可带未保存的页面参数；正式导入不带覆盖值，只使用数据库已保存策略。
- 删除上品编辑器的“成本 ×3”前端硬编码。新采集 SKU 尺重完整时通过统一报价接口填写采购成本、售价、原价、最低价、物流等级和运费；尺重/货源价缺失时 fail-closed 并提示人工补全。
- 草稿预检和 P5 自动上架流水线也读取同一策略；P5 采购成本统一包含策略中的采购缓冲。
- 验证：48 项定价/上架/流水线测试通过；真实接口保存后数据库仅一条策略记录。货源价 ¥10、100g、100×80×50mm 在默认策略下得到采购成本 ¥15、售价 ¥42.46、原价 ¥84.92、最低价 ¥33、Extra Small 运费 ¥5.50。

## 2026-08-10 FBS 订单图片列表可视化

- FBS 订单列表 API 现在随订单返回 `lines`，包括商品名、Offer ID、数量和已同步的 Ozon 图片 URL；详情接口保持同一结构。
- 主列表新增“商品”列，每行直接显示最多 3 张商品缩略图、首个商品名称、商品种数和总件数；“查看明细”仍保留完整商品列表。
- 真实 K 店铺验证：13 个订单全部返回商品明细和图片，页面渲染 13 行商品预览与 13 张缩略图；首图完成解码，尺寸为 900×1200，控制台无错误。

## 2026-08-12 KC000006 Offer ID 超长修复

- Ozon HTTP 400 根因：第三个变体 `KC000006-Чехол_для_телефона_с_дизайном_Человека-паука` 长 53 字符，超过 `Request.OfferId` 最大 50 字符。
- 新增前后端一致的 Offer ID 规范化：短值保持不变；超长值保留可读前缀，并追加基于完整原值生成的稳定 8 位哈希，保证 ≤50 且避免简单截断撞码。
- 规范化已接入 SKU 自动生成、单个/批量译俄文、人工编辑失焦、草稿创建/更新、P7 自动上架、预检和最终提交；输入框显示字符计数并设置 `maxlength=50`。
- KC000006 超长 SKU 已保存为 `KC000006-Чехол_для_телефона_с_дизайном_Че-3f77d6d6`（50 字符），其余 9 个 SKU 不变；本地预检返回 `ready_for_approval`，未自动重新提交 Ozon。

## 2026-08-13 Ozon 完整商品卡回拉

- 采集箱“从 Ozon 拉取”由单个 `product_id`/单变体改为按 `/v3/product/info/list` 的 `model_info.model_id` 归组，同模型全部子商品分别写入一个草稿的 SKU 行。
- 每行恢复独立 Offer ID、售价/原价/最低价、条形码、FBS 库存、mm/g 尺重、变体字典文本与 `dictionary_value_id`；商品级公共属性只保存一次。
- 已有草稿返回 HTTP 409 和草稿编号，前端可直接打开原草稿；只有运营人员明确确认后才使用 `overwrite=true` 重建属性与变体，避免静默覆盖编辑内容。
- 真实 K 店铺只读验证：KC000005 的任一子 SKU 均识别为同一 `model_id`、共 3 个变体；冲突接口正确返回 `draft_id=4, variant_count=3`。后端健康检查正常、前端 HTTP 200。
# 2026-08-13：Ozon 上架后的状态驱动库存监控

- 商品提交接口不再通过固定等待几秒直接写库存。提交后为草稿持久化 `waiting_product` 库存任务。
- 后台每 15 秒扫描到期任务：确认全部变体 Offer ID 已在 Ozon 生成，再确认全部状态为 `price_sent`。
- 条件满足后，根据各 SKU 的尺重与售价匹配 Extra Small / Budget / Small / Big FBS 仓库，写入草稿中保存的库存数量。
- 调用 `/v2/products/stocks` 后，再用 `/v2/product/info/stocks-by-warehouse/fbs` 回读；只有每个 SKU、仓库和目标库存一致才标记 `completed`。
- 采集箱“提交结果”展示库存监控状态，并为未完成任务提供“检查库存”入口。
- 验证：`test_listing_stock_monitor.py` 与 `test_ozon_seller.py` 共 11 个测试、3 个子测试通过；前端 `node --check` 通过。

## 2026-08-13：模板与分类动态属性的合并顺序

- 模板仍绑定 Ozon `category_id + type_id`，但不再被当成完整表单快照。
- 套用时先请求该类目当前完整属性；模板只覆盖已保存的固定项。
- 随后调用采集属性匹配和 AI，对模板没有保存的空项继续补全；品牌、原产国、件数和尺重默认规则继续补空。
- 模板保存了描述才覆盖当前描述；未保存描述则保留该采集商品已经生成的描述。

## 2026-08-13：自动售价底线 26.99 CNY

- 全局定价策略新增 `listing_price_floor_cny`，默认并当前设为 `26.99`。
- 定价迭代完成后，若公式售价低于该值，最终售价改为 `26.99`；佣金、杂费、利润、原价均按最终售价重新计算。
- Ozon 最低售价统一回归取整规则：有小数向下取整，整数减 1。因此售价 26.99 的最低售价为 26。

## 2026-08-13：属性输入、提交完成态和模板勾选

- 多选属性的内部搜索框不再随每次输入重建，支持中文输入法、俄文和单字符搜索。
- 保存并提交成功后按钮切换为“已提交”，顶部显示提交状态并提供“返回采集箱”。加载已提交草稿时也恢复相同完成态。
- 每个属性行增加“存入模板”勾选；创建模板时只保存勾选且有值的字段。描述改为编辑页显式勾选是否存入模板。
## 2026-08-13：修复可选字典属性以纯文字提交

- 根因：KC000006 的“装修风格”(10016)虽显示“休闲”，但草稿只保存 `value_text`，没有 Ozon `value_id`，提交 payload 因此缺少 `dictionary_value_id`。
- 修复：创建/更新草稿、保存模板时统一规范化字典值；本地缓存精确命中时补齐 ID，否则返回 422。预检现在覆盖所有已填写的字典属性，不再只检查必填项。
- 数据修复：KC000006 及“徽章”“徽章2”模板中的“休闲”已绑定 Ozon 值 `970672426`。
- 验证：`test_listing_service.py` 与 `test_listing_templates.py` 共 11 项通过；真实草稿 6 预检 issues 为空，后端 `/health` 正常。
## 2026-08-13：编辑页返回采集箱

- 商品编辑页顶部两个返回入口统一指向 `index.html#collection-box`，不再返回 ERP 经营概览首页。
- ERP 首页加载时已有 hash 路由处理，进入后会直接激活并加载采集箱列表。
- 验证：静态页面实际响应中的两个返回链接均为采集箱地址，前后端健康检查正常。
## 2026-08-13：采集箱提交结果三项固定展示

- “提交结果”固定分为库存、问题、分数三行，不再把评分和问题压缩为一个数字角标。
- 库存读取持久化库存监控状态；只有 `completed` 显示“已生效”，其他状态显示等待、确认或重试。
- 问题显示真实 Ozon 问题数量，悬停查看完整内容；未校对显示“待校对”，真实无问题显示“无问题”。
- 分数只展示 Ozon 返回的真实 `quality_rating`，缺失时显示“待同步”。
## 2026-08-13：新采集商品自动串行生成描述与富内容

- 进入没有草稿的新采集商品编辑页后，自动执行“生成俄文描述 → 等待成功 → 用该描述生成富内容 JSON”，无需点击 AI 按钮。
- 修复旧自动请求字段错误：描述接口改用 `product_title/source_description/specs/target_lang`，不再传无效的 `title/attributes`。
- 移除类目属性加载阶段的富内容抢跑调用，避免描述尚未返回就生成空富内容。
- 编辑已有草稿仍只加载已保存内容，不自动覆盖人工修改。
- 自动生成失败不再静默吞掉，页面会明确提示失败原因。
## 2026-08-13：过滤货源零库存 SKU

- 编辑器从采集商品生成变体前过滤库存明确为 0 的 SKU，只将库存大于 0 或库存证据缺失的行进入 Ozon SKU 表。
- 后端 P4 变体映射同步执行相同过滤，避免绕过前端生成零库存商品；全部为 0 时阻止映射。
- 原始 `source_variants` 不删除，继续保留采集证据和库存记录。
## 2026-08-13：1688 重复采集与跨店发布识别

- 主标识采用 `source_platform + 1688 Offer ID`，同一货源只保留一份采集快照。
- 插件采集响应返回 `duplicateStatus`，包含已采集、本店发布、其他店发布及具体店铺/ERP Offer ID。
- 采集箱状态列显示“本店已发布”或“其他店已发布”。
- Ozon 最终提交前再次检查，同一 1688 货源已在其他店铺发布时返回 409，避免重复铺货。

## 2026-08-13：采集/发布标识独立显示

- 采集箱新增独立“采集/发布标识”列，不再把重复采集和发布状态挤在普通状态文字下方。
- 每行固定显示“已采集”；存在 Ozon 商品记录时显示“本店已发布”；跨店重复时显示“其他店已发布”并通过悬停展示店铺与 Offer ID。
- 前端静态资源版本更新至 `v=23`，避免浏览器继续使用旧缓存。
- 验证：真实采集箱页面已显示新列，KC000005 等已发布商品同时显示“已采集”和“本店已发布”。
# 2026-08-14：自动上品中心第一阶段

- 新增 `automation_tasks`、`automation_runs`、`automation_candidates`、`automation_events` 四类持久化记录。
- 新增 `/api/v1/automation` 概览、任务创建/启停/立即运行、运行历史和候选明细接口。
- 真实灰度任务“收纳盒”搜索3个候选，详情核验3个，2个尺重及库存合格并进入 xymall 采集箱，1个因尺重不完整被淘汰。
- 修复批量新建货源时未先 flush 就创建店铺关联导致的事务回滚；异常处理现在先 rollback 再更新失败运行。
- 自动调度器已接入后端生命周期；当前到期任务默认分配首个活跃店铺，后续里程碑将替换为多店额度和类目路由。
- UI 设计规格保存于 `docs/automation-center-ui-spec.png`；自动上品中心已新增任务、流水线和店铺额度骨架。SKU图片处理明确由外部成熟模块接入。
- 多店调度按任务 `shop_ids` 范围、活跃状态和当日真实提交审计选择剩余额度最多的店铺；平台上限展示200，自动安全额度固定180。
- 自动调度每轮最多推进3个已采集候选进入 AI 门禁；低置信类目、必填属性不完整、AI内容未验证或质量问题会停在需处理，不创建可发布批次。
- 新增审批批次：只接收同店铺 `draft_ready` 候选，最多180个；批准与真实提交分别要求显式确认并写审计，真实提交每次最多20个。
- 已提交候选由后台轮询 Ozon 导入状态；成功后继续复用现有库存状态驱动监控，失败进入 `publish_failed`。
- 自动化、Open1688、P0-P7 与库存监控相关测试共42项通过；新版自动上品页面已用浏览器验证，修复了右侧额度栏被全局 `aside` 样式污染的问题，控制台无错误。

## 2026-08-14 自动化前端成品收口

- 新增候选商品池：真实图片、Offer ID、采购价、销量、SKU 尺重覆盖、店铺、状态原因、搜索与状态过滤。
- 候选商品支持单个/批量 AI 草稿处理；类目、属性或质量门禁不确定时进入人工处理，不越过门禁。
- 新增审批与发布中心：批次状态、店铺、商品数、审批人、批次明细、审批与分批提交。
- 真实 Ozon 提交需要二次输入“确认提交”，后端同时要求 confirmed=true；每次最多 20，每店每日安全额度 180。
- 自动任务接口支持编辑、复制和非破坏性归档；归档保留历史运行数据。
- 修复 hash 深链，刷新候选商品或审批发布页面不会返回经营概览。
- SKU 图片继续交给外部成熟图片模块，本次未修改。
- 安全验证：开发和验收期间没有启动真实严选采集或 Ozon 发布。
- 回归结果：backend 全量 112 passed，3 subtests passed；仅 FastAPI on_event 弃用警告。

## 2026-08-14 可信决策记忆 MVP

- 新增 `backend/app/decision_memory_service.py`、`DecisionMemoryRecord`、`DecisionFeedbackRecord`。
- 类目匹配已停止使用旧的未验证点击历史，改为仅合并 `trusted_memory` 推荐。
- 编辑器类目选择后提供“确认正确并记住 / 仅本次使用”，自动首选不会被记录为学习结果。
- 新增“管理可信记忆”弹窗，可看来源、可信度、确认次数、Ozon 成功/拒绝次数，并停用、恢复或撤销。
- `poll_task_status` 只在导入状态发生变化时反馈一次，避免重复轮询反复加权。
- 验证使用纯内存 SQLite：可信记忆重点回归 44 项通过；backend 全量 126 项和 3 个子测试通过；JS/Python 语法检查通过。
- 浏览器实测 `http://127.0.0.1:5500/listing-editor.html`：页面非空、管理弹窗打开/关闭成功、空状态正确、控制台无 error/warn。
- 验收过程未创建真实记忆、未改商品数据、未调用 Ozon 发布。

## 2026-08-14：KC000009 提交、图片与可信记忆闭环

- 修复编辑器直提只保存草稿、没有审计、没有流水线关联、没有可信记忆的问题。
- 草稿新增 `learning_attribute_ids_json`；前端“记住此属性”会在普通保存时持久化，不再依赖顶部“存为模板”。
- Ozon 导入确认后，以任务号幂等创建可信类目记忆与勾选属性记忆；记忆管理页同时显示类目和属性记录。
- 提交前检查图库、SKU 图和属性/富内容中的 localhost URL，上传 OSS 并验证后统一替换；失败时阻止提交。
- KC000009 已真实重提：新任务 `5407013254`，Ozon 返回 `status=imported`、`product_id=5952598340`、`errors=[]`。
- 本地图 `trans_1786673610346_0.jpg` 已替换为 `https://ozonshanghai.oss-cn-shanghai.aliyuncs.com/ozon-erp/images/20260814/bbbc55b1830ecc718ceacc3b.jpg`；草稿图库和富内容已无 localhost 引用。
- 草稿 9、货源 34 与流水线 17 已关联；流水线为 `published/imported`，任务号为 `5407013254`；已新增 `listing_submitted` 审计。
- 已生成 1 条 `ozon_verified` 类目记忆和 4 条属性记忆：保证=无担保、包括棍棒=不、每包数量=1、电池片数量=1，可信度均为 0.9。
- 独立审查后继续收紧门禁：多 SKU 必须全部在当前任务中成功且无 errors 才能学习/标记 imported；库存监控必须回查当前 task，不能把旧 Offer 当作本次成功；空 task_id 和任意 localhost payload 均失败关闭；学习记录与幂等 receipt 同事务提交。
- 隔离数据库回归：backend `128 passed, 3 subtests passed`；Node/Python 语法检查通过。真实 Ozon 回读用于生产验收，不作为测试夹具。

## 2026-08-14：采集箱发布结果刷新简化

- 采集箱每行提供独立勾选，并提供“刷新所选结果”和“刷新全部结果”；表头不再放全选框，避免与“刷新全部结果”重复。刷新直接使用每条草稿已绑定的 `shop_id/draft_id`，依次回读 Ozon 分数、问题与库存状态。
- 列表继续固定展示库存、问题、分数三项；刷新完成后直接重载持久化结果，不创建额外任务流程，也不再次选择店铺。
- “从 Ozon 拉取”取消顶部店铺前置条件，改为跨全部已接入店铺搜索 Offer ID；搜索结果显示所属店铺并用该店铺拉取完整商品卡。
- 修复拉取弹窗的右上角关闭和“取消”被 submit 拦截的问题，两种方式均可立即关闭。
- 前端静态服务增加 `no-store/no-cache` 响应，避免本地开发页面继续使用旧 JS。
- 浏览器验收：勾选一条后按钮变为“刷新所选结果（1）”并启用；弹窗取消和右上角关闭均使 dialog 隐藏；控制台无 error/warn。
- 修复新发布商品的评分误报：Ozon 评分响应缺少 `rating/groups` 时以 `null` 持久化并显示“Ozon 暂未评分”，不再把缺失字段默认保存为 `0/100`；响应明确返回 `rating: 0` 时仍保留真实零分。

## 2026-08-14：AI Ozon 套图接入第 6 步

- 新增 `visual_image_jobs`、迁移 `9a72c19f4e31`、AI 图片服务和生成/查询/应用 API。
- 第 6 步已有“AI 生图”、任务状态、事实/SKU 风险摘要和候选图选择；候选图改为点击卡片选取，顶部“全选”只负责全选/清空，按钮为“使用所选套图”。
- 部分失败任务只要保存了已生成 slot 仍可应用；不再强制等待 hero，第一张最终选中图作为首图。应用后 AI 图片加入产品图片库，保留原图库；变体产品图弹窗支持拖拽排序和删除，颜色样本跟随变体首图。
- 生成是后台任务；重复点击运行中的同一商品不会启动第二个付费任务。
- 应用接口接受当前任务实际生成的 URL及当前草稿已有图库 URL，并要求 `confirm_replace=true`；成功后记录 `ai_visual_set_applied` 审计。尚未调用真实付费生图测试。
- 隔离数据库测试覆盖完整/部分失败应用、非 hero 首图回退、URL 白名单、SSRF 内网地址阻断和空任务序列化；backend 全量 `135 passed, 3 subtests passed`，JS 语法通过。
- 浏览器桌面验收：真实草稿 9 正常加载，第 6 步按钮/候选区可见，未生成时应用按钮禁用，折叠/展开有效，控制台无 error/warn。移动端页面存在项目原有的整体横向布局问题，不属于本次桌面 ERP 接入范围。
- 安全收口：参考图只允许公网 HTTP(S)，逐跳校验重定向，拒绝本机/内网/保留地址，仅接受 JPEG/PNG/WebP 且最大 12MB；客户端不能再传入审计操作者。
- 当前后台任务依赖单机 FastAPI 进程，适合桌面 ERP 真实小流量试跑；进入每天 180 商品的自动化前，仍需升级为带租约/心跳的持久任务队列，并把每次生图运行改为不可覆盖的独立历史记录。

## 2026-08-14：KC000009/KC000010 Image 2 失败诊断与模型切换

- 数据核对：`KC000009` 是草稿 9 / 货源 34，目前没有 visual image job；数据库中唯一失败任务是草稿 10 / `KC000010` / 货源 35，失败发生在 Image 2 第一张候选图请求阶段，分析阶段已成功保存。
- 失败记录原先只保存 `Client error 400`，因为服务在 `raise_for_status()` 前没有保存沧猿响应正文，无法判断具体供应商原因；已改为保存 HTTP 状态、模型、尺寸、参考图数量、slot 及响应 JSON/文本摘要。
- ERP 默认模型已从 `gpt-image-2-1k` 切换为供应商面板中的 `gpt-image-2`（用户确认的 0.015 元/张模型）。本次未重新调用沧猿，避免继续产生费用。
- 费用说明：ERP 当前没有沧猿账单查询接口，不能仅凭本地任务证明“扣了 3 张”；需要以沧猿后台账单/调用明细为准。失败任务本身只确认本地没有成功落图。
- 本地排查发现两个临时诊断脚本：`backend/_test_edit.py` 发 1 次 `/images/edits`，`backend/_diag_edit.py` 按两个尺寸发 2 次，合计正好 3 次；它们属于排查 400 的付费探针，不是 ERP 正式流程。现已加显式 `ALLOW_PAID_IMAGE_TEST=1` 门禁，默认不会再误调用。
# 2026-08-15：AI 套图单参考与部分成功闭环

- Terra 继续读取多张采集图，并要求逐张记录 OCR、图片角色、事实证据和参考适用性；付费 Image 2 每个槽位固定只发送 1 张筛选后的产品图。
- Image 2 参考图不再发送 2/3 张，避免沧猿 multipart 网关 502 和尺寸/宣传图污染产品身份；ERP 模型固定归一化为 `gpt-image-2`。
- 套图按槽位独立尝试并持久化。某一槽位失败不会中断后续槽位；只要有成功图片，任务保持可选择/可应用，全部失败才是 `failed`。
- 失败信息保留槽位和供应商响应摘要，前端继续展示“部分失败但可使用已生成图”。
- 验证：`backend/tests/test_visual_image_service.py` 8 passed；Python 编译通过。未触发真实付费重跑。

## 2026-08-17：Ozon 导入报错自动反馈与修正重提

- 后台监控导入任务；出现 Ozon SKU 字段错误时持久化为 `import_failed`、停止库存流程并写入 `listing_import_failed` 审计。
- 编辑器提交成功后自动重载草稿并显示已持久化问题；已提交草稿提供“修改后重新提交”，确认后创建新的 Ozon task_id。
- 同步反馈优先使用 `ozon_product_id`，无 ID 时用 `import_task_id` 获取导入错误；真实草稿 15 已解析 Ozon product_id `5989648461` 与 `INCORRECT_DENSITY` 错误。
- 验证：`tests/test_listing_service.py tests/test_listing_stock_monitor.py` 共 15 passed，前端脚本语法检查通过。

## 2026-08-17：人工尺重修正与 SKU 提交值联动

- 右侧“人工尺重修正”保存时除更新货源快照外，同事务覆盖当前店铺关联草稿的所有 SKU 长、宽、高和重量，并记录 `listing_package_corrected` 审计。
- 编辑器保存后从草稿重新读取变体值，避免仅在前端内存临时修改、刷新后回退旧尺重。
- 验证：隔离测试覆盖货源、关联草稿和 SKU 四个尺重字段的同步；相关测试共 16 passed。

## 2026-08-17：图片翻译切回象寄

- 编辑器的“翻译图片”已切回象寄，目标语言使用 `RUS`。
- 后端返回 `index/source_url/translated_url/error` 逐图结果，前端按原索引替换，避免多选时部分失败造成图片错位。
- 腾讯云接口保留但不再作为编辑器默认入口；浏览器脚本版本已递增，刷新编辑器会加载新路由。

## 2026-08-17：象寄阿里图片翻译协议修正

- 供应商最新 OpenAPI 只定义单图 URL 接口 `POST ?Action=GetImageTranslate`，字段为 `Url`，成功图片返回 `Data.SslUrl`；旧代码错误调用未在该文档定义的 `GetImageTranslateBatch/Urls`，因此会报参数或空结果错误。
- 编辑器多选体验保持不变：后端按原选择顺序逐张调用官方接口，目标语言仍为 `RUS`，并且不传 `EngineType=5`，继续使用象寄配置的阿里图片翻译通道。
- 每张图会返回象寄的 `Code/Message/RequestId` 错误证据；单图失败不再影响其他图片成功替换。回归测试使用 mock 验证请求协议，不消耗象寄额度。

## 2026-08-17：描述长度与变体即时联动

- KC000018 草稿描述字段确实为空；其货源描述正文为 9415 字符，旧代码错误提交完整采集 JSON，超过描述接口 10000 字符限制。现在只提交货源描述正文并截断为 9000 字符。
- SKU 售价输入与“同首行”复制会立即推导原价/最低价；售价、重量或任一包装尺寸变化后，180ms 防抖刷新全部 SKU 的仓库匹配与库存状态。
# 2026-08-17：AI 套图任务与返回闭环

- 根因复盘：KC000016 在 Terra 分析完成后、Image 2 返回前发生后端重启；旧 `BackgroundTasks` 没有持久运行记录，留下 `generating`，无法判断供应商是否已计费。
- 新增 `attempt_history_json` 和 `current_run_id`。一次点击是一个独立运行批次；每个槽位在准备参考、发送供应商请求、收到 HTTP 响应、落图成功/失败时立即写入账本，接口和编辑页均返回/展示请求数、回包数和运行编号。
- 后端启动时不再恢复或重放任何付费调用：将未完成任务标记 `interrupted`；已发送未完成、或旧版本无账本的任务都显示“扣费无法确认”，须人工查沧猿后台后明确重试。
- 验证：AI 套图专项 11 passed；后端全量 142 passed、3 subtests passed；未调用真实生图。

## 2026-08-17：1688 严选勾选即采集

- 严选页结果操作栏已固定；单个卡片勾选会通过官方详情核验尺重，并在完整时直接写入当前 Ozon 店铺的采集箱。全选本页会按照后端每次最多 20 个 Offer ID 的限制自动分批。
- 尺重不完整、官方调用失败或未返回结果时，卡片显示原因、取消勾选并恢复可重试；已采集留明显状态。
- 店铺全量 API 尚不能实现：当前 `jxhy.product.getPageList` 和 `alibaba.pifatuan.product.detail.list` 未提供供应商稳定 ID 或按店铺列商品能力。不得依据供应商名称做不可靠的全店采集；等待补充官方端点及授权范围。

## 2026-08-17：插件本地 ERP 请求改走后台代理

- 根因：1688/Ozon 内容脚本直接访问 loopback ERP，浏览器私有网络/CORS 策略会在请求未进入后端时抛出笼统的 `Failed to fetch`。
- 修复：内容脚本和弹窗改为 `chrome.runtime.sendMessage -> Manifest V3 background -> 本机 ERP`；后台仅允许 `localhost/127.0.0.1:8000` 的 `/api/` 路径，并把 HTTP 状态和后端 `detail` 原样转为可见错误。
- 清单：两种插件加载根目录都声明 `127.0.0.1:8000` host permission，版本升至 `0.5.2`；重载扩展后再验证 1688 单商品采集。

## 2026-08-17：采集 SKU 与尺重继承修复

- 根因：KC000021（货源 105）采集快照已有 8 个 SKU 及尺重，但编辑器在当前类目无 `is_aspect` 时直接退出变体生成，草稿退化为单个空白 `KC000021` 占位行；变体生成又只使用商品级 `packageInfo`，忽略 SKU 自己的尺重。
- 修复：无变体维度时仍生成所有可售采集 SKU；每行优先使用其 `weightG/lengthMm/widthMm/heightMm`，商品级值只作兜底。加载历史草稿时，只要识别到唯一全空占位行且货源有多个 SKU，就恢复变体，不覆盖人工填写行。
- 验证：真实 KC000021 货源接口返回 8 个 SKU、8 行完整尺重；编辑器页面加载后货源面板显示 8 个变体和 30g/100×100×20mm，控制台无 error。恢复结果需点击普通“保存”后持久化到草稿。

## 2026-08-17：旧单行占位草稿恢复全部货源 SKU

- 旧版本的多 SKU 草稿可能只有一条 `seller_sku=Parent Offer ID` 的错误占位行；即使该行后来带上了图片、库存、尺重或自动价格，旧恢复条件仍会误判为人工变体，导致右侧显示 8 个货源 SKU、表格只显示 1 行。
- 现在只要货源有多个 SKU 且草稿唯一行仍是 Parent Offer ID，就恢复为全部可售货源 SKU。已有通用人工字段（条码、售价、原价、最低价、成本、库存、仓库、俄文名称）复制到新行；各 SKU 的采购价、图片、尺重始终使用采集快照。
- 恢复只在加载旧草稿时发生，真正拥有独立 SKU 后缀的人工行不会被自动覆盖；恢复后仍须点击“保存”持久化。
- 已对历史草稿 `KC000021`（店铺 1、草稿 21、货源 105）执行一次受限修复并写入 `legacy_source_variant_restore` 审计：单条父 SKU 占位已替换为 8 条可售货源 SKU。回读草稿接口确认 `variant_count=8`；未调用 Ozon。

## 2026-08-17：预检先保存当前编辑值

- 旧预检只在“尚未创建草稿”时保存；已存在草稿的运营人员即使刚选类目、填写 SKU 价格和尺重，预检仍读取数据库中的旧值，造成页面已填完整却报“未选类目/尺重不完整”。
- 预检现在统一执行“保存当前页面 -> 调用后端校验”；保存失败时停止校验并保留明确错误，不会把旧草稿的结果当作当前表单问题。

## 2026-08-17：提交作为唯一预检入口

- 编辑器移除独立“预检”按钮、样式、事件和函数，避免已提交草稿因单独校验被改写本地状态，或运营将旧草稿预检结果误当成当前表单结果。
- “保存并提交”固定先保存当前页面；后端 `/submit` 随后调用 `validate_listing_draft`。校验失败直接返回问题且不调用 Ozon，校验通过后才继续图片公开化和 Ozon 导入。
# 2026-08-17：采集箱快速审核侧栏

- 采集箱每行新增“快速审核”，不再要求运营逐个进入完整编辑器才能确认基础数据。
- 侧栏集中显示并处理尺重/价格准备、图片状态、SKU 状态和 Ozon 内容质检；尺重保存复用 `/pipeline/source-products/{id}/package`，继续联动全部草稿变体和仓库匹配，不复制定价公式。
- 明确可修复的 Ozon 问题可在侧栏调用既有 `auto-fix`；图片翻译、AI 套图、SKU 事实和类目不确定项保留“进入完整编辑器”入口。
- 后端采集箱响应新增 `review_summary`，只返回已有证据和缺失状态，不用默认值伪造人工确认结果。
- 验证：`node --check frontend/app.js`、`python -m py_compile backend/app/main.py`、后端 `/health` 正常；真实浏览器页面 105 行商品均显示“快速审核”，点击首行后侧栏正确展示商品、尺重价格、图片、SKU、质检四组内容，控制台无 error/warn。未点击保存按钮，未修改商品数据。

## 2026-08-17：快速审核图片工作区

- 快速审核侧栏左列展示较大图片卡片；点击图片打开原图细节预览，勾选后可调用象寄图片翻译并把结果回填到当前草稿图库。
- 图片翻译只允许已有草稿直接回填；未建草稿的商品引导进入完整编辑器，避免把翻译结果写入无归属记录。
- “生成 AI 套图（应用后置顶）”复用现有付费任务和人工确认门禁；应用套图时后端与编辑器都将所选图片置于图库最前，第一张作为主图。
- 验证：浏览器实际显示左侧图片墙、5 张图片、翻译按钮和 AI 置顶按钮；点击首图打开原图弹窗，控制台无 error/warn。未调用翻译或生图接口，未产生付费或商品写入。

## 2026-08-17：采集箱删除不复活

- 根因：删除接口过去只删除 `source_product_shops` 关联；插件再次上报同一 1688 Offer ID 时，幂等采集逻辑会重新建立关联，导致商品“删除后又回来”。
- 修复：店铺关联新增 `is_deleted` 软删除标记；采集箱、货源详情、尺重更新、AI 图片和管线查询统一过滤已删除关联。插件重复上报只更新证据，不恢复本店隐藏项；其他店铺不受影响。
- 已将用户指出的 source product `37`、`28`、`11` 标记删除，采集箱从 105 条变为 102 条。验证数据库标记为 `is_deleted=1`，后端 `/health` 正常，管线回归测试 34 项通过。
# 2026-08-17 — 快速审核图片与尺重可读性

- **变更**：快速审核侧栏改为最大 1160px，图片栏 300px，尺重四列输入每列至少 110px；商品编辑器中间表单最大宽度提高至 1180px，右侧来源栏收至 280px，SKU 数字字段至少 82px。
- **图片状态规则**：快速审核有草稿时必须以草稿 `images` 作为图片墙的唯一展示来源；翻译回填和单张删除都只更新当前草稿图库，成功后立即重渲染该图库，禁止再次以采集源图覆盖可见状态。
- **验证**：Browser 打开采集箱首条快速审核，侧栏实测 1160px、四个尺重输入各 177px 且 `scrollWidth == clientWidth`，并确认每张图片都有删除按钮；编辑器实测 SKU 数值输入至少 80px。
- **补充修复**：快速审核原图弹窗独立于采集箱列表弹窗，必须同样采用固定大容器和 `overflow:hidden`；图片卡不得使用含删除按钮的 `<label>`，否则浏览器点击复选框可能不切换。现已改为语义化卡片，勾选后按钮明确显示“翻译所选图片（N 张）”。
- **补充修复 2026-08-18**：尺重保存接口现在直接按统一 `PricingService` 计算并持久化售价、原价、最低价（本地实测 KC000021：26.99 / 53.98 / 26）。象寄新版正式 POST 地址为 `https://api.tosoiot.com/`，旧地址 `api.xiangjifanyi.com` 会导致网络错误；已切换并用真实图片验证返回 `Code=200` 和 `SslUrl`。

- **补充修复 2026-08-18：采集箱初始化中断**：主页面曾直接给不存在的 `#add-listing` 绑定事件，导致 app.js 在加载阶段抛出 `Cannot read properties of null (reading 'addEventListener')`，后续采集箱导航和数据加载均未执行。现在可选控件先判空，采集箱加载器通过 `window.loadCollectionBox` 暴露给共享视图激活路径，并将脚本版本升至 `app.js?v=72` 以避免旧缓存继续执行旧代码。
- **验证**：`node --check frontend/app.js`、`quick-review.js`、`qr-injector.js` 通过；前端 `http://127.0.0.1:5500/index.html` 和后端 `/health` 均返回 200；强制重新加载后点击“采集箱”显示 104 条商品，点击首行“快速审核”弹窗打开，分类匹配、图片、尺重、SKU、质检区域均可见，浏览器应用控制台无 error/warn。未触发提交、翻译或生图写操作。

- **回归修复 2026-08-18：采集箱字段恢复**：快速审核注入期间 `renderCollectionBox()` 曾退化为 6 列，覆盖了页面原有 10 列表头，导致 KC/Offer 编号、货源 ID、采集/发布标识和提交结果不可见。现已按现有表头恢复逐行复选框、编码、状态、重复发布标识、Ozon 评分/问题/库存结果，并保留快速审核、编辑和提交入口；选择行后“刷新所选结果/删除所选”会正确启用。
- **验证**：强制重新加载后采集箱显示 107 行；首行显示 `KC000110`、货源 ID、状态“未编辑”、采集标识“已采集”和操作按钮；勾选首行后两个批量按钮均启用；浏览器应用控制台无 error/warn。

- **反馈审计 2026-08-18**：评分与 Ozon 问题不再只依赖当前草稿字段。`sync-feedback` 每次调用都会以 `shop_id + KC/Offer ID` 写入追加式审计快照；快照保存 Ozon 商品 ID、评分、问题、审核/校验状态、反馈分组和同步时间。服务启动时已将 15 条现有草稿反馈补入历史；采集箱按当前 KC 返回 `feedback_history`、`feedback_count`，即使未来草稿重建仍可回显历史证据。
- **验证**：本机接口回读 `KC000021`：评分 `82`、问题 `9` 条、历史 `1` 次；浏览器采集箱同一行显示“9 项问题 / 82/100 · 反馈 1 次”，控制台无 error/warn。未触发新的 Ozon 同步或提交。


---

## 2026-08-18 采集箱严重回归复盘

### 事故
修复"采集箱打不开"和快速审核模块时，动了 `renderCollectionBox` 核心渲染函数，同时新增全局按钮绑定时没有验证原有功能，导致：
- 表头全选框丢失
- 刷新所选 / 刷新全部 / 删除所选 三个按钮完全没绑定事件（DOM 还在，点了没反应）
- 列数从 10 列退化为 6 列，KC 编号、采集/发布标识、提交结果列全部丢失
- 商品图片点击放大功能被删
- 删除操作绑定了店铺筛选判断，全局删除实现后前端没去掉，导致"请先选择店铺"
- 快速审核分类匹配候选为空（关键词词典不足 + ensure-draft 调用的老数据）

### 根因
1. 改动核心视图（采集箱 render）时用整体重写替代增量注入，破坏了原有稳定结构。
2. 违反"禁止未验证就声称已修复"规则：几次回复"已修复"前都只做了语法检查，没有在真实浏览器走完整交互。
3. 跨模块改动（快速审核 → 采集箱 → 全局删除/刷新）没有跑回归清单。

### 修复
- 恢复 10 列完整渲染
- 补全选框 + 三个按钮事件绑定（全局删除 / 全局刷新）
- 新增点击图片放大（全局事件委托，不侵入 render）
- 扩充分类匹配关键词词典 + fallback 双字滑窗，保证候选不为空
- ensure-draft 返回 variants，快速审核展示 SKU 列表
- 编辑器 variant_values_json 坏数据修复（13 条 Python 单引号格式 → 标准 JSON）
- 编辑器 api() 函数加 JSON parse 兜底，显示响应原文而不是报 position N 错误

### 新规则
- AGENTS.md 新增**规则 23：采集箱是核心稳定面，任何改动必须跑回归清单**（12 项）。
- 以后任何采集箱相关改动交付前，必须对照清单逐项验证并列出证据。
# 2026-08-19 Ozon 全局元数据缓存

- Ozon 类目、产品属性、字典值属于平台全局数据，不按店铺隔离读取。
- 读取端统一使用 `ozon_global_category_cache`、`ozon_global_attribute_cache`、`ozon_global_dict_values`。
- 店铺表只保留同步兼容快照；类目同步时同时写入全局表，避免新类目只落在单个店铺。
- 已验证现有数据覆盖：7422 个类目、1531 条属性、69818 条字典值，均等于所有店铺去重后的集合。
- 验证命令：`python -m py_compile backend/app/sync_service.py backend/app/listing_metadata_service.py backend/app/main.py`、`node --check frontend/listing-editor.js`。

# 2026-08-19 1688 视频采集链路

- 根因：扩展已解析 `payload.video`，但 `pipeline/extension_bridge.py` 未将其转换为 `source_media`，因此数据库、采集箱和编辑器均无视频可显示。
- 修复：视频 URL 以 `media_type="video"` 写入货源媒体；采集箱标题旁显示“视频”链接；新建草稿自动继承视频链接；编辑器视频字段可保存并回显。
- Ozon 导入不发送第三方 1688 视频 URL，Ozon 商品视频需走平台自身视频资源流程，不能把外部 URL 塞进商品导入 payload。
- 历史采集记录没有视频证据，需从插件重新采集该商品后才会显示。

# 2026-08-19 Ozon 类目/类型提交门禁

- 事故：`SRC107` 的 Ozon 任务 `5457781238` 确认已真实调用 `/v3/product/import`。Ozon 接受黑色 SKU、拒绝米色 SKU，错误为 `description_category_has_no_description_type`；根因是提交瞬间使用了不属于同一 Ozon 树分支的类目/类型组合，之后在编辑器中改正不会回写旧任务。
- 修复：草稿预检与提交共用校验，若全局类目快照存在但 `(category_id, type_id)` 不在其中，直接阻止提交；属性 `8229`（类型）的字典值必须与顶层 `type_id` 完全一致。每次 `listing_submitted` 审计额外记录每个 SKU 实际发送的 `description_category_id/type_id` 快照；SKU 属性改为深拷贝，禁止变体间共享可变 payload。
- 验证：`pytest tests/test_listing_service.py -q -k "cross_category_type or global_cache or accepts_multiple_dictionary_values or rejects_optional_dictionary_text"` 为 `4 passed`；`py_compile backend/app/main.py backend/app/listing_service.py` 通过；重启后 `/health`、前端首页和 `POST /api/v1/shops/4/listing-drafts/24/validate` 均返回 200，当前 `SRC107` 草稿无本地预检问题。

# 2026-08-19 候选图片预览与官方媒体补采

- 候选商品缩略图只能在 ERP 内打开大图预览，不得使用 `target=_blank` 跳往 1688 或其他页面；预览层必须支持点击遮罩空白处或 `Esc` 关闭。
- 采集货源没有全局“五张图片”上限。编辑器显示的是已入库的 `source_media` 数量；最近 100 条本地证据中虽有 87 条为 5 张，但也存在 6、8、10、11、15、26 张，说明旧批次的实际入库数据不足，不能用 UI 限制解释。
- “从 1688 官方详情补采价格/图片/视频”只追加数据库中缺失的官方图片 URL，绝不删除、重排或覆盖已有货源媒体和人工草稿图库；接口返回 `refreshed_media` 以便界面提示新增数量。
- 验证：`node --check frontend/app.js frontend/listing-editor.js`、`python -m py_compile backend/app/main.py` 通过；前端和后端 `/health` 均返回 200。真实候选页点击缩略图后 URL 保持 `#candidate-pool`、`#image-modal` 显示为 `grid`，点击遮罩后为 `none`，浏览器 console 无 error/warn。

# 2026-08-19 字典属性不得伪填文字

- Ozon 下拉/字典型属性只能保存当前类目菜单返回的 `value_id + value`；AI、自动填充、可信记忆或历史草稿中的纯文字均不是有效选择。
- 自动填充的 AI 结果现在必须在本次提供给模型的菜单选项中按 `value_id` 命中，且一律采用菜单的规范值；模型返回文字、缺少 ID、或返回不属于该菜单的 ID 时，属性保持空白并标记人工选择。
- 编辑器在渲染时会清除字典型的无 ID 旧文字；单项 AI、AI 填充全部和材料匹配也只应用真实菜单项。保存时再次排除任何无 ID 的字典文本，避免无意义写入草稿。
- 验证：`pytest -q backend/tests/test_auto_fill_service.py` 为 `2 passed`，覆盖“AI 返回锅但无菜单 ID 必须留空”和“AI 返回菜单 ID 后使用规范菜单值”；`node --check frontend/listing-editor.js` 与 `py_compile backend/app/auto_fill_service.py backend/app/main.py` 通过。真实编辑器 `shop=4, sp=107, draft=24` 检查单选字典字段，所有显示文字均带菜单 ID，`invalidDictionaryTexts=[]`，控制台无 error/warn。

# 2026-08-19 手动类目与重复采集图库合并

- 手动从类目下拉菜单选择后，立即清空“可信记忆/自动匹配建议”；同时使正在返回的旧自动匹配请求失效，避免异步结果把建议重新显示或覆盖手动选择。
- 1688 重复采集不再删除旧 `source_media`。新快照只追加此前没有的图片/视频 URL，SKU 与价格证据继续按最新快照更新；这样二次采集只拿到当前可见五张时，不会把历史图库抹掉。
- 扩展图片采集增加 `data-src`、`srcset`、页面脚本中的 CDN 图片 URL，并继续过滤缩略图、Logo 和无效资源；单次上限仍为 80 张。
- 验证：隔离数据库重复采集 `a,b` 后再次采集 `b,c`，最终媒体为 `a,b,c`；`node --check` 扩展与编辑器脚本、Python 编译均通过。官方详情补采接口对 `1029759417495` 实测返回 5 张官方图，故不能把官方接口本身声称为全图库来源。
- 扩展采集回执现在同时返回“本次识别图片数”和“ERP 合并后图库总数”；重复采集时可直接判断是页面提取少，还是历史图库已合并保留。

## 2026-08-20：已发布草稿的货源素材回填

- 新增 `POST /api/v1/shops/{shop_id}/listing-drafts/{draft_id}/refresh-source-materials`：仅在运营明确点击时，把同一 1688 货源快照中新增加的图库图片合并到既有草稿，并按源 SKU 更新对应 SKU 图；缺少视频时补入视频。
- 此操作只写本地草稿和审计 `listing_source_materials_applied`，绝不创建新草稿、绝不创建 Ozon 商品、绝不调用 Ozon 写接口。运营确认回填结果后，才可自行点击“保存并提交 Ozon”。
- 验证要求：重复调用不产生重复图库 URL；SKU 图只匹配同一源 SKU；返回新增图片数、SKU 图更新数和视频补入状态。

# 2026-08-19 XY000001 库存匹配与反馈文案

- `XY000001`（货源 `1029759417495`）的 Ozon 反馈为内容质量评分条件：商品状态 `approved`、校验 `success`，并非 10 条导入错误；采集箱现在区分显示“Ozon 错误”和“内容建议”，同时展示库存任务状态及原因。
- 库存任务已确认卡在本地仓库匹配：`extra small ECONOMING` 中间有空格，而旧逻辑按 `extrasmall` 连续文本匹配，导致 23 次重试后仍未调用 `/v2/products/stocks`。仓库名匹配现在统一去除空格和标点，适用于全部店铺和价格档位。
- 验证：新增带空格 `extra small ECONOMING` 的匹配回归测试；需重启后端后检查 XY000001 的库存任务进入写入并回读确认流程。

# 2026-08-20 官方详情补采无反馈

- 根因：编辑器仍加载旧的 `listing-editor.js?v=55`，且补采接口只在 toast 中返回结果；当官方接口新增图片为 0 时，运营看起来像按钮没有响应。
- 修复：资源版本更新为 `listing-editor.js?v=56`、`listing-editor.css?v=24`；接口返回 `official_image_count`，来源面板固定显示官方图片数、新增图片数、视频/价格补采结果及当前图库总数。
- 验证：重启 8000 端口后，`POST /api/v1/shops/2/pipeline/source-products/114/refresh-official-detail` 返回 HTTP 200 和 `official_image_count=5`；真实编辑器点击按钮显示“官方详情返回 5 张图片，新增 0 张，视频无新增，补回 0 个价格；当前图库 5 张”，控制台无错误。

# 2026-08-20 首页空 Hash 路由

- 根因：`viewFromHash()` 在地址没有 hash 时，将空字符串拼接进 `.view#${CSS.escape(view)}`，产生无效的 `.view#` 选择器，首页脚本初始化直接中断。
- 修复：`viewFromHash()` 对空 hash 直接返回 `dashboard`；`setView()` 同样将空视图名归一化为 `dashboard`，避免其它调用路径重复触发。
- 验证：`node --check frontend/app.js` 通过；浏览器从 `http://127.0.0.1:5500/` 空 hash 打开首页，加载 `app.js?v=87` 后无新选择器错误；点击“店铺管理”正常进入 `#shops` 并渲染店铺列表。

# 2026-08-20 编辑器字典、SKU 媒体和视频预览

- 单选字典输入增加请求序号，旧搜索响应不会再覆盖当前正在输入的内容；输入只是搜索，只有点击当前 Ozon 菜单项才保存 `value_id`。
- 1688 采集器会把 SKU 独立图加入图片候选；后端也会把每个变体的 `image_url` 合并到 `source_media`，避免二次采集后图库仍只保留五张主图。扩展版本升至 `0.5.3`，必须在 Chrome 扩展管理页重新加载后才会使用。
- `media.woxq.cn` 对浏览器播放器请求会返回 490。编辑器预览改为通过受限本地解析端点取得短期 CDN 跳转；该端点只接受已采集的淘宝视频代理地址。实测返回 HTTP 302 到可播放 MP4 CDN 地址。
- Image 2 运行 `5f214534` 的五个槽位均收到供应商 `No available image quota` 或 `System is overloaded` 502。该状态保留请求账本，不会自动重发或重复扣费。

# 2026-08-20 1688 官方详情的长详情图与伪 SKU 图

- 根因：1688 官方详情的 `images` 仅为主图库；商品 `description` HTML 还包含长详情图。旧补采只读取前者，因此 `XY000006`（货源 `967310141714`）只获得 5 张图，遗漏 15 张详情图。
- 修复：`detail_to_capture()` 现在从详情 HTML 的 `img[src]` 提取、规范化并去重 `detailImages`；官方补采将主图库和详情图库一并增量写入 `source_media`，随后“回填最新货源素材”合并到同一草稿。SKU 专属图仍保持独立，不会混入公共图库。
- 防御：字符串 `None`、`null`、`undefined` 不再被当作 SKU 图片 URL；补采和草稿回填会清空这类旧占位值，禁止污染 Ozon 图片 payload。
- 验证：官方详情解析为主图 5 张 + 详情图 15 张；`POST /api/v1/shops/2/pipeline/source-products/119/refresh-official-detail` 为草稿 `35` 增加 15 张，随后草稿图片总数为 20、5 个 SKU 图均为 `null`。官方接口、原采集记录及网页直接响应均未返回视频 URL，因此未虚构视频，也未提交 Ozon。

# 2026-08-20 草稿二次打开必须以保存值为准

- 根因：`normalizeLoadedVariantValues()` 遗留了不在函数作用域内的 `loadToken/categoryId/typeId` 判断，草稿恢复每行变体值时抛错并被静默吞掉；已保存的“商品颜色/颜色名称”遂在第二次编辑时显示为空。
- 修复：移除失效的异步请求判断；“颜色名称/Название цвета/Color name”不再被误判为字典颜色维度，首次从 1688 创建变体时会继承采集 SKU 名称，已保存值则原样恢复。编辑器资源升至 `listing-editor.js?v=70`。
- 视频删除：`PUT listing-drafts` 使用 Pydantic `model_fields_set` 区分“未传 video_url”和“明确传 null”；后者会清空草稿视频，下一次打开不再从旧草稿复活。来源视频只在新建草稿/运营明确补采时填写，绝不覆盖草稿删除。
- 验证：草稿 `34` 数据库保留三条颜色和完整俄文颜色名称，`video_url=null`；真实编辑器重新打开后颜色为蔚蓝色/绿色/天蓝色，颜色名称三条均显示，视频输入框为空，控制台无 error/warn。

# 2026-08-20 新采集 SKU 名称的延迟维度回填

- 根因：1688 货源 SKU 行会先于 Ozon 类目变体维度创建；随后异步加载“颜色名称”维度时，旧逻辑只重绘表格而不补充已创建行的空字段。部分扩展快照还使用 Python 风格的单引号字典串，不能只用 `JSON.parse` 读取。
- 修复：统一解析标准 JSON 与 Python 风格 SKU 规格串；每次变体维度到达后，按 `seller_sku → source_sku` 回找来源，只回填当前为空的维度。颜色名称继承完整采集 SKU 名称；字典颜色仍保持空白，等待 Ozon 菜单匹配。
- 保护：已保存的人工颜色、名称及其它变体值不覆盖，适用于所有货源而非单一 XY 编号。
- 验证：真实编辑器打开货源 `121`，8 行“颜色名称”显示来源名称；加载 `listing-editor.js?v=72`，浏览器无新错误。

# 2026-08-21 AI 套图应用必须自动持久化

- 根因：“全选”只更新前端选择集；新商品没有草稿时，“使用所选套图”也只写浏览器内存，刷新或离开页面即丢失。自动创建草稿后地址栏又未绑定 `draft`，刷新仍可能重新进入未保存货源视图。
- 修复：“使用所选套图”现在是明确的持久化动作。没有草稿时先保存完整编辑表单并创建草稿，再调用既有 AI 套图应用接口；创建成功后立即用 `history.replaceState` 把 `draft` ID 写入当前地址。
- 验证：货源 `122` 的 AI 任务 `10` 全部 5 张已应用到草稿 `36`，状态为 `applied`，图库保存 23 张、前 5 张为本次 AI 图、SKU 主图为 hero；使用 `listing-editor.js?v=74` 从 `draft=36` 重开后仍显示“已应用到草稿”、5 张全选且无控制台错误。

# 2026-08-21 AI 套图逐槽位状态与卡死恢复

- 套图面板固定展示销售首图、尺寸规格、结构细节、使用步骤、场景用途5个槽位；每槽显示准备参考图、等待供应商、已回包保存中、成功、失败、中断扣费待确认或未开始未发送。
- 整体状态必须给运营明确建议：正常运行时等待；回包后保存超过3分钟提示可能卡住且禁止重复点击；部分成功/失败或中断时允许使用已有图片；中断后只有实际发送过的槽位提示扣费待确认，未开始槽位明确未发送。
- 供应商结果图片下载使用30秒空闲读取超时、150秒整体时限和30MB大小上限；单槽超时只记该槽失败并继续后续槽位，不再无限拖住整批任务。
- `interrupted` 且已有生成图的任务允许显式应用已有图片，但绝不自动重试或补发。
- 验证：`XY000008` 任务 `11` 重启后为 interrupted；页面加载 `listing-editor.js?v=77`，5槽依次显示“成功 / 失败 / 中断扣费待确认 / 未开始未发送 / 未开始未发送”，“使用所选套图”可用，控制台无错误；后端视觉任务测试12项通过。

## 2026-08-21 生图批次隔离与旧成功图片保留

- 页面“供应商请求/已回包”只统计当前 `run_id`，历史批次仍保留在账本但不得累计到本次数字。Terra 分析阶段失败且没有 Image 2 attempt 时，必须明确显示“本次请求0、回包0、未生图”。
- 开启新批次不得清空旧批次已成功的图片。旧图在同槽位新图成功前继续可见、可选择；新图成功后才按槽位替换旧图。Terra 或后续槽位失败不会清除已有付费成果。
- `XY000008` 运行 `cff65c91` 实际为 Terra HTTP 504，当前批次没有 Image 2 请求；此前运行的 hero 文件已从本机恢复到任务 `11`，页面显示旧首图1张可用，其余4槽“未开始·未发送”。
- 验证：编辑器 `v79` 显示“分析失败·本次未生图”“供应商请求0、已回包0”，旧首图可选择应用且控制台无错误；后端视觉任务测试13项通过。

## 2026-08-21 变体图片角标与弹窗同源计数

- 变体产品图的数量统一定义为“当前 SKU 专属首图 + 全部公共产品图，按 URL 去重”。角标和弹窗必须调用同一个列表函数，禁止角标额外加1而弹窗只展示公共图。
- 弹窗第一张明确标记“SKU首图”并固定；其余标记“公共图”并允许排序。删除 SKU 首图只清空当前变体图片，删除公共图才修改共享图库。
- 验证：`XY000008` 两行角标均为6；第一行弹窗实际渲染6张，其中1张SKU首图、5张公共图，关闭按钮正常且控制台无错误；编辑器资源为 `v80`。
# 2026-08-21 编辑器来源直达与 AI 五槽位常驻

- “③ 本地信息”的来源 URL 右侧新增“打开原链接”，校验为 HTTP(S) 后在新标签页打开当前输入地址。
- AI Ozon 套图区域从初始状态开始固定展示销售首图、尺寸规格、结构细节、使用步骤、场景用途五个槽位；未运行时显示“未开始”，任务运行后逐槽位覆盖真实状态。
- 真实页面验证：`listing-editor.js?v=81` 已加载，`sp=121` 页面存在 5 个槽位且均显示“未开始”，浏览器控制台无 error/warn；前端与后端健康检查均为 HTTP 200。
# 2026-08-21 AI 生图范围选择与单槽位重做

- AI 生图入口新增“生成全套（5张）/只生成主图”选择；请求通过 `slots` 明确传递目标槽位。
- 五个槽位各自显示成功、失败、中断待确认、等待等状态；失败项可点击“重做此图”，已有成功图也允许人工重做，未开始项可单独生成。
- 后端生成任务支持目标槽位列表，新批次保留其他槽位既有成功图和历史请求账本，不会因单图重做清空整套结果。
- 验证：`test_visual_image_service.py` 14 项通过；OpenAPI 已出现 `GenerateRequest.slots` 五值枚举；XY000008 页面显示 5 个槽位、5 个逐图操作按钮与两个生成范围选项，切换“只生成主图”成功，控制台无 ERP 应用错误。验收未点击实际生图，未产生付费调用。
# 2026-08-21 商品编辑器左侧工作队列

- 编辑页改为三栏工作台：左侧商品列表独立滚动，中间保留完整编辑器，右侧保留采集素材证据。
- `GET /api/v1/shops/{shop_id}/pipeline/source-products` 现在归并最新草稿，返回 `draft_id`、`offer_id`、`draft_status`、`editor_status`。
- 状态规则：无草稿=未编辑；已有草稿但未提交=已编辑；草稿已提交、已有 task_id 或 Ozon product_id=已发布。
- 点击左侧商品会在同一编辑器切换；已有草稿自动携带 `draft`，不会重建；当前商品高亮。人工改动未保存时切换会先确认。
- 真实店铺 2 验证：12 个商品，7 个已发布、5 个未编辑；从 XY000008 点击 XY000007 后 URL 正确切换为 `sp=122&draft=36`，高亮同步，控制台无错误。当前真实数据暂时没有“已编辑”记录，图例和规则已实现。
# 2026-08-21 产品图库大图连续浏览

- 产品图片放大层改为可连续浏览的原图查看器，左右按钮循环切换全部公共产品图，并显示 `当前位置 / 总数`。
- 支持键盘左右方向键、Esc、右上角关闭、底部关闭和点击遮罩关闭；不会影响缩略图选择、删除、主图和翻译逻辑。
- 真实草稿 34 验证：5 张图库从 `1 / 5` 点击右箭头切到 `2 / 5`，图片 URL 确实改变；左箭头返回 `1 / 5`，关闭后 dialog 消失，控制台无错误。
# 2026-08-21 商品列表滚动定位与草稿加载竞争修复

- 左侧商品列表按店铺把 `scrollTop` 保存到会话；列表刷新、商品切换和页面重载后恢复原位置，若当前项仍在视野外则按最近位置滚入视野。
- 96 条商品真实验证：从列表前部点击第 68 条 KC000010，切换到 `sp=35&draft=10` 后当前项仍在中段可见；再次点击刷新仍保持 KC000010 高亮和位置。
- 验收同时发现已有草稿的 Offer ID 会被未等待的“下一个编号”请求覆盖；现已让店铺初始化等待队列/模板加载，并且 URL 有 `draft` 时不再生成新编号。
- 历史草稿的 Ozon 评分存在 `77.5` 等半分值，响应模型误设为整数会导致草稿接口 HTTP 500；已改为浮点评分，使左侧点击能加载对应历史草稿。
# 2026-08-24 批次样板与类目记忆修正

- 修正类目树类型叶节点丢失父级 Category ID：后端递归携带最近父类目；前端对历史 `Type ID` 有值但 Category ID 为空的选择，使用全局类目缓存唯一反查补全。
- “确认正确并记住”在操作区域显示保存中、记录 ID 或明确错误，不再静默拦截。
- 候选池新增“设为批次样板”“批量套用样板生成草稿”“仅查看异常商品”。样板草稿必须有完整 Category ID/Type ID；只继承运营勾选的学习属性，其余图片、SKU、尺重、采购价、内容和定价逐商品重建。
- 新接口：`POST /api/v1/automation/candidate-batches/apply-sample`。最多处理 100 个候选，只生成本地草稿，不审批、不发布；逐项返回 `draft_ready` 或 `needs_review` 并写审计。
- 验证：前后端语法检查通过；`/health` 正常；OpenAPI 已包含批次接口；Type `97229` 唯一补全到 Category `200001425`；真实候选池渲染批次控件，选择一项后按钮状态正确且控制台无错误。
- 候选池增加绿/黄/红审核等级、逐项证据摘要、等级筛选及“选择本页绿色”。绿色判定要求类目、SKU、价格、尺重、图片和质检六类硬证据全部通过；真实第一页30条均因证据不完整保持黄色，未发生放宽门禁误判。
# 2026-08-24：店小秘 1688 店铺清单采集静态分析

- 已静态解包并分析用户提供的 `dxm_crawl_2.8.62.crx`，结论见 `docs/1688_SHOP_CATALOG_COLLECTION_RESEARCH.zh-CN.md`。
- 核心机制是利用当前 1688 登录会话调用 MTop 店铺商品列表，每页 30 条读取 `offerCount` 和 `offerList[].id`，再进入限并发详情队列；不是开放平台一次返回整店完整商品。
- 后续若实现，必须采用“公开 Offer ID 清单 → 持久化断点队列 → 严选官方 API / 非严选低并发详情补采”的分层方式，并在验证码或风控时暂停等待人工。
- 已实现扩展 `0.6.0` 的第一阶段店铺清单扫描：`injected.js` 通过页面 MTop 客户端按30条静默分页，`content.js` 持久化断点和去重进度，后端 `/api/1688/shop-scan/check` 只读核对 ERP 已有 Offer ID。模拟两页分页及真实后端去重接口已验证；真实1688登录页仍需运营重新加载扩展后验收。
- `0.6.1` 修复入口可发现性：扩展弹窗和1688右侧浮窗均显示“扫描全店商品”；弹窗使用版本化 PING 检测旧标签页脚本，并在需要时清理旧浮窗、重新注入当前版本。语法检查及入口检索已通过。
- `0.6.3` 将全店扫描改为扫描与详情采集同步：每页30个 Offer ID 持久化到 `AutomationCandidateRecord` 并标记 `queued_detail`，插件后台worker连续领取详情、成功后经既有 `ingest_capture` 入采集箱。历史保存在扩展本地的扫描结果会按100条补写队列，接口按运行批次+Offer ID幂等去重。隔离SQLite测试通过“首次2条入队、重复页0条新增、worker领取下一条”；真实后端健康和路由加载已验证。
- 2026-08-24 修正安全边界：普通全店 `queued_detail` 队列继续暂停，只有人工明确建立的有限试采批次使用 `test_queued_detail`，worker 领取前原子改为 `test_running_detail`，避免重复开详情页和意外放开整店。
- 真实店铺 `shop1447848357646.1688.com` 的 3076 个去重 Offer ID 中抽取前 20 条试采：20/20 成功入采集箱，无验证码/风控；共解析 79 个 SKU，20 条全部有 SKU 价格和 SKU 图，462 张商品图，15 条有视频，20 条有属性，只有 5 条带完整尺重。该结果说明网页详情补采可行，但尺重仍是主要人工证据缺口，不能据此自动铺货。
- 同店铺后续采用“100 条真实门禁 + 2956 条冻结续采”机制：100 条必须全部 `collected` 才允许 worker 领取 `full_hold_detail`；任一失败、中断或 `waiting_human` 都不会放行剩余队列。当前任务仅采集入库，不生成草稿、不调用 Ozon。
- 扩展 `0.6.4` 将详情 worker 改为两个独立低并发槽位；每个槽位回传后用独立 one-shot alarm 立即领取下一条，30 秒 alarm 仅作掉线兜底。两个槽位共享人工验证暂停开关，任何验证码都会阻止继续领取。

# 2026-08-25：采集箱来源店铺与分页

- 1688 店铺批次的 3076 表示 3076 个去重商品链接/Offer ID，不是 SKU；这些商品合计解析出 12928 条 SKU。
- 货源记录新增 `source_shop_name`、`source_shop_key`，本批次 3076 条已回填为“义乌市卓博饰品厂”与 `shop1447848357646.1688.com`，后续采集由快照自动继承。
- 采集箱新增独立的“来源店铺”和“Ozon 店铺”筛选，并改为服务端分页（50/100/200 条每页）；不再受旧的 500 条查询上限影响。“刷新全部”也按当前筛选逐页读取全部数据。
- 真实验收：筛选“义乌市卓博饰品厂 + xymall”返回 3076 个商品，每页 100 个、共 31 页；第 1 页切到第 2 页后首条商品发生变化。原 10 列表格、全选、刷新所选、刷新全部和删除所选控件均保留。

# 2026-08-25：1688详情图漏采与页面图标误采修正

- 根因：扩展旧版把整页脚本/DOM中的 Alicdn URL并入图库，却没有读取 `description.fields.detailUrl`，因此页面图标会误入、真正详情图为空；后端只追加媒体使错误长期残留。
- 扩展升级到 `0.6.5`：结构化主图/SKU图优先；后台读取 `itemcdn.tmall.com/1688offer/*` 的 `offer_details.content`，独立返回详情图；完整采集携带 `mediaComplete`，后端据此替换旧媒体。
- Offer `999680574138` 已用真实详情接口验证并修复：5张主图、2张SKU图、7张详情图，共14张；4张历史页面图标已清理，详情尺寸图已入库。前后端语法检查及 `/health` 均通过。
- 已运行 `backend/scripts/repair_1688_shop_media.py` 修复来源店铺 `shop1447848357646.1688.com` 的全部3076条：兼容新式 `offer_details` JSON、旧式 `var desc` 和 GB18030 编码；3076/3076标记为 `media_repaired`。最终3076条均有图片，共72703张（随后清理67张遗留共享占位图后为72636张）；已知 TFS/GW 页面图标、共享2020占位图和平台视频占位封面剩余0张。失败请求从不覆盖原图库。

# 2026-08-25：通用批量刊登工程第一阶段

- 初版误将当前徽章业务写成品类专用入口，已在用户纠正后重构为通用引擎；以后更换商品只创建任务，不增加按钮或专用表。
- 新增 `bulk_listing_batches`、`bulk_listing_batch_items`，按可选来源范围持久化样板草稿、固定价格尺重库存、SKU/颜色策略、目标Ozon店铺、分配方式、次日续发开关及每商品断点状态。
- 新增 `/api/v1/automation/bulk-listing-batches/sources`、来源预览、批次创建/列表/详情接口；创建任务只建立本地队列，不生成草稿、不调用AI、不提交Ozon。
- 候选池保留唯一“创建批量刊登任务”入口，来源店铺使用动态下拉；当前义乌市卓博饰品厂只是一个可选来源任务。
# 2026-08-25 批量刊登分类与图片规则

- 通用批量刊登任务已取消“样板草稿 ID”前置条件，改为直接选择 Ozon 完整分类对并加载全部分类属性。
- 批量任务创建端校验必填属性；所有非空字典属性必须携带 Ozon 菜单 `value_id`。
- 新增独立规则开关：本地 OCR 排除中文尺重图、保留英文尺重图；保留图片统一翻译为俄文。OCR 只影响任务草稿素材，不删除来源快照。
- 当前只建立本地任务及规则，不自动创建发布草稿、不调用 Ozon 写接口。
## 2026-08-25 批量刊登审批发布闭环

- 批量任务明细新增已准备商品选择、全选本页、清空和“批准并上传 Ozon”。一次人工确认后，前端先调用 `approve-prepared` 建立按店铺分组的本地审批批次，再自动逐店调用提交接口；运营不需要跳转“审批发布”重复确认。
- 审批发布页显示每个店铺的商品创建额度。额度来自官方 `POST /v4/product/info/limit`；失败时回退本地 180/日保守门禁并显示来源。
- 真实提交端点仍为 `/approval-batches/{id}/submit`，要求 `confirmed=true`、单次最多 20 个，并在每次发送前重读额度；未提交部分保持队列。
- 编辑器已允许 `returnTo=bulk-listing`，异常草稿处理后可返回批量任务。
- 验证：Python 编译、前端 Node 语法检查与 `backend.tests.test_ozon_seller` 已通过；浏览器交互验收仍不得点击真实 Ozon 提交确认。

### 质量事故修正

- 发现旧批量草稿只保存标题、描述和变体基础行，未持久化流水线属性、富内容、SKU图和变体颜色；旧发布 payload 又绕过草稿读取流水线旧报价，并把全图集复用于每个 SKU。
- 已修复 `create_listing_draft_from_pipeline`：保存完整属性、简介、`raShowcase + raTextBlock` 富内容、公共图库、SKU独立图片、稳定不碰撞的颜色字典 ID/俄文名称和来源视频策略。
- 已修复 `build_import_payload`：草稿成为唯一来源；每个 SKU 使用草稿价格/尺重/颜色，`images[0]` 为该 SKU 独立图片，公共图随后去重追加。
- 批次 #1 已设置 `paused_quality_audit`，审批接口返回 409。前10个本地草稿已重建并通过完整性校验；已经导入Ozon的商品尚未根据修复后草稿重新提交。
## 2026-08-25 批量刊登改为复用单品链路

- 现有批次、商品队列、店铺分配与断点继续沿用，不另建批量上架器。
- 批量文字生成已直接复用编辑器的标题翻译、描述生成、30个主题标签及富内容服务；主题标签返回数量不是30时不得提交该商品。
- 批量任务新增单一入口“继续处理并提交”：一次确认后，后台逐商品执行生成、草稿持久化、完整性检查和原有 `submit_to_ozon`，单条失败记录后继续；额度不足记录 `waiting_quota`。
- 验证：Python/Node语法通过，`/health` 200，OpenAPI含 `/bulk-listing-batches/{batch_id}/execute`；`confirmed=false` 返回422且批次1仍为4 imported、6 prepared、3066 queued；前端真实页面显示新入口。

## 2026-08-25 1688 整店采集改为发现即采集

- 修复后端详情领取接口仍沿用旧的100条试采门禁的问题。`queued_detail`、`test_queued_detail` 和 `full_hold_detail` 现在均可由扩展 worker 直接领取，扫描页发现 Offer ID 后即可进入详情采集队列。
- 扩展文案统一为“全店采集/扫描并采集中”，不再暗示必须先完成全局扫描；已发现的商品与新扫描商品可并行推进，按 Offer ID 幂等去重并支持断点恢复。
- 验证码、登录、滑块和访问风控仍是唯一需要人工恢复的暂停条件，不得绕过验证或触发 Ozon 写接口。
- 实测 `/api/1688-crawler/extension/next` 返回普通 `queued_detail` 任务，`kind=detail`、`testBatch=false`；当时队列由81条降为78条，后端 `/health` 返回200。

## 2026-08-25：AI 主题标签门禁放宽到 20–30 个

- 修正共享 `generate_product_hashtags`：提示词仍以 30 个为目标，但后处理接受 20–30 个有效俄文标签；去重后最多保留 30 个，少于 20 个才报错。
- 新增 `backend/tests/test_ai_service.py` 覆盖 29 个通过、35 个截断去重、19 个失败。
- 批量批次 #1 中因“实际返回29”失败的商品已重新置为 `queued`；未启动批量执行、未提交 Ozon。
## 2026-08-25 批量额度与标签门禁修正

- AI 主题标签由“必须正好 30 个”改为去重后 20–30 个可提交，最多保留 30 个；29 个不再阻断。
- 批量引擎将图片数量建议、OCR 失败提示等非结构性提示与类目/属性/草稿完整性错误分开；仅阻断错误进入问题状态。
- Ozon 每日创建额度不足统一进入 `waiting_quota`，保留已生成草稿，第二天点击“继续处理并提交”时只恢复这类商品；额度错误不归入普通失败。
- 调度器轮询 Ozon 导入结果时同步批量商品行的 `imported`/`failed` 状态，真实导入错误保留在商品行。
- 验证：`py_compile` 通过、AI 标签测试 3 项通过、`GET /health` 返回 `{"status":"ok"}`；本次未调用真实批量提交接口。
# 2026-08-26：批量刊登防重复执行与即时进度

- 批量“继续处理并提交”增加持久化 \`running\` 状态门禁和进程内批次锁；同一批次重复点击返回 409，不会再排队第二个后台 worker。
- SQLite 连接增加 30 秒等待时间；批量状态校正遇到短暂锁冲突时回滚并返回可用状态，错误处理中补齐 JSON 响应，避免前端误报 \`failed to fetch\`。
- 批量任务列表返回已处理数量、进度百分比、最近活动时间和当前处理商品；前端每 5 秒轮询运行批次，在任务行显示进度条、当前商品和“后台处理中”提示，并在点击后立即锁定按钮。
- 当前正在运行的批次不直接重启后端；后端代码将在下一次持久化游标后的受控重启中生效。验证：Python/Node 语法检查通过，前端页面 HTTP 200、后端 \`/health\` 正常，浏览器页面显示进度条且无控制台错误。
### 2026-08-26 批量定价事故与长期修复

- 徽章批次的界面固定价为 26/52/25，但历史批次 `rules_json` 缺少 `pricing_mode_system`，后端 `rules.get(..., True)` 隐式启用系统定价，生成 49.42/98.84/49。
- 规则已提升为长期门禁：定价模式必须显式传入；未勾选系统定价使用固定值，缺失字段拒绝创建；worker 每个商品间检查质量暂停。
- 已提交到 Ozon 的价格只记录为待修正范围，不直接篡改数据库或伪造回写；后续需走官方价格更新接口并保留审计。

### 2026-08-26 批量异常支持勾选后批量重试

- 批量刊登明细新增行勾选、表头/工具栏“全选当前筛选”和“批量重试所选”；仅异常状态可勾选，等待队列不会被误选。
- 新增 `POST /api/v1/automation/bulk-listing-batches/{batch_id}/items/batch-retry`，复用既有逐商品处理链路，按选中 ID 入队并逐条回写结果，不创建新批次。
- 质量审计暂停仍是硬门禁：批量重试只放行明确勾选的商品，未勾选的等待商品保持原状态；批次仍显示“需处理”。
- 验证：Python/Node 语法检查、OpenAPI 路由、前端 HTTP 200、后端 `/health` 通过；未执行真实批量重试或 Ozon 提交。

### 2026-08-26 SKU 图与公共详情图严格隔离

- 商品编辑器进入已有草稿时，按草稿保存的 `source_product_id` 自动加载右侧采集素材，不再要求从几千条货源下拉中手选。
- 公共产品图库只包含主图/详情图，最多取 15 张；每个变体的 SKU 图继续保存在 `variant.image_url`，不因公共图库裁剪而删除。
- Ozon payload 生成时再次排除 SKU 图并限制公共图库 15 张；每个 SKU 的 `images[0]` 仍为该变体独立 SKU 图。
- 旧草稿的“回填最新货源图片”也只清理公共图库中的 SKU URL，不修改变体 SKU 图。
- 验证：44 个 SKU 图样本的公共媒体数量为 14/15 且 SKU 图数量为 0；Python/Node 语法检查通过。既有测试套件另有与本改动无关的缓存/授权测试失败，未调用真实 Ozon 写接口。

### 2026-08-26 新版 1688 详情页 SKU 解析与媒体边界修复

- 原因：无忧易售跳转的新版详情页可能不再把 SKU 放在旧的 `skuMapOriginal`，且首次 SKU 接口响应会在普通内容脚本注入前完成；旧逻辑会退化为默认单 SKU。
- 扩展现在在页面最早阶段只读监听 SKU/价格接口，在内容脚本就绪后回传缓存；解析器兼容数组和对象型 `skuMap`、`skuList`、`skuInfos`，再以页面带明确 SKU 数据属性的已渲染行作为最后兜底。
- 公共图库不再追加变体 SKU 图；描述区 DOM 降级严格限制在商品详情描述容器，避免推荐商品、广告和页面图标进入详情图库。
- 验证：`injected.js`、`early-inject.js`、`content.js` 和测试脚本均通过 Node 语法检查；`content-collector.test.js` 覆盖对象型网络 SKU、SKU 图不进入公共图库、详情描述图区限定抓图并通过；扩展清单可解析，`/health` 与前端首页均为 HTTP 200。
- 仍需实际验收：重新加载解压扩展后，在 Offer `1032638315098` 与 `1065276676651` 各重新采集一次；若仍未解析到 SKU，保留网络接口 URL 和响应字段名再补解析路径，不猜字段、不改动现有 3076 条采集记录。

### 2026-08-27 新版 SKU 延迟状态补采

- 真实反馈显示上一版仍可能无 SKU。根因进一步收敛为：采集浮窗只在首次加载时读取一次，而新版详情页会延迟渲染 SKU；同时 SKU 可能位于 `window.context` 之外的初始状态对象。
- 修复：浮窗在未发现 SKU 时自动于 1.2 / 3.2 / 6.5 秒重试；页面脚本从 `context`、`__INIT_DATA__`、`__GLOBAL_DATA__`、`__INITIAL_STATE__`、`__PRELOADED_STATE__` 中有界提取 SKU/规格容器，内容脚本递归识别嵌套 SKU 映射和列表。每个快照最大 500 KB，最多 20 个、5000 节点，不读取或回传整页会话数据。
- 验证：新增嵌套 React 状态 SKU 用例，连同对象型网络 SKU、SKU 图片隔离、详情图区限定抓图均通过 `content-collector.test.js`；三个扩展脚本语法及 manifest 校验通过，后端 `/health` 返回 200。
- 实页验收仍必须在登录后的 Chrome 页面执行；本机无登录会话的直接请求被 1688 风控页拦截，不能以该响应作为 SKU 结构证据，也没有绕过风控。

### 2026-08-27 扩展重载上下文失效防护

- 反馈：在扩展重新加载后，旧页面脚本会报 `Extension context invalidated`，随后 `chrome.runtime.onMessage` 可能为 undefined，造成第二个未捕获异常。
- 修复：内容脚本通过统一 runtime 安全访问和消息包装处理失效上下文；无法再调用扩展 API 时返回明确的“刷新当前 1688 页面”提示，并跳过消息监听注册。页面脚本不再因热重载中断而抛出 `onMessage` 异常。
- 验证：普通 runtime 与空 runtime 两种 VM 加载均通过 `content-collector.test.js`；扩展脚本语法检查及后端 `/health` 均通过。
- 操作规则：重新加载解压扩展后，必须刷新已打开的 1688 标签页。旧内容脚本无法重新绑定到新扩展上下文，这是 Chrome 生命周期限制，不得尝试绕过或继续在旧上下文发请求。
# 2026-08-27：标题与描述切换到 DeepSeek Flash

- 产品标题、描述和流水线翻译共用 `backend/app/llm_provider.py` 的本地提供商配置。
- 默认提供商为 `deepseek`，密钥只从 `D:\Desktop\api\deepseek.txt` 或本机 `DEEPSEEK_API_KEY` 读取；不会写入 `.env`、日志或版本库。
- 已通过 DeepSeek 模型列表查询确认该账户提供 `deepseek-v4-flash`；未返回 `deepseek-v3-flash`，因此默认采用可调用的 v4 Flash，避免按不可用模型名造成全部文案任务失败。
- 火山引擎保留为 `LLM_PROVIDER=volcengine` 的备用配置，使用 `VOLCENGINE_LLM_*`；旧 `LLM_*` 仅作为火山兼容回退，禁止被误发送到 DeepSeek。
- 验证要求：运行 AI 服务和流水线翻译测试；以最小标题请求确认响应元数据为 `method=deepseek`、`model=deepseek-v4-flash`，且密钥不出现在输出中。

### 2026-08-27 云牛顿 1688 OAuth 部署边界

- 云牛顿 AppKey/AppSecret 与原有严选（JXHY）配置完全隔离，存储路径为本机忽略的 `.local-secrets/yunniudun.json`；AppSecret、AccessToken、RefreshToken 均通过本机 Fernet 加密，API 只返回 AppKey 与缺失项，绝不回显密钥或 Token。
- 授权使用 Alibaba `author` 生命周期：本地保存应用 → 生成一次性 state 的 `https://auth.1688.com/auth/authorize.htm` URL → 运营在 1688 页面确认授权 → 本地 callback 校验 state 并换取 Token。终端仅将成功/失败显示给运营。
- 云牛顿应用不接受 `127.0.0.1` 回调时，默认使用 1688 可接受的共享回调 `https://auth.1688.com/auth/authCode.htm`。运营完成 author 后，将浏览器返回的完整 URL 粘贴到本地 `/api/v1/yunniudun/token` 交换；系统仍校验一次性 state，Token 不会出现在页面或日志中。
- 已核对文档：网关为标准 1688 `param2/1/com.alibaba.agent/{API_NAME}/{APP_KEY}` 表单请求，必须携带 `access_token`、`_aop_timestamp`、`_aop_signature`；`newtoncloud.task.get` 仅需 `taskId`（另可带 `fromIndex`、`includeBlocks`），授权用户由 Token 自动解析。
- 新增 `validate_existing_access_token()`：只对随机不存在的任务 ID 调用 `newtoncloud.task.get`，不创建、恢复、询盘或下单；自动测试覆盖接口名、签名和无副作用参数。2026-08-27 实测现有 author Token 被服务端判为签名不匹配，不能用于云牛顿，须在该云牛顿应用下重新执行 author 授权。未接入补采任务、自动采集、询盘或订单写操作。
- 新增 ERP 左侧“云牛顿接入”页面与本地接口 `PUT /api/v1/yunniudun/access-token`、`POST /api/v1/yunniudun/validate`：Token 使用密码框提交，保存后前端立即清空且接口不回显；验证必须经过页面二次确认，执行的仅是随机不存在任务 ID 的 `task.get` 探针。测试覆盖 Token 不出现在保存响应。
- 2026-08-27 已完成受控后端重启：先将全部自动采集任务（9–16）持久化为 `paused`，并将遗留 `running` 运行记录标为 `interrupted`、全量续采队列标为 `paused`，所有已有发现/采集/失败数量保留；重启后确认 `/health` 为 200，云牛顿状态接口为 200，保存与验证路由已注册。前端 `http://127.0.0.1:5500/index.html?ui=120#yunniudun` 验证密码框、禁用自动填充、保存/验证按钮已启用且无控制台错误。未启动任何采集、草稿、询盘、下单或 Ozon 发布动作。

### 2026-08-27 云牛顿本地补采任务（已验证）

- 新增 `yunniudun_supplement_jobs`，仅记录单链接补采的店铺、1688 链接、Offer ID、任务状态、云牛顿任务标识、原始/规范化结果、解析问题和审计信息。该表不关联草稿创建或 Ozon 写入。
- 新增 `GET/POST /api/v1/yunniudun/supplements`：POST 只创建本地 `draft` 任务，验证目标店铺和标准 1688 商品链接，返回“尚未调用云牛顿”。前端“云牛顿接入”页已增加按链接输入、店铺选择和本地任务表。
- `yunniudun_supplement.py` 将未来云牛顿返回规范为现有采集字段，强制 Offer ID 一致；公共图片、详情图片、SKU 专属图分离，无 SKU 时明确记 `missing_sku_variants`，不猜测变体组合。
- 已完成 Node/Python 语法检查及 `test_yunniudun.py`、`test_yunniudun_supplement.py`、`test_extension_bridge.py`、`test_open1688.py` 共 16 项通过。确认自动化任务为 4 个 `archived`、12 个 `paused` 且批量刊登仍为 `needs_review` 后，已受控重启后端；`/health` 与补采列表接口均为 200，新表已创建且没有恢复任务。真实浏览器用 Offer `822581032243`、店铺 `xymall2` 创建了本地任务 #1，页面显示“待启动 / 待云牛顿回包”，有对应审计事件，`provider_task_id` 和 `source_product_id` 均为空，控制台无错误。不得调用真实云牛顿 `task.create`，直到补齐官方字段和运营确认。

### 2026-08-27 1688 选品筛选工作台

- `frontend/index.html` 已将原“严选商品大厅”明确为“选品工作台”，展示严选 API 已连接状态、关键词/类目/价格/规则/官方标签筛选、热门关键词、分页、全选、尺重核验和采集所选入口。
- `frontend/app.js` 已补齐严选搜索、官方筛选条件加载、授权配置弹窗、候选卡片、Offer ID 链接、价格二次硬过滤、尺重核验及入库动作；入库复用 `/api/v1/open1688/jxhy/inspect-package` 和 `/collect`，不调用 Ozon 写接口。
- 真实浏览器验证：`冬季保暖`、5–30 元返回 2000 个接口候选，当前页 19 个可选，展示商品卡片和分页；超出 30 元的接口返回项未显示。勾选首个商品并点击“仅核验所选”后显示“缺少完整尺重”。页面控制台无 error/warn。
- 受限：云牛顿 Agent 搜索仍未接通；当前页面只提供严选 API 选品，真实云牛顿 `task.create` 继续等待官方详细参数确认。测试命令：`node --check frontend/app.js`、`pytest tests/test_open1688.py tests/test_yunniudun_supplement.py`（9 项通过）。

### 2026-08-27 批量重试上限与当前批次处理

- 批量 #1 的“需处理”按真实尝试次数处理：达到 3 次或以上的 49 条已归档放弃；1～2 次中可自动修正的项目已执行修正重提，其中 5 条最终以 Ozon `imported` 确认，仍有阻断回执的项目归档。
- 425 因 Ozon 店铺额度已满保持 `waiting_quota`，没有继续外发；350 因多个 SKU 复用同一张 SKU 图保持 `needs_review`，没有硬提交；历史失败 5 条未被这次操作误改。
- 成功重提后清空草稿当前错误快照，旧错误保留 `bulk_ozon_feedback_received` 审计；调度器对 3 次以上记录自动归档，单条历史回扫异常不会中断后续记录。
- 本次修正同时补齐 `sanitize_hashtags`、批量生成/内容生成/采集模块的 `re` 导入。相关回归测试 17 项通过，`compileall`、后端 `/health=200`、前端首页 `200`；未执行库存补推。

### 2026-08-27 云牛顿 task.create 已接入

- 用户提供官方 `newtoncloud.task.create` 详情：POST `https://gw.open.1688.com/openapi/param2/1/com.alibaba.agent/newtoncloud.task.create/{APPKEY}`；应用参数 `message` 必填，`sessionId`、`taskId`、`auto`、`fileUrls`、`fileNames`、`model`、`workflowName` 可选；响应含 `success`、`taskId`、`sessionId`、`status`、`error`、`errorCode`、`eagleTraceId`。
- 新增 `backend/app/integrations/yunniudun.py:create_read_only_task()`，按已有 1688 HMAC-SHA1 签名方式发送 `message` 与 `auto=true`，不把 Token 或密钥写入响应；官方错误码在异常信息中保留，便于区分并发/排队/小时/日配额。
- 新增 `POST /api/v1/yunniudun/supplements/{job_id}/start`。仅接受 `{confirm:true}`；任务行必须是本地 `draft`/`failed` 且未有 provider task，成功后保存 provider task/session/status 和审计事件，重复启动不会创建第二个任务。未确认请求返回 400，不触发外部调用。
- 前端补采任务表新增“启动补采”按钮，点击时明确提示可能消耗积分；启动后显示“已提交补采”。本次通过 `node --check`、云牛顿/补采/严选测试共 17 项，以及真实页面和接口检查；未发送真实 `task.create`。
- 该阶段已解除：官方 `task.get`/`task.fetch` 出参已取得并完成只读适配；真实任务仍须运营在页面明确确认后才允许 `task.create`。

### 2026-08-27 云牛顿任务查询与复杂表格回传

- `backend/app/integrations/yunniudun.py` 已实现 `get_task`、`list_tasks`、`fetch_task_table`、`list_models`：统一使用官方网关表单和 HMAC-SHA1 签名，不回显 Token/AppSecret。
- `task.get` 按数据库 `next_index` 增量读取并解析 `chunks`；补采只有收到 `END` 才入库，`WAIT_USER` 保持“等待人工验证”，`KILL` 保持失败并显示真实错误。
- 新增只读接口：`GET /api/v1/yunniudun/tasks`、`GET /api/v1/yunniudun/models`、`POST /api/v1/yunniudun/task-table`。复杂表格按 `result.data.result` 和 `fieldSpec` 返回，兼容采购场景 `newton/purchase/recall`。
- 验证：本项目明确 `PYTHONPATH=.` 后，云牛顿/补采/严选测试 21 项通过；`compileall`、`node --check frontend/app.js` 通过；无活动批量/采集任务后受控重启，`/health`、OpenAPI 路由和补采列表均返回 200。未调用真实 `task.create`、`task.resume` 或任何 Ozon 写接口。
- 2026-08-27 入口调整：候选商品行在缺少 SKU 证据时直接显示“云牛顿补采 SKU”。确认后前端调用本地创建/启动接口并自动轮询，不再要求运营复制链接进入“云牛顿接入”页；候选接口同时返回该链接最近补采任务状态和真实错误。

### 2026-08-27 严选采集与尺重门禁分离

- 严选页新增“尺重不完整也先入库”（默认勾选）和“去采集箱编辑”。勾选后请求 `require_complete_package=false`，只保存 1688 货源快照，不创建 Ozon 草稿、不提交 Ozon。
- 采集箱是严选采集后的编辑入口：选择目标店铺后进入“采集箱”，在对应商品行点击“编辑”。编辑器仍要求补齐重量、长宽高并通过预检，尺重门禁没有被绕过；取消勾选即可恢复原先严格跳过行为。
- 验证：真实严选页显示两个新控件，前端 `node --check frontend/app.js` 通过；后端 `/health` 正常。未调用真实严选写操作或 Ozon 写接口。

### 2026-08-27 1688 商品智能标题及卖点推荐

- 新增独立集成 `backend/app/integrations/image_product_intelligent.py`，对接 `com.alibaba.image:image.product.intelligent.generate-1`，配置文件为 `.local-secrets/image_product_intelligent.json`，与严选、云牛顿凭据隔离并使用 Fernet 加密。
- 新增 `GET/PUT /api/v1/image-product-intelligent/status|application|access-token` 和 `POST /api/v1/image-product-intelligent/generate`；旧 `/api/v1/open1688/intelligent-title` 也改为调用该独立配置。响应保留 CPV、卖点和标题建议，不返回任何凭据。
- 1688 严选页新增 AppKey（默认展示用户提供的 `4663405`）、AppSecret、AccessToken 密码框和安全保存按钮；商品编辑器标题区新增“1688 智能识别”，使用当前主图和已确认类目调用，建议标题需人工点击采用，CPV 的 `fid/vid` 只作证据不直接写入 Ozon 字典。
- 验证：`test_image_product_intelligent.py`、`test_open1688.py` 共 9 项通过；`compileall`、`node --check frontend/app.js`、`node --check frontend/listing-editor.js` 通过；受控重载后 `/health` 和智能标题状态接口均返回 200。未调用真实智能标题 API，等待运营在页面保存 AccessToken 后再使用。
- 入口调整：左侧新增“授权中心”，集中展示严选授权、云牛顿授权和商品智能标题授权；严选/云牛顿沿用原配置与页面，智能标题凭据表单直接显示。真实浏览器验证授权中心三栏、AppKey 预填、状态加载和“进入云牛顿授权”跳转；桌面截图确认布局，无新增控制台错误。
- 授权错误修正：官方返回 `Request need user authorized` 时，后端转换为明确中文提示，指出必须用商品智能标题 AppKey 重新 author 授权；授权中心新增该 AppKey 专属授权链接、回调 code 换 Token，且更换 AppKey 会清空旧 Token，避免跨应用令牌误用。回归测试共 11 项通过，授权 URL 接口返回 200。
- 授权 state 并发修正：此前后端只保存一个 `authorization_state`，验证/重复打开授权页会覆盖用户正在使用的 state，导致回调报“授权 state 不匹配”。现改为保存最多 5 个 15 分钟内的 state，成功换 Token 后消费匹配项；兼容旧单值配置和 query/fragment 两种回调参数。更换 AppKey 会清空旧 state 与 Token。验证：商品智能标题与严选回归测试 13 项通过，`compileall`、前端 `node --check`、`/health`、状态/授权接口均正常；未调用真实生成或 Ozon 写接口。

### 2026-08-27 批量徽章任务回传与库存修正

- 只读核对批量 #1：执行回合结束后为 274 条已导入、56 条 Ozon 回传需修正、5 条真实失败、其余等待；最后一批新增错误均为 Ozon `periodic_limit_exceeded`，证实实际创建额度已经耗尽，不能使用本地提交计数代替平台真实额度。
- 库存草稿分层结果：176 份已完成 Ozon 仓库库存回读；105 份仍等待 Ozon 导入任务生成完整 SKU；额度耗尽的草稿不再标为库存失败。真实内容/图片/属性问题仍保持 `import_failed` 并在批量行展示。
- `23171` 主题标签 payload 已改成一个 attribute value（值为完整空格分隔标签行），修复 Ozon `ERROR_ATTRIBUTE_IS_NOT_COLLECTION`。AI 仍以 20–30 个俄文标签为目标，数量不足不再被错误地当成“多值”。
- 新增品牌内容双层门禁：标题、描述、主题标签、4191 简介与 11254 富内容在生成提示词和提交预检均禁止品牌/厂家/店铺/供应商/平台（含 Ozon）；货源中 `brand` 及英文专有名词先清理，富内容欢迎语不再带店铺名。该规则不改 Ozon 的品牌字典属性（例如“无品牌”），只约束商品面向买家的文本。
- 额度回传修正：历史 `periodic_limit_exceeded` 批量行自动本地归类为 `waiting_quota`，不重发请求；在莫斯科 03:00 重置之前，所有已观察到额度耗尽的目标店铺会使批次显示“等待额度”且继续接口拒绝重复外发。重置后仍需运营明确点击“继续处理并提交”。
- 验证：`pytest tests/test_quality_preflight.py tests/test_listing_stock_monitor.py tests/test_image_product_intelligent.py tests/test_open1688.py` 共 21 项通过，`compileall` 与两个前端 `node --check` 通过。新增测试覆盖主题标签单值 payload、四个文本面的品牌清理，以及日额度错误不触发库存写入。后端重载前确认批量 `processing=0`；重载后 `/health=ok`、首页 HTTP 200、批量详情为 `waiting_quota`。真实浏览器刷新批量刊登页显示“可处理 2621 / 等额度 120”、状态“等待额度”，不再展示可点击继续按钮，控制台无 error/warn。未因本次修正调用新的 Ozon 写接口。

### 2026-08-27 库存真实回读审计与 warning 判定修正

- 用户指出不能以本地库存状态替代 Ozon 实际结果。已只读核对当日被库存流程处理的 107 份草稿、477 个 SKU：Ozon 商品已生成 477 个，425 个已为 `price_sent`，其中 398 个在 FBS 仓库库存回读中没有任何记录，只有 27 个与本地目标库存匹配；另 52 个仍为 `moderated/new`，尚不具备推库存条件。
- 根因得到 Ozon 单任务和商品详情双重确认：`OZE66808F4F5` 等 SKU 已 `imported`、有 `product_id`、已 `price_sent`，但导入回执把 `erased_attribute_value`（主题标签单值修正）放在 `errors` 数组且级别为 `warning`。旧监控虽识别 warning 不应失败，后续 `accepted` 条件仍错误要求 `errors` 为空，造成“已确认 0/N”并跳过库存接口。
- 修复：库存监控只以未解决 ERROR 对应的 Offer ID 阻断；`warning` 与允许的跨店重复错误保留审计但可继续进入 `price_sent → 库存写入 → 仓库回读`。新增自动测试覆盖 warning 不阻断库存。`pytest -q tests/test_listing_stock_monitor.py` 为 7 项通过，`compileall` 通过。
- 本次仅修改本地判断和测试，**未重载后端、未补推 398 个库存、未重试或写入任何 Ozon 数据**；待运营确认补推范围后，再按 Ozon 实际 `price_sent` 与仓库匹配结果执行。

### 2026-08-27 Ozon 回执主动修正与二次回测

- 批量调度器现在持续轮询已提交任务并保存逐 Offer 的 Ozon 错误/警告快照；成功导入但带 warning 的商品不会被覆盖回错误或跳过库存监控。
- 对 `ERROR_ATTRIBUTE_IS_NOT_COLLECTION`、主题标签品牌/营销、标题重复/无意义、原版/Ozon/平台词等确定性内容问题，调度器复用现有质量预检与 `auto-fix`，修复后复用 `submit_to_ozon` 重提并重新等待 Ozon 回执；每个商品最多自动修复重提两轮。
- 图片粗俗/冒犯文字、品牌证书/权属、额度耗尽、缺少尺重/仓库等不自动猜改，继续保留真实错误和“需处理/等待额度”状态。
- 已增加 `tests/test_automation_scheduler_feedback.py`，覆盖嵌套回执保留 Offer ID、可修文本与图片审核边界、额度不进入自动修复。`compileall` 与库存/质量/调度器相关测试通过；后端已重载并确认 `/health=200`、首页 `200`。未因该修正启动批量或补推库存。

### 2026-08-28 当前交接：库存对账与安全恢复

- 后端已在 `127.0.0.1:8000` 安全模式运行，`/health` 返回 200；`OZON_ENABLE_BACKGROUND_WRITES=0`，因此没有恢复批量外发、自动重提或库存补推。
- SQLite `quick_check=ok`，已改为 `NullPool + busy_timeout=60000`。异常退出留下的 journal 保留给 SQLite 自行恢复，禁止手删。
- 批量 #1“徽章”：item #323（draft #362，task #5517312785）已从遗留 `processing` 安全转为 `waiting_quota`，尝试次数 4，不会重发。当前汇总：等待额度 2736、已导入 281、需处理 7、失败 2、已归档 50；批次没有运行。
- 已实际执行五店 Ozon 只读库存对账。可售 `price_sent` 但 FBS 无库存（0 或无记录）为：店铺 1=90（7+83）、店铺 2=71（39+32）、店铺 3=11（1+10）、店铺 4=78（0+78）、店铺 5=0；共 250。尚未 `price_sent` 的行不应补库存。
- 店铺 1 的 `OZE66808F4F5` 已进入对账：`price_sent` 且无 FBS 库存记录；SKU 级复查发现它关联草稿 #280，目标库存 999、FBS 仓库 `1020002236187000`，已进入受控补推候选。此前按商品卡父编号查草稿会漏掉此变体级 Offer ID，禁止再用该口径下结论。
- 五店受控补推候选（已 `price_sent`、本地库存目标存在、仓库已匹配）共 134 条：店铺 1=40、店铺 2=20、店铺 3=7、店铺 4=67、店铺 5=0；另有 116 条可售缺库存但缺少本地库存/仓库证据，不能猜测写入。运营明确确认候选范围后才逐 SKU 写入并回读。
- 验证：`pytest tests/test_listing_stock_monitor.py tests/test_ozon_seller.py -q` = 19 passed；`python -m compileall -q app`、`node --check frontend/app.js` 通过。新增测试覆盖没有本地草稿的历史 Ozon Offer 仍进入对账。

### 2026-08-28 已执行 FBS 库存补录

- 运营明确确认后，按“`price_sent` + 本地库存目标 + 仓库匹配”范围调用官方 `/v2/products/stocks`，共计划 134 条。结果：店铺 1 40/40、店铺 2 12/20、店铺 3 6/7、店铺 4 67/67、店铺 5 0；成功 125 条全部由 `/v2/product/info/stocks-by-warehouse/fbs` 回读为目标库存，无回读不一致。
- 未成功的 9 条均为 Ozon `PRODUCT_HAS_NOT_BEEN_TAGGED_YET` / `tags validation failed`：店铺 2 为 8 条、店铺 3 为 1 条。未盲目重试；已回写 5 份关联草稿为 `waiting_tag`，消息为“等待商品标签校验”，下次复查最早 15 分钟后。
- `OZE66808F4F5` 位于店铺 1 的成功集合，已按草稿 #280 的目标库存 999 写入仓库 `1020002236187000`，并回读一致。
- 新增 `waiting_tag` 监控状态与自动测试：该 Ozon 错误延迟 15 分钟再复查而非 15 秒快速重发。库存/Ozon 测试现为 20 passed；未恢复批量发布或自动外发 worker。

### 2026-08-28 批量徽章：实时额度继续与暂停保护

- Ozon 实时额度已读取：店铺 1 剩 100、店铺 2 剩 97、店铺 3（xymall2）剩 59、店铺 4 剩 79，合计可创建 335；四店均未满。历史 `waiting_quota` 是旧回执，不能再作为永久锁。
- `POST /bulk-listing-batches/{id}/execute` 现会先逐店读取上述官方额度，只恢复有余额店铺的 `waiting_quota` 行；前端等待额度行显示“重新读取额度并继续”。
- 修复 worker 收尾覆盖暂停状态的问题：运营暂停后不能再被剩余 queued 行改回可继续。后端以 `OZON_ENABLE_BACKGROUND_WRITES=0` 安全运行，`/health=200`、前端首页=200；`compileall`、库存/回执相关 pytest 15 项与前端语法检查通过。
- 回归操作曾意外触发一次真实提交，已立即暂停。Ozon 任务 `5521303358`（店铺 4）回执为 16 个 SKU `imported`，伴随跨店重复 ERROR 与图片 URL warning；未启动后续批量处理。当前批次 #1 已恢复为 `paused_quality_audit`，统计为 submitted 282、imported 281、failed 112、queued 2625、needs_review 7。继续操作须由运营在页面明确点击。
- 后续受控“继续”发现旧额度失败行仍带 Ozon task ID，已再次暂停并修正：批量继续现在只选取 `ozon_task_id IS NULL` 的新队列；已有 task 的行只做回执跟踪，不会重新外发。修复后需重启安全后端并重新确认批次状态再继续。
- 继续排查发现历史 `queued/prepared/failed` 行也可能已经保存 task ID；新增统一隔离逻辑，批量列表/继续前会将这类行归入“已提交等待回执”或“等待额度”，从可重发队列中移除。
- 进一步修正批量恢复时间基准：数据库时间为 UTC 朴素值，旧代码用上海时区判断 stale，导致正在处理的行被页面刷新误回队列。现恢复和最新活动判断统一 UTC。
- 修复后继续执行批次 #1：后端安全模式仍为 `OZON_ENABLE_BACKGROUND_WRITES=0`，本次明确人工继续启动 200 条窗口；实时额度读取为店铺 1=91、店铺 2=95、店铺 3=58、店铺 4=79。当前实测队列从 2618 降至 2615，`processing=1`，`submitted=286`（接口提交数口径）、`imported=281`、`needs_review=8`、`waiting_quota=114`、`failed=2`；任务仍在运行，页面刷新可查看即时进度。
- 批量执行改为连续轮转：继续接口默认上限 10000，按店铺实时额度逐商品处理；某店额度耗尽时只冻结该店剩余队列，其他店铺不停。前端不再发送 `max_items=200`，运营无需反复启动 200 条窗口。
- 最新验证：后端 `/health=200`，当前批次无 `processing` 遗留，状态为 `paused_quality_audit`（需处理 8、失败 3、已提交 298、已导入 281、队列 2603、等待额度 114）。安全模式仍为 `OZON_ENABLE_BACKGROUND_WRITES=0`；下一次点击继续才会按新连续轮转逻辑运行。

### 2026-08-28 图片直链与富内容提交修复

- Ozon 回执中的 `pics_url_unsupported` / `some_image_failed` 已确认来自提交时仍使用 1688 `cbu*.alicdn.com` 热链；浏览器可打开不代表 Ozon 服务端可下载。提交入口现在会先将 Alibaba CDN 图片下载并镜像到已验证的公共 OSS，镜像失败直接阻止本次写入并保存清晰审计原因。
- 提交前拒绝 `None`、`null`、`undefined`、本地文件和无主机 URL；SKU 首图与公共详情图继续分离，不会因公共图 15 张上限砍掉 SKU 图。
- 修复富内容 `11254` 空占位属性遮挡真实 JSON 的问题：payload 始终恰好携带一个可解析的 `11254` 值，并在提交审计中记录图片数量、镜像数量及富内容存在性。
- 验证：质量预检测试 4 项通过，Python 编译通过；当前批次仍为 `paused_quality_audit`，未重启后台、未继续批量、未向 Ozon 发起新的写请求。修复后须先生成只读 payload 审计，再由运营明确点击继续。

### 2026-08-31 本地批量直提与后台库存闭环

- 运营明确要求批量任务为“生成即提交”，不得因库存回读、Ozon 导入回执或人工批准步骤卡住下一条。提交成功后只持久化 `task_id` 并排入后台库存/回执队列；下一条立即开始处理。库存监控与只读回执轮询在本机 PostgreSQL 环境启用，单次最多处理到期草稿 20 条，不扫描全历史。
- 后端重启会中断 FastAPI `BackgroundTask`。启动时现立即恢复没有 Ozon `task_id` 的遗留 `processing` 行到队列；已有 `task_id` 的行保持已提交、只等回执，绝不重发。若恢复后没有活动行，批次自动回到“可继续”，避免界面假显示“正在处理”。
- 重试/恢复时已生成的俄文标题和描述直接复用，不再重复调用 AI 造成数分钟停顿或改写既有文案；只有两项都为空的新商品才调用 AI。
- 验证：`python -m py_compile backend/app/main.py backend/app/automation_routes.py` 通过；本机 `/health=200`。遗留 item #651 在恢复后直接创建 Ozon task `5539603986` 并进入 `imported`；随后 item #655 创建 task `5539615497` 并进入 `imported`。这证明库存/回执未阻塞下一条直提。

### 2026-08-31 批量文案与库存回读修正

- PostgreSQL 迁移遗漏了 `ozon_error_patterns` 表，导致批量预检查询报错、标题/主题标签自动清理没有执行。现已将其纳入 ORM schema 并补回规则；缺表时使用保存点降级，绝不再让预检异常回滚草稿价、尺重和库存。
- 提交前的标题及主题标签清理扩展为营销、广告、促销、销售、品牌/平台、宣传性形容词；主题标签继续只保留 20–30 个以内的事实性俄文词，不因数量少一两个阻断提交。
- 旧 LLM 曾把提示词/推理文本写入标题。重试时仅复用“无中文、无提示词泄漏、无换行/Markdown、长度合法且俄文占比足够”的文案；否则重新生成，禁止导入 Ozon。
- 批量草稿在质量预检前先提交 SKU 库存目标，避免预检事务回滚后 `listing_variants.stock` 变为 NULL。库存监控改为“先仓库回读、仅写差额”；Ozon 返回 `TOO_MANY_REQUESTS` 时等待两分钟再回读，不连续重写。
- `listing_drafts.ozon_product_id` ORM 字段已改为 `BIGINT`，与 PostgreSQL 一致；此前 Ozon 的 6,175,411,989 级产品 ID 因 32 位绑定溢出，使真实库存已写成功却无法落本地完成状态。
- 验证：草稿 #729 的两个 Offer 在 Ozon FBS 仓库均实测 `present=999`，本地已回读为 `completed`；17 个库存为空的近期导入草稿已补回目标库存，后台首轮回读已完成 14 个。`py_compile` 和 `/health=200` 通过。
# 2026-08-31：Ozon 已导入商品的标题特殊符号自动修正闭环

- **事实**：xymall2 批量项 #691（草稿 #738，4 个既有 Offer）收到 Ozon `DESCRIPTION_DECLINE`、属性 `4180/名称`、`В названии много спецсимволов`。该类回执可能同时显示 `imported`，不能把它当作可销售，也不能按“3 次新建导入上限”直接放弃。
- **修正路径**：预检新增 `clean_title_special_symbols`，去除 CJK 标点、Markdown、斜杠和重复分隔符，只保留商品标题所需的字母、数字、空格、逗号和普通连字符。对 Ozon 已有 Offer，`/v1/product/attributes/update` 不会实际更新名称；必须复用完整提交构造器，以**相同 Offer ID**提交一次更新。Ozon 按 Offer ID 更新原商品，不创建新的 Offer。
- **持久化与回读**：更新任务号必须写入 `bulk_listing_batch_items.ozon_task_id`、`listing_drafts.import_task_id` 和 `pipeline_products.task_id`，状态置为 `submitted`，由既有轮询读取 `/v1/product/import/info`。不得因 API 接受请求就称成功；还要调用 `/v4/product/info/attributes` 回读 `4180` 名称。
- **实测证据**：任务 `5539860952` 返回 `imported` 且无错误；Ozon 回读 product_id `6175510449`、`6175510525`、`6175510547`、`6175510639` 均为清洗后的标题 `capybara - значок брошь - капибара - поднимает штангу`。
- **验证要求**：修改该流程后运行 `python -m py_compile app/quality_preflight.py app/automation_scheduler.py`；至少验证一次标题清洗样例、一次更新任务无错误、一次 Ozon 属性回读。
# 2026-08-31：标题与主题标签必须作为同一提交硬校验

- **根因**：旧批量只对主题标签做格式规范化，未把营销泛词、AI 提示词泄漏、中文、标题重复和名称超长作为提交阻断条件；旧“自动修复”还曾把 AI 返回内容未经二次验收直接写回标题。
- **规则**：`run_quality_preflight` 在实际提交前必须先自动清理标题符号和主题标签营销词，再校验标题：非空、≤255、无 CJK/提示词泄漏、俄文占比足够、无重复实词。任一项仍失败，写为 `issues_remaining` 并阻止 Ozon 提交。主题标签继续允许 20–30（或合法短集），但必须是俄文事实词；广告/促销/可靠性/轻便/风格/设计师等营销泛词自动剔除。
- **修复结果**：草稿 #647 和 #747 的标题与主题标签均已更新到其原 Offer；Ozon 任务 `5540085806`、`5540086511` 均回执 `imported` 且无错误。
- **验证**：标题 `булавка значок брошь значок + + +` 必须被判为重复词不合格；合规俄文标题通过；`#надежность #дизайнерский_значок #стиль #металл #рюкзак` 只保留后两个事实标签。

### 2026-08-31 批量提交门禁与 OCR 默认规则补强

- 根因：批量预处理确实调用了 `run_quality_preflight`，但未把其 `issues_remaining` 合并到批次阻断错误，因此标题重复、非俄文或提示词泄漏仍可能越过提交；同时新建界面会显式传递 OCR 的 `false`，覆盖后端默认值。
- 修正：提交前固定执行“自动清理 → 同一草稿复检 → 剩余硬错误写为 `quality_preflight_blocked` 并进入 `needs_review`”。主题标签少于 20 个不阻断；只有无任何有效标签才作为内容问题保留。自动修复的营销、品牌/平台、原版、特殊符号和主题标签问题继续直接回写后提交。
- 图片：中文尺重图与营销/厂家/定制/促销/OEM 图默认启用 OCR 过滤；被排除图不进入翻译，SKU 图仍独立持久化，来源快照不删除。批次 #1“徽章”已回填 `ocr_remove_marketing_images=true` 和 `ocr_remove_chinese_size_weight=true`；仅后续尚未处理的行适用，不倒改已生成草稿。
- 验证：`pytest tests/test_quality_preflight.py -q` 为 5 passed，`compileall` 通过；隔离后端 `:8001/health=200`，批次 #1 规则回读两项 OCR 均为 `true`，当前未启动新的 Ozon 写请求。

### 2026-08-31 生成阶段俄文异常兜底

- 根因：少数 AI 响应不是正常俄文商品文案，而是中文推理、提示词片段或俄文不足的混合文本；仅删除中文字符会留下不可发布的残片，并被提交门禁反复阻断。
- 修正：生成阶段先校验 CJK、提示词泄漏及俄文比例。仅当某字段不合格、且已选 Ozon 类目缓存存在标准俄文名称时，以该类目最后一级名称生成最小事实文案；例如“`Бижутерные украшения / Значок`”只会生成“`Значок` / `Значок. Бижутерное украшение.`”。不带品牌、营销词、供应商或未证实卖点；合格 AI 文案绝不覆盖。没有俄文类目证据仍进入门禁，禁止猜测。
- 验证：`pytest tests/test_quality_preflight.py -q` 为 5 passed；`pytest tests/test_pipeline.py -q -k "bad_ai_output or non_russian_category"` 为 2 passed；`python -m compileall -q app/pipeline/content_generation.py app/automation_routes.py` 通过。

### 2026-08-31 回执自动修正不得绕过提交门禁

- 根因：历史 `auto_fix_listing` 在“标题重复”场景会直接采用翻译服务输出，随后回执修正链路可不经复检重新导入；审计中已发现过中文推理文本被写入该标题。
- 修正：标题自动翻译先验证为可发布俄文才写回；异常时仅允许用已选全局 Ozon 类目的俄文末级名称兜底。`_try_auto_repair_and_resubmit` 在所有本地/AI 修正完成后再次执行 `run_quality_preflight(auto_fix=false)`；任何残余错误均进入 `needs_review`，绝不调用 Ozon 写接口。
- 运行：批次 #1 曾短暂停在当前条目边界加载此保护，确认 `processing=0` 后重启本机后端并恢复连续轮转；没有重发已有 task。恢复时官方剩余额度：店铺 1=85、店铺 2=70、店铺 3=0、店铺 4=70。
- 补充：判定标题时必须要求单行。不能先删除中文再仅凭残留的俄文例词判为合格；多行“规则/说明/Markdown”残片一律触发类目俄文兜底。实测批量项 #725 原多行推理标题被替换为 `Значок`，提交 task `5541413724` 后回执为 `imported`。

### 2026-08-31 后端退出与操作日志

- 退出证据：旧进程日志显示 SQLite `database is locked` 触发未处理请求异常；旧异常处理器在该异常路径又出现 `JSONResponse` 未定义，导致错误响应链再次失败。另有一部分手工启动的 uvicorn 绑定交互终端，终端结束时进程随之退出。当前正式库为 PostgreSQL，SQLite 锁日志属于旧运行实例，但两类问题都不能继续依赖人工重启。
- 修正：新增 `scripts/run_ozon_backend_watchdog.ps1`，以隐藏后台进程运行 uvicorn；进程退出会写入 `backend/logs/watchdog.log` 并自动重启。启动恢复只回收没有 Ozon task 的遗留 processing，已有 task 只轮询，不重复提交。已验证 `/health=200`，批量 #1 重启后可继续运行。
- 操作日志：新增 `/api/v1/operation-logs` 和左侧“操作日志”页面，读取 `audit_events` 的操作者、时间、店铺、动作、对象、结果详情，支持店铺/动作筛选。Ozon 回执、自动修正、库存同步和人工按钮动作沿用追加式审计，原始详情不覆盖。

### 2026-09-01 后端常驻运行保障

- **修正事实**：正式配置实测为 `postgresql+psycopg`、`is_sqlite=false`，数据库 `SELECT 1` 成功；`app.main` 已可直接导入 `JSONResponse`。因此历史 SQLite 锁库和 `JSONResponse` 未定义均不再是当前正式运行路径。
- **常驻方式**：`OzonERP-BackendWatchdog` 已登记为 Windows “用户登录时”计划任务，并立即启动；它独立运行 `scripts/run_ozon_backend_watchdog.ps1`，子进程为单 worker 的 Uvicorn。Uvicorn 非正常退出后，脚本记录退出码到 `backend/logs/watchdog.log`，等待 3 秒再启动，不依赖 Codex 或交互终端。
- **验证**：计划任务状态为 `Running`，守护 PowerShell 与其 Uvicorn 子进程均存在，`http://127.0.0.1:8000/health` 返回 `{"status":"ok"}`。2026-09-01 在批量无 processing 条目时，主动终止旧 Uvicorn PID `37700`，看门狗在 11 秒内启动 PID `36776`，健康检查恢复；批量 #1 的已提交/等待额度数量保持不变，证明重启不会重发已持久化 task。
- **前端同等保障**：此前 5500 静态服务仍由交互会话启动，后端正常时也会单独退出。现已新增 `scripts/run_ozon_frontend_watchdog.ps1` 并登记 Windows 计划任务 `OzonERP-FrontendWatchdog`；实测首页 `http://127.0.0.1:5500/index.html` 返回 HTTP 200（65810 bytes），同时后端 `/health=ok`。前端退出只会恢复静态界面，不会触发任何 Ozon 写操作。

### 2026-09-01 Ozon 图片尺寸与下载回执自动修正

- **根因**：草稿 #894（14 个 SKU）公共详情图均为 694×691 或 800×800 且 OSS HTTP 200；但 14 张 SKU 缩略图仅 105–178px。旧提交器按“SKU 图优先”把它们放入 `images[0]`，Ozon 因而同时回传 `primary_image_load_failed`、`pics_invalid_dimensions` 与 `some_image_failed`。
- **修正**：`_materialize_submission_images` 现在在镜像后对每张去重 URL 并行读取并解码，要求最小边 ≥200px、最大尺寸 ≤4320×7680。坏图只从当前 Ozon payload 排除；若仍有合规公共详情图，自动回退为首图并继续提交；没有合规图则在本地阻止提交。SKU 素材、公共图库和编辑器选择均不删除。
- **回执闭环**：上述三种 Ozon 技术性图片回执被归入确定性自动修复，调度器以相同 Offer ID 重建安全 payload 并重提（仍受三次真实提交上限和 Ozon 日额度约束）；图片文字/二维码/不雅内容审核不在此分支自动猜改。
- **验证**：草稿 #894 的实际 payload 复核为 14 个低分辨率 SKU 图全部过滤、14 个 SKU 首图均回退到合规的公共 OSS 图片；`pytest tests/test_quality_preflight.py -q` 为 7 passed，`compileall` 通过。

### 2026-09-01 404 图片不得在重试时复发

- **根因**：提交前虽然能识别 1688 图片 HTTP 404，但旧顺序先构造 OSS 镜像候选，再执行图片可下载预检；死图因此先进入 `fetch_and_upload`，直接把整张商品卡判为失败。批量重建草稿时又会从采集快照重新拼回该 URL。
- **规则**：必须先执行图片下载/尺寸预检，再建立 OSS 镜像候选；单张 404、无效尺寸或无法解析的公共图只从本次 Ozon payload、草稿公共图库和富内容中剔除，保留来源采集快照作为证据。若某个 SKU 无任何合规图片才阻断提交。确认失败的 URL 需持久化到草稿，重试不得重新带回。
- **验证**：新增 404 回归测试，验证死图不上传 OSS、有效图继续镜像；本次货源 `1069383716019` 的批量项 `933` 已成功导入任务 `5543818582`，公共图库保留 15 张有效图，4 个 SKU 库存已通过 Ozon 回读确认。

### 2026-09-01 批量额度满店铺自动让行

- **问题**：批量 worker 先按商品 ID 取 `max_items`，再在循环内判断额度。某个店铺已满时，它的前置商品会占满本轮窗口，后续有额度店铺无法及时接替；额度在本轮开始后耗尽时，还可能继续完成该店铺的生成链。
- **规则**：提交模式启动前读取批次全部目标店铺的实时创建额度，只把有剩余额度的店铺加入本轮取数范围；满额度店铺的未提交行保持 `waiting_quota`，有额度店铺继续按原分配顺序处理。全部店铺都满时，批次直接进入 `waiting_quota`，禁止反复生成。显式重试仍会返回该行的真实“等待额度”结果，不跨店改写已分配店铺。
- **验证**：后端重载后 `/health=200`；批次 #1 当前无 `processing`，已提交/导入任务和 Ozon task ID 未被重排。新代码已通过 `compileall`，下一次继续批量时会先按店铺容量过滤队列。

### 2026-09-01 批量明细表全选、排序、店铺筛选与实时刷新

- **根因**：旧逻辑错误地把“是否允许重新处理”绑定到“是否允许勾选”，因此已成功行变灰；表头勾选又只选择可重试行，违背了独立行选择与全局选择的基本表格行为。明细还没有更新时间排序、店铺多选或独立实时刷新，且每次刷新会把运营人员拉回表格顶部。
- **修正**：所有行勾选框始终可用，表头与工具栏的“全选当前筛选”覆盖全部可见行。批量重处理接口只把失败/需处理/等待额度等行重新排队，已成功或等待 Ozon 回执的选中行不改变并返回 `skipped_count`，因此全选后操作不再整体报错或误重提。默认 `updated_at` 倒序，提供正序切换；店铺表头为可勾选多选下拉。批次运行/回执中每 5 秒刷新明细，保留滚动位置、筛选、排序及已选集合。
- **验证**：真实浏览器打开 #1“徽章”任务（3076 行），“已成功”筛选时 539 个行勾选框均未禁用；单行可独立勾选，表头全选实测选中全部 539 行，按钮显示“批量重新处理所选（539）”。时间、店铺筛选与自动刷新验证保持通过；`node --check frontend/app.js`、前端 HTTP 200、后端 `/health=ok` 通过，浏览器无 ERP console error。
# 2026-09-01 Ozon 市场分析采集

- 扩展版本升至 0.7.2；Seller “Ozon 上的商品”页按钮会显示“采集当前分析表”。
- 新接口：`POST /api/ozon/market-snapshots`；新表：`ozon_market_snapshots`；迁移：`d913a86e51f0`。
- 已通过 `node --check`、单一 Alembic head、`test_ozon_market_snapshot.py` 与完整 `test_pipeline.py`（28 tests）。
- 尚需在用户已登录且加载新版扩展的 Seller 页面完成真实 DOM/点击/入库回读；未做此验证前不得宣称页面采集已经通过。

### 2026-09-02 款式分组变体与 8 图 AI 套图

- 编辑器变体表按款式分组合并显示：款式图和款式名称纵向合并，同一款式下保留每个尺寸 SKU 的价格、库存、尺重、属性和独立图片选择。款式单元提供“AI 作图”入口。
- AI 套图由商品级唯一任务改为“店铺 + 货源 + 款式键”唯一任务；每个款式只能使用该款式的 SKU 图作为唯一生图参考。槽位扩展为 8 张：首图、尺寸、细节、步骤、用途，以及居家、玄关、礼赠/中性场景三张。
- 应用结果只写入所选款式下 SKU 的 `image_urls_json` 和首图，公共详情图库及其他款式 SKU 保持不变；审计保留目标 SKU、款式键、前后图片集合和生成账本。单槽失败/重启中断仍不自动补发。
- 验证：`pytest backend/tests/test_visual_image_service.py backend/tests/test_listing_service.py -q` 为 35 passed；`node --check frontend/listing-editor.js` 与 Alembic 单 head 通过。尚未执行付费生图、Ozon 写入或真实浏览器款式表点击验收。

### 2026-09-02 Ozon 前台详情页插件采集入口

- 根因：公开 Ozon 商品详情页此前复用了列表页的“导入可见商品”按钮；详情页没有商品卡列表，按钮返回“未找到商品”，让人误以为插件没有 Ozon 详情采集能力。另一个问题是详情回传接口业务拒收时仍返回 HTTP 200，旧 popup 会误报“采集成功”。
- 修正：商品 URL `/product/...-数字` 上的悬浮入口改为“采集当前商品”，从已渲染的标题、图片、价格、属性、类目、评分和页面可见尺重/销量提示创建只读快照，发送到 `POST /api/ozon-learning/extension/detail-result`。列表页和 Seller 分析页原有入口不变。popup 和详情页均只有收到 `ok=true` 且 `ingested=true` 才显示成功，否则展示后端的店铺/标题等真实拒收原因。
- 验证：`node --check browser_extension/erp-collector-extension/{content,popup,background}.js`、manifest JSON 均通过；笔记本 `http://192.168.0.147:8000/health` HTTP 200；对详情回传接口提交无效店铺探针，确认返回 `ingested=false` 和 `storeId is required`，不会再误报成功。仍需运营重新加载 0.7.4 扩展后在登录的真实商品详情页点击一次，并回读采集箱新记录；在此之前不得称真实采集已验证。

### 2026-09-02 Ozon 内容安全归档闭环

- `OZ1D23E83C94` 所属的同卡共享图片经实际审计含跨性别旗帜及 `trans rights are human rights`、`respect trans women` 等文字；因此不是“描述改一下”可以修复的问题。
- Ozon 回读确认：`6179582062`、`6179582066`、`6179582101` 归档后均为 `is_archived=true / UNAVAILABLE`；`6179582127` 已不存在，不能伪报为已归档。草稿 #825 已标为 `archived`，同步任务亦停为 `archived`，禁止自动重提/库存回写。
- 新门禁：文本预检硬阻断、中文/英语/俄语 OCR 图文排除、Terra 生图分析声明 `content_safety`，以及全部生图提示禁止生成相关旗帜、符号和口号。回归：39 passed（质量预检、Ozon 客户端、OCR 门禁、AI 生图）。
