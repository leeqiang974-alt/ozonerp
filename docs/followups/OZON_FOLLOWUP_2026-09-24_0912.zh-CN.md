# Ozon ERP 跟进报告（2026-09-24 09:12 CST）

## 跟进目标

- 核对笔记本生产环境中每天 08:05 的自动采集任务是否执行。
- 若未执行则安全启动并查明原因；若已经执行则禁止重复启动。
- 核对同时运行的 Ozon 批量上架任务状态。
- 落实“两个项目的每次变更分别形成独立跟进报告”的强制规则。

## 变更前与环境事实

- 权威生产仓库位于笔记本 `C:\OzonERP`；工作站 `E:\new ozon erp` 不是权威生产仓库。
- 生产后端 `http://192.168.0.147:8000/health` 可访问。
- 用户明确要求忽略笔记本 Chrome 插件版本，本轮未处理插件。

## 实际检查与操作

1. 只读调用自动化总览、运行详情和批量上架状态接口。
2. 确认 08:05 任务已经正常执行，因此没有调用启动接口，避免重复采集。
3. 确认批量上架批次仍有持续活动，没有人工重复启动或重置队列。
4. 在工作站项目规则 `AGENTS.md` 中新增双项目独立跟进报告要求。
5. 本报告同时保存到工作站项目和笔记本权威仓库的 `docs/followups/`，供其他对话直接引用。
6. 本轮没有修改 Ozon 业务代码、生产数据库或生产任务配置。

## 后续更新：公共 Ozon 商品尺重回填（2026-09-24）

- 生产来源记录 `shop=1 / source_product=3039011467 / record=4192` 实际保存了 `weightG=198`、`lengthMm=110`、`widthMm=110`、`heightMm=60`；共有 6 个 SKU，编辑器可用商品级包装信息回填 SKU 的缺失包装字段。
- 修正笔记本权威前端 `C:\OzonERP\frontend\listing-editor.js` 的提示与定价门禁：尺重完整时明确提示已回填；缺少 CNY 采购成本时单独提示，不再误报为尺重缺失。
- 来源分流保持为两套逻辑：Ozon 公共页只将 RUB 作为平台售价证据，绝不当作 CNY 采购成本；1688 来源在采购价和尺重均为有效正数时，继续调用既有自动核价计算匹配售价。
- 防御性校验拒绝空值、零、负数、`NaN` 和 `Infinity`；即使 Ozon 历史负载异常带有 `price_cny`，也不会自动报价。
- 修改前备份：`C:\OzonERP-backups\20260924-093000\frontend\listing-editor.js`，原 SHA-256 为 `ed487ff44a226c1b4ae928087e459b5575de285abd3e26f3837236cff65c363c`。
- 验证：生产文件 `node --check` 通过，`/health` 返回 `status=ok`、`database=ok`，最终 SHA-256 为 `ffc4077f0d451a4915f8aafa96dbe290c2bc2212c2fe25430f661d56b887ae32`；独立复审无 Critical 或 Important。真实编辑页自动化验收在加载阶段超时，未记作通过，仍需人工打开该示例确认 6 行回填值与提示文本。

## 后续更新：报告归并规则修正（2026-09-24）

- 用户澄清：只保留 Ozon ERP 与 M.Video ERP 各一份累计跟进报告，不按每次对话或变更新建文件。
- 已将尺重回填记录归并进本报告，并删除重复的 Ozon 单次报告；笔记本 `C:\OzonERP\docs\followups\` 现仅保留本文件。
- `AGENTS.md` 已更新为“固定累计更新唯一报告”的规则。

## 后续更新：Git 全量留档（2026-09-24）

- 经用户明确授权，将笔记本 `C:\OzonERP` 的全部当前改动作为 GitHub 留档。
- 索引或历史变更前已完成仓库外完整备份：`C:\OzonERP-backups\git-prepush-20260924-0930`，并验证其中含 `.git\HEAD`。
- 已在 `codex/auto-incremental-sync` 创建提交 `3421603`（`chore: snapshot working tree 2026-09-24`，1479 个文件）；该提交包含用户授权的历史工作文件、脚本、媒体和日志快照。
- 两次 GitHub HTTPS 网络操作均被笔记本网络中断：`git fetch` 返回连接重置，`git push` 返回服务器空响应；因此提交仍只在笔记本本地，尚未到达 GitHub。待网络恢复后仅需执行 `git push origin codex/auto-incremental-sync`。

## 后续更新：VPN 后 GitHub 同步重试（2026-09-24）

- VPN 恢复后已成功 `fetch`，确认 GitHub 分支比笔记本旧基线多 4 个提交；采用非改写历史的 merge 整合。
- 冲突仅在 `content.js`、`manifest.json`、`popup.js` 三个扩展文件；保留笔记本当前生产版本，GitHub 旧版本仍由合并父提交留档。合并提交为 `5c7f5bd`。
- 推送大型完整快照传输约 90 秒后被 GitHub HTTP 408 中断；随后连接 443 再次不可用。当前不能宣称已推送成功，待 VPN/网络稳定后重试同一条 `git push origin codex/auto-incremental-sync`。

## 后续更新：源码责任边界（2026-09-24）

- 用户明确指定工作站为 Ozon ERP 源码编辑与 GitHub 留档的第一责任人；笔记本改为仅部署和运行。
- 已将该规则写入 `AGENTS.md`。当前工作站副本尚未与 GitHub 安全校准，迁移前不把它误称为唯一权威副本；笔记本外安全备份继续保留。

## 验证证据

- `task 19 / run 58`：状态 `completed`；2026-09-24 08:05:23 开始，08:21:10 完成；发现 5044、检查 4540、进入人工处理 82；无运行级错误。
- `task 20 / run 59`：状态 `completed`；08:21:11 接力开始，08:37:12 完成；发现 5041、检查 4527、进入人工处理 86；无运行级错误。
- 两个任务下次调度时间均为 2026-09-25 08:05:00。
- 2026-09-24 09:12:28 检查批量上架批次 1：`processing=1`、`queued=492`、`submitted=1884`、`succeeded=1837`，最近活动时间为 09:12:23，说明后台仍在推进。
- 同一时间后端健康检查返回 `status=ok`、`database=ok`。

## 结论

- 今天 08:05 的任务不是“未启动”，而是已经启动并完成；无需补启动，也不存在今天的漏调度原因。
- 9 月 20 至 22 日曾出现搜索超时，9 月 23 日曾在 package/search 阶段失败，但今天已经恢复成功。这些历史失败不能当作今天未启动。
- 批量上架批次状态字段为 `ready_to_continue`，但同时存在 1 个 processing 项且活动时间持续更新；应以实时处理数和活动时间判断，当前不是完全停摆。

## 剩余风险与建议优先级

1. 高：继续监控批量上架批次，若 `last_activity_at` 长时间不再变化，再按任务号与 Ozon 回执判定是否卡住；不能直接把 processing 行重排队。
2. 高：历史 Git 提交标题存在疑似凭据文本。应在凭据提供方撤销/轮换；不要通过生产仓库改写历史来仓促处理。
3. 高：笔记本权威仓库仍有大量未提交改动且与远端有差异。任何 Git 整理前必须先在 `C:\OzonERP` 外做安全备份并检查真实提交图。
4. 中：修正自动化总览中的状态命名，使 `ready_to_continue + processing>0` 不再给运营造成“任务停止”的误解。

## 恢复/回滚

- 本轮未启动、暂停、重试或更改生产任务，不需要任务回滚。
- 唯一持久变更是工作站 `AGENTS.md` 的报告规则；如需撤销，可删除“双方项目跟进报告强制规则（2026-09-24）”小节。

## 可复制到其他对话的摘要

2026-09-24 已核实 Ozon 08:05 自动任务正常执行：task 19/run 58 于 08:05:23—08:21:10 完成，task 20/run 59 于 08:21:11—08:37:12 完成，均无运行级错误，下次调度为 9 月 25 日 08:05。未做重复启动。批量上架批次 1 在 09:12 仍有 1 项 processing，最近活动持续更新；本轮未改业务代码、数据库或任务配置。

## 后续更新：M.Video 单 SKU Dry-run 信息框整合（2026-09-25）

- 需求：先做一个可查看效果的轻量原型，在插件显示的 Ozon 产品信息框下方提供 M.Video 单 SKU 预览入口，并将红色“Ozon 当前商品采集”框一并放入同一信息框；不新增独立页面、不扩展复杂功能页。
- 版本：扩展、内容脚本与 popup 握手版本统一为 `0.7.73`。
- 信息框层级经真实 Ozon 页面核对为：标题栏 → `ozon-erp-sidebar-content` 产品信息 → `ozon-mvideo-preview-button` → `ozon-mvideo-single-preview` Dry-run 面板 → `ozon-embedded-collector` 红色采集框；所有内容位于同一个浮动框内，通过滚动查看。
- 点击“M.Video 单SKU预览”后，按钮切换为“收起 M.Video 预览”，面板展示当前 SKU、规格、Ozon 市场价、无品牌、标题/描述待模型重构、类目合规待预检及包装单位换算结果；再次点击可收起。
- 原型严格保持 Dry-run，明确显示“不会真实发布”；Ozon RUB 市场价只作为市场价格证据，不进入 CNY 采购成本；包装按 `mm ÷ 10 = cm`、`g ÷ 1000 = kg` 精确换算。
- 发布门禁在缺少 CNY 采购价、M.Video RUB 售价、库存、标题重构、描述重构、类目合规预检时正确阻止；真实示例的包装尺重门禁按已采集数据通过。
- 验收过程中没有点击红色框内“采集当前商品”，也没有触发真实发布；因此未向 ERP 写入新货源或产生发布侧副作用。
- 自动化验证：`node --check browser_extension/erp-collector-extension/content.js`、`node --check background.js`、`node --check popup.js` 均通过；`node tests/ozon-public-structured.test.js` 通过，输出 `ozon structured public-page parser tests passed`。
- 本轮只处理 Ozon 扩展单 SKU Dry-run 链路及其累计报告，未修改 `E:\mvideo`，不更新 M.Video 报告。
- 交付路径：工作站提交并推送 `codex/auto-incremental-sync` 到 `git@github.com:leeqiang974-alt/ozonerp.git`；笔记本后续只从已验证提交拉取和部署。
